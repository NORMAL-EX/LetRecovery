# 历史 DiskPart 脚本目录（已停用）

LetRecovery 已不再生成或执行 DiskPart 脚本。磁盘、分区、卷、盘符、格式化和扩缩操作统一通过内置的参数化 VDS、WinAPI 与 IOCTL 边界执行。

旧版配置字段只为兼容读取而保留。若安装目录中仍存在 `.txt`、`.cmd` 或 `.bat` 磁盘脚本，程序会停止并报告错误，不会自动转换、猜测含义或回退执行。请改用程序内置的“一键分区”和磁盘工具。
