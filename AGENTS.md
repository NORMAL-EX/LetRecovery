# LetRecovery Agent Development Guide

## 不要为了所谓的安全，让软件变得更不安全

安全设计必须降低**总体实际风险**，而不是单纯增加校验数量、变量数量或代码复杂度。任何新增门禁都必须同时比较：它要防御的攻击在真实威胁模型中发生的概率与后果、攻击者已经具备的权限、该门禁能否真正阻止攻击，以及它引入的兼容性问题、误报、安装失败和无法恢复的概率与后果。若一个措施只能略微提高同权限攻击者的成本，却显著提高正常用户安装失败的概率，它不是安全增强，而是在让软件变得更不安全。

- **优先使用最少、稳定、直接且可解释的授权条件。** 能用一个本次会话生成的高熵随机 marker、受认证配置中的同值和“唯一精确匹配”完成目标重绑定时，就不得再叠加磁盘号、分区号、GUID、容量、卷标、可用空间、整盘布局摘要等易变库存字段。其它分区出现同名但内容不同的 marker 只是环境噪声，必须静默忽略，不得升级为错误。
- **指纹不是真实性。** GUID、磁盘号、分区号、容量和布局既可能因固件、驱动、克隆、热插拔、WinPE 枚举和分区操作而合法变化，也可能被具备足够权限的攻击者修改或伪造。不能为了防一个已经能够修改磁盘元数据的攻击者，让大量正常机器承担更高的误判和安装失败概率；需要真实性时使用明确的随机会话值、HMAC 或签名，需要防瞬时换绑时只在首次破坏性写入边界对当前对象做一次必要确认。
- **按风险和发生概率合理设计。** 评审安全逻辑时必须写清威胁、攻击前提、预期收益、正常环境中的变量数量、误报面、失败后果和恢复方式。优先选择能消除高概率真实事故、且误报面小的措施；不得用“理论上更安全”代替概率、后果和实测依据。复杂方案只有在简单方案无法覆盖明确的高风险场景，且具有真实环境测试证明不会显著降低成功率时才允许采用。
- **安全门禁必须有停止条件。** 同一事实已经由权威 API、受认证 marker 或操作后回读证明后，不得在每个后续阶段以不同库存字段重复证明。重复校验若读取的是会自然变化的状态，只会制造新的竞态和失败点。
- **错误必须按实际影响分级，禁止以小失大。** 返回错误不等于必须终止整个任务。每个错误处理点都必须回答：失败的对象是不是用户明确要求的核心结果；缺少它是否会让系统无法启动、写错目标、损坏已有数据或使后续操作必然不可信；继续执行能否仍然得到可用且可说明的结果。单个不必要的 Wi‑Fi、打印机、虚拟机增强、外设或其它非启动存储驱动未导入，单个可选语言包、更新包或美化组件失败，只能记录有界 warning 并继续，不得让已经完成镜像释放的整次安装失败。只有启动存储覆盖、核心镜像完整性、明确目标、引导、用户明确选择的必需组件或其它真正承重结果失败时，才允许失败关闭。批量操作必须隔离单项失败并在最后报告总数和有界样例，禁止因为一个非必要项失败而跳过后续所有必要步骤。
- **破坏性边界之后更要避免扩大失败。** 删除、格式化或首次覆盖原系统后，流程已经不能恢复旧系统；此后新增校验和可选步骤不得轻易把可用的新系统变成“安装失败”。后破坏阶段应优先完成镜像、启动、启动存储和最小可用系统，再汇总非关键缺失；只有继续执行会写错对象、扩大数据损坏或产出确定不可启动系统时才停止。

### 系统 API 必须先完整查清再动手

以下是实现和审查的强制阻断规则。禁止只搜到一个函数名、看到一个示例或凭参数名称猜含义后就开始修改：

1. 使用或改变 Win32、COM、VDS、SetupAPI、DISM、WIM、BCD、注册表、文件系统或驱动相关边界前，必须先查全该接口的**函数契约、所有参数和单位、输入输出结构体及 union 字段、标志位组合、异步结果数据体、返回码与 success warning、对象生命周期、共享模式、操作后的权威回读方式、最低 Windows 版本和当前依赖版本中的实际声明**。
2. 必须沿仓库调用图检查公开封装、薄适配层、正常端、PE 端、mock、测试、错误转换和文档，确认同一语义没有第二套旧实现。禁止只改最外层调用或一个副本；禁止函数签名改了而数据体、调用方式、返回值解释、恢复路径和测试仍沿用旧语义。
3. 技术资料优先使用微软官方文档、协议规范、Windows SDK 头文件和仓库锁定依赖的实际类型声明。示例代码、论坛和搜索摘要只能用于发现线索，不能替代原始契约。若官方资料存在版本差异，必须同时核对 Windows 7 最低版本，并提供可证明等价的 Win7 回退；没有可靠依据时不得进入承重或破坏性路径。
4. 调研结论必须落实到代码注释、回归测试或架构文档，至少记录最容易误用的参数单位与特殊值。例如 `IVdsCreatePartitionEx::CreatePartitionEx.ulAlign` 是字节数，`0` 才表示由 provider 决定；不得把 `1` 猜成布尔式“启用默认对齐”。
5. 完成前必须再次搜索 API 名称、结构体名、HRESULT/Win32 错误码、旧参数常量和所有调用点，并逐项确认生产路径、回滚路径、重试路径和测试使用同一契约。缺少任一层的检查，功能不得标记完成、不得打包。

### 高误报、低收益阻断检查的强制禁令

Shrink 的 COM/异步 `Wait` 成功不代表调用方后续 `Refresh` 或 IOCTL 回读一定成功。正常端在仍可逆阶段收到共享 Shrink 错误后，必须重新读取同一当前卷：只有观察到同磁盘、同起点的实际尾部缩小时，才可按观察到的字节数扩回；范围未变化时不操作，查询失败、换绑、范围增长或其它异常时严禁按请求值、估计值或异步数据体盲目扩容。错误必须同时保留原失败与恢复结果。

`CreatePartitionEx` 一旦成功启动异步操作，后续 `Wait`、`Refresh` 或首次布局回读失败都不能证明分区尚未创建。共享边界必须重新读取 canonical 当前布局：布局未变时返回原错误；只有观察到唯一新增、角色正确、容量满足最低请求且完全包含在本次选定授权 envelope 内的当前分区，才可按该实际 extent 精确删除；额外变化、歧义、越界或回读失败必须报告 partial state 并保留现场，禁止按请求 offset 猜测删除。

以下条款是代码审查和完成验收的合并阻断项，不是建议。违反任一项时必须撤回该检查、修正生产路径和回归测试；不得用“更安全”“通常如此”“绝大多数机器如此”作为保留理由：

1. 严禁把经验布局、默认值或外观整齐度当成磁盘操作的合法性条件。严禁要求 Windows、VDS 或 IOCTL 已返回的真实 offset、extent length、缩卷结果、空闲 extent 或磁盘容量必须是整 `1 MiB`、固定 `4 KiB`、`512 B`、固定比例或其它人为边界。
2. `1 MiB` 只能在没有显式 provider extent 时作为新布局起点的非阻断性偏好。首选位置放不下时必须回退使用 provider 返回的合法 extent。显式 offset 必须原值使用，禁止先 `align_up`、`align_down`、截断或取整后再比较或写入。
3. 确有原始磁盘 I/O 对齐要求时，只能读取当前磁盘实际逻辑/物理扇区大小，并按真实设备约束处理；禁止使用固定常量猜测。设备对齐查询失败时停止对应原始 I/O，不得编造默认值继续。
4. Windows provider 返回的当前 extent 是当前会话事实。阻断检查只允许验证：范围非空、checked overflow、完全包含在当前空闲或用户选择范围内、不与受保护范围重叠、系统调用结果，以及操作后的实际布局回读。不得把 GUID、容量、历史长度、理想化几何或“请求值必须与 provider 实际值逐字节相等”额外升级为合法性门槛。
5. 新增任何会阻断安装、缩卷、建分区、格式化、搬移、暂存回收或引导修复的检查，必须在同一批修改中提交：明确的真实威胁或故障模型、真实故障样本或权威 API 契约、现有范围检查和操作后回读为什么不足、正常环境误报分析，以及非典型但合法 Windows 几何测试。缺少任一项不得进入阻断路径。
6. 一旦日志或虚拟机证明某检查发生误报，必须删除错误前提并全仓搜索同类代码。禁止只放宽常量、增加系统版本白名单、吞掉错误或把检查搬到另一层。
7. 涉及磁盘的测试必须包含扇区对齐但非整 `1 MiB` 的 offset/length，以及 provider 实际返回值与请求值略有差异、但仍处于合法包含范围并满足最小容量的场景。测试必须证明范围安全，不得把理想请求几何固化为新门槛。
8. 完成磁盘相关修改前必须全仓搜索 `% 1048576`、`% (1024 * 1024)`、`is_multiple_of`、`align_up`、`align_down`、固定 `1 MiB`/`4 KiB`/`512 B`、`unexpected geometry`，以及对 offset、size、length、GUID、磁盘号的阻断判断；必须逐项删除，或在代码和最终报告中说明权威依据与其保护的真实写盘边界。
9. 跨重启任务只允许用本次密码学随机、非空的会话 marker/config 精确配对来定位。不同 SessionId 的旧 marker、旧配置和同名异内容文件必须静默忽略；磁盘号、盘符、磁盘/分区 GUID、容量、旧分区长度和整盘摘要只能用于日志或当前会话诊断，禁止叠加成跨启动身份指纹。
10. 系统 API 的权威执行结果和操作后回读优先于独立启发式预检。不得把查询/预估当成实际写入能力，不得因 provider 返回值与请求略有差异但仍合法而失败，不得恢复普通驱动的独立签名阻断链。环境服务缺失等明确诊断只能解释真实 API 错误，不能替代真实 Shrink、Create、Format、DISM 或 BCD 结果。

涉及正常端/PE 磁盘交接、全盘重装、双系统、VDS 或目标身份识别时，还必须完整阅读 `docs/PE_DUAL_BOOT_HANDOFF_LESSONS.md`。没有虚拟机日志跨过原故障位置时，只能声明代码和非破坏性测试完成、等待虚拟机或实机验证。

本文档适用于整个仓库。它既是开发者的架构索引，也是代码模型在修改 LetRecovery 时必须遵守的开发契约。

LetRecovery 是具有管理员权限的 Windows 系统安装、备份和磁盘维护工具。兼容性、可恢复性和防止误操作始终高于重构速度、代码简短程度或界面效果。

## 文档同步是完成定义的一部分

任何开发者或代码模型在结束一次功能开发前，都必须执行以下检查：

1. 每完成一个新功能，无论是否新增文件，都必须同步更新本文档中相关文件的职责描述、扩展路径或安全约束；不能以“仍然使用原文件”为理由跳过。
2. 检查本次是否新增、删除、移动、重命名了 `.rs` 文件，或改变了现有文件的主要职责、公开接口和安全边界。
3. 发生上述变化时，必须在同一批修改中更新本文档的“Rust 文件职责目录”和相关开发说明。
4. 新增的每个 `.rs` 文件必须在职责目录中出现且只出现一次；删除或移动文件时必须同步删除或修改旧条目。
5. 新功能涉及正常系统端和 PE 端时，必须同时检查两端和 `lr-core`，不得只更新一个副本。
6. 如果在开发或审查时发现本文档遗漏、描述过时或与代码不符，应立即补齐，不要等待单独的文档任务。
7. 向用户汇报完成前，应明确说明本文档更新了哪些内容；如果只是修复而没有改变职责，也必须说明已经复核且无需修改职责目录。

如果代码和本文档冲突，以经过验证的代码行为为准，但必须在当前修改中修正文档。不能为了符合文档而猜测或悄悄改变现有行为。

## 仓库结构与依赖方向

- `lr-core/`：两端共享的核心策略、纯逻辑、Windows 适配和可测试命令边界。
- `正常系统端/`：桌面环境主程序，负责系统安装、备份、在线下载、工具箱和写入 PE 启动配置。
- `PE端/`：WinPE 中运行的安装、备份、扩容和离线系统处理程序。
- `assets/`：发布包资源、语言文件、工具和内置运行时文件。
- `docs/`：架构、安全、第三方二进制来源及用户文档。
- `.github/ISSUE_TEMPLATE/`：问题与功能反馈表单；Bug 表单只要求用户描述现象、复现步骤、上传对应阶段日志和截图，不得再要求手工抄写日志已经自动记录的软件版本、源/目标系统、固件、Secure Boot、BitLocker、磁盘结构、所用 PE 和实体机/虚拟机线索。
- `.github/workflows/`：PR、主分支和发布流水线；release 在替换 PE 原生 Win32 EXE 时必须同时从底包 WIM 删除旧 `opengl32.dll`，不得把已停用的 egui/glow 运行时重新带入发布包，并必须确保 WIM 中存在 `Users\Default\AppData\Local\Temp`，避免未显式指定 `/ScratchDir` 的 DISM 在 WinPE 中因默认 `%TEMP%` 目录缺失而以错误 3 失败；正常系统端的 `assets/release/bin/dprk_easter_egg.mp3` 必须经过大小和复制后 SHA-256 校验再同步到最终 `pkg/bin/`，不得注入 PE WIM；从外部底包生成正式或预发布包时必须由 `.github/scripts/normalize-release-config.ps1` 在 CI 中直接生成与 `AppConfig`、`InstallPrefs` 和可持久化高级选项默认值同步的确定性安全配置，不得依赖工作区或仓库根级 `config.json`，并以此重建整份 `pkg/config.json`；只能保留本次构建并重新计算的 `pe_cache` 元数据，严禁继承底包维护机的界面偏好、提示关闭状态、本地路径、账户选项或安装选项，并必须显式保持 `log_enabled=true`、`easy_mode_enabled=false`、`easy_mode_tip_dismissed=false`、`easy_mode_settings_tip_dismissed=false` 和 `enable_advanced_options=false`；重建必须原子替换并回读验证，最终 `LetRecovery.7z` 还必须重新解出 `config.json`，验证它与已审计 `pkg/config.json` 字节哈希一致且没有生成默认值以外的字段，任何缺失、解析失败、残留或回读不一致都必须阻断发布，离线安装器也只能从同一份已审计 `pkg/` 构建；正式或预发布 Release 必须在最终 `pkg/` 生成后使用固定 Inno Setup 6.7.1 构建离线安装器，完整校验后只上传 `LetRecovery.7z` 和 `LetRecovery-Setup-x64.exe` 两个资产。只有 Release 创建成功后，流水线才允许使用 `RELEASE_PAT` 检出私有仓库 `NORMAL-EX/letrecovery-website`，通过 `.github/scripts/update-website-version.ps1` 将本次标签规范化后写入其根目录 `version.json`，并只提交该文件到网站仓库 `main`；失败流程不得留下网站版本提交，版本提交必须防止递归触发 CI。最终 `pkg/`、7z、安装器和本地 H 盘解压包必须复用 `.github/scripts/assert-release-tree-clean.ps1`，拒绝运行日志、开发产物目录、调试中间文件和 reparse point。
  从底包进入最终 `pkg/` 的所有 `*.log` 都是旧运行实例的诊断材料，流水线必须在替换二进制后删除并再次枚举确认不存在；本地 ISO、7z 和安装器打包也必须执行同一检查，禁止把开发机、维护机或虚拟机日志分发给下一位用户。
- `installer/`：基于 Inno Setup 6.7 现代动态主题的 x64 离线安装器工程，从完整 `pkg/` 生成支持静默安装和卸载的单文件 `LetRecovery-Setup-x64.exe`；CI 与本地构建必须复用 `build-installer.ps1` 的包结构、版本和输出校验，不能在工作流中复制另一套编译逻辑。

发布配置归一化还必须显式保持 `automation_export_enabled=false` 和 `pe_maintenance_entry_enabled=false`；自动化导出与手动 PE 维护都是用户主动开启的高级入口，不得被底包、维护机偏好或旧 `config.json` 带入最终发布包。已删除的 `mica_enabled` 是可忽略的旧配置字段，当前 `AppConfig` 和归一化发布配置均不得再生成它。

官网源码由独立私有仓库 `NORMAL-EX/letrecovery-website` 维护，本仓库不得重新引入旧 `官网/` 目录或复制网站构建流程。网站显示版本只能来自该仓库根目录受版本控制的 `version.json`，普通构建和部署不得根据当前时间动态改变版本；本仓库的 `.github/scripts/update-website-version.ps1` 是 Release 标签规范化、原子写入和回读验证的唯一入口。

依赖方向必须保持为：正常系统端和 PE 端可以依赖 `lr-core`，`lr-core` 不得反向依赖任一端。两端出现相同的纯逻辑、命令构建或 Windows API 适配时，应优先迁移到 `lr-core`，端内保留兼容再导出或很薄的环境适配。

## 不可违反的安全规则

### 禁止在开发机和普通 CI 中执行真实破坏性操作

自动测试、代码模型和普通 CI 禁止真实执行以下操作：

- `format`、`format.com`、DiskPart 写操作和卷删除；
- DISM 镜像释放、捕获或对宿主机、测试磁盘、VHD/VHDX 中离线系统的写入；
- BCD、ESP、引导扇区和活动分区修改；
- 分区创建、移动、扩容、写盘或镜像还原；
- 离线注册表注入、SAM 修改、重启或关机。

相关测试必须使用纯函数、`DryRunCommandExecutor`、mock、临时普通文件或显式 preview。必须进行真实验证时，只能使用可丢弃虚拟机、可丢弃 VHDX 和专用测试磁盘，并由人工明确启动。

发布包 WIM 维护是唯一例外：用户在当前任务中明确授权后，代码模型可以更新仓库工作区内用户明确指定的发布包 WIM；该操作属于构建产物维护，不得借此挂载或修改宿主机、测试磁盘、VHD/VHDX 中的离线系统。执行时必须同时满足以下条件：

- 先解析并复核 WIM、编译产物、挂载目录和最终目标的绝对路径；WIM 与最终目标必须位于当前仓库工作区，挂载目录必须是本次创建的碰撞安全空临时目录。
- 只能先复制到碰撞安全的临时 WIM，再对临时副本执行挂载、文件替换、过时运行库删除、提交和导出清理；原 WIM 在全部步骤验证成功前不得修改。
- 注入范围只能是本次已构建并验证的 PE 程序、发布流程规定的固定离线资源，以及用户明确要求删除的过时发布运行库；不得注入任意宿主文件或改变 PE 的系统配置、注册表、驱动和引导设置。
- 必须检查每条 DISM 命令的启动结果和退出码；任何失败都要卸载并丢弃临时副本，清理挂载目录，保留原 WIM 不变，禁止在状态不明时继续或提交。
- 提交后必须导出到新的干净 WIM，只读复核镜像索引、内部目标文件、已删除文件、PE 导入依赖和文件哈希；全部通过后才可原子替换原 WIM，并同步更新 `pkg/config.json` 中对应的 MD5、SHA-256 或大小字段。
- 需要管理员权限时必须通过显式提权审批，不得静默绕过；授权只覆盖本次指定的发布包 WIM，不覆盖任何真实磁盘、分区、引导或离线系统操作。

### 危险命令的实现要求

- 内置磁盘、分区、卷、盘符、格式化、扩缩、活动标记和 MBR 签名操作统一复用 `lr-core::windows_storage` 的参数化 VDS/WinAPI/IOCTL 边界，不得再生成或启动 DiskPart 脚本；历史 `RunDiskpartScripts` 配置字段只为旧配置可读，UI 和新配置必须固定关闭，发现旧 `.txt/.cmd/.bat` 脚本时失败关闭，禁止自动猜测转换或回退执行。
- 本机账户枚举和更新、注册表读写/配置单元装卸、固件模式探测、文件版本读取、当前 Wi-Fi profile 获取、系统重启安排、文件属性修改、文件树复制、可执行文件路径搜索和既有计划任务触发必须复用仓库内已核对微软文档的参数化 Win32/COM 边界，不得回退解析 `net.exe`、`reg.exe` 查询输出、PowerShell、`netsh wlan show/export`、`shutdown.exe`、`attrib.exe`、`xcopy.exe`、`where.exe` 或 `schtasks.exe` 的本地化文本。`.reg` 文件导入保留 `reg.exe import` 作为 Windows 自有格式解析兼容边界，调用前仍须验证普通文件和路径。
- 新进程执行优先通过 `lr_core::command::CommandRequest` 和 `CommandExecutor`。
- 程序及参数必须逐项传递，不得用字符串拼接构造 shell 命令。
- 只有确有兼容需求时才能使用 `cmd /c`，并必须严格验证所有可变输入和命令元字符。
- 盘符、磁盘号、分区号、文件系统、卷标、路径、URL 和服务端字段必须在进入系统命令前验证。
- 写操作必须检查进程启动结果、退出码、stderr、工具可能返回的文本错误以及操作后的可观察结果。
- VDS 格式化默认不得强制卸载占用中的卷；只有安装流程已证明运行程序、日志、镜像及其它后续输入均不在目标卷上时，才可显式请求强制卸载，以兼容 WinPE 中只读探测遗留的卷句柄。镜像源与目标同卷时必须在格式化前停止；即使用户关闭格式化，也必须在紧邻首次目标写入处执行同一来源和后续依赖守卫。格式化入口只通过实际 `IVdsVolume` 对象核对 marker 当前匹配卷的 extent，成功后回读 extent、文件系统和卷标；这一次确认完成后，镜像释放、驱动、更新、PCA、引导、高级项和无人值守不得再按易变库存重复认证同一安装目标。只有自动暂存分区删除、扩缩容等会改变分区拓扑的独立事务，才继续使用其各自受认证的 canonical source/temporary pair 与操作后布局回读。
- 批量格式化、快速分区以及卷扩缩等存储工具必须把确认时的完整卷身份带到最终 VDS 调用，执行前同时复核盘符和实际 `IVdsVolume` 对象，执行后回读物理范围；不得把“未请求强制卸载”当作错卷保护。分区创建、删除、活动位和盘符访问路径变更同样必须携带共享 IOCTL 生成的规范磁盘布局快照并走 checked 边界，快照缺失或查询失败时停止；创建、删除和活动位修改的后置布局只能包含本次明确授权的精确 delta，拒绝额外并发拓扑变化，MBR extended container 仅可为容纳或移除同一次逻辑分区而保持起点不变地窄幅扩缩。每次拓扑变化后必须重新抓取快照再授权下一步；离线块移动必须在首次原始写入前通过同一个已打开的 `PhysicalDrive` 写句柄同时复核 layout、capacity 和 `StorageIdAssocDevice` 身份，关闭句柄后仍须以移动前的强卷身份和规范快照重新复核，禁止继续按可能复用的磁盘号操作。普通多 extent 卷格式化可按稳定卷 GUID 绑定，但安装目标和基础分区扩缩仍须拒绝无法表示为单一精确 extent 的卷。VDS 查询、写操作启动、`Refresh` 和异步 `Wait` 原则上必须精确返回 `S_OK`；success warning 只能按微软针对具体接口的文档逐项分类。`IVdsVolume::GetProperties` 返回 `VDS_S_PROPERTIES_INCOMPLETE` 时，若调用点只消费已经返回且严格验证的 `pwszName`，并随后从该卷句柄通过 IOCTL 回读权威 extent，则不得因其它无关属性缺失误阻断；名称缺失、格式非法或句柄/extent 回读失败仍须停止。异步操作结果中的 `Shrink` `VDS_S_NO_NOTIFICATION` 与 `Clean` `VDS_S_DISK_PARTIALLY_CLEANED` 必须在刷新和严格后置条件收束成功后才可继续，其它未识别 warning 一律失败关闭；同步 `DeletePartition` 的 success warning 也必须刷新和回读，但无论后置状态是否已变化都只能报告明确的 partial-state 错误，不能伪装为完整成功。Clean 前必须卸载目标盘上所有 mounted volumes，Clean 后必须通过 `QueryUnallocatedDisks` 或保留句柄链重新绑定未分配磁盘；初始化前复核保留句柄仍指向同容量 RAW 磁盘，禁止只经 pack 或按可能复用的盘号盲目重新打开。
- 正常端自动创建的安装暂存分区只有在交接事务把 marker 原子升级为当前 `SessionId`、规范目标布局摘要、目标完整 extent 和暂存完整 extent 的授权记录后，PE 才可删除；旧版、缺字段、重复字段、超限、非普通文件或任一实时身份不一致的 marker 只能记录 warning 并保留分区。删除成功后不得再按旧数据盘符清理路径，因为 Mount Manager 可立即把该盘符分配给无关卷；此时只允许清理仍经当前目标身份绑定的 marker。
- 写操作应 fail-closed；查询或探测失败可返回 `Unknown`、跳过或使用既有安全回退，但不得伪装成成功。
- 临时盘符只能通过共享 `GetLogicalDrives` WinAPI 边界选择；API 返回零表示查询失败，必须按“无可用盘符”失败关闭，禁止用 `Path::exists` 把空光驱、断开的网络映射或不可访问卷误判为空闲盘符。对没有 `IVdsVolume` 对象的隐藏 OEM/ESP 分区，必须使用 `IVdsAdvancedDisk` 按精确磁盘号和起始偏移分配、查询并对称删除盘符；挂载后必须再次用卷范围 IOCTL 核对同一物理身份，已有盘符只借用不删除，本次创建的盘符在所有成功、错误和取消路径都必须显式清理，清理失败必须记录并向用户失败关闭。
- BCD、BCDBoot、Bootsect 和 XP/2003 引导写入属于承重步骤：进程启动、非零退出、关键文件缺失、活动分区写入或结果文件回读任一步失败都必须停止，禁止只记录警告后报告引导修复完成。正常端创建 PE 启动项时每条 `bcdedit` 都必须检查退出状态并保留 stdout/stderr；缺失 `boot.sdi` 时不得猜测或伪造文件格式，只能使用已存在的可信系统/介质原件，否则失败关闭。
- 临时脚本必须使用碰撞安全的临时文件并保证清理，禁止固定临时文件名。
- 新危险路径必须先把命令构建和结果判断提取成可测试逻辑，再接入真实执行器。

### 简单、可靠、低误报优先

- **禁止为了“看起来更安全”叠加未经真实环境证明的复杂前置校验。** 对 Windows 自带 DISM、SetupAPI、VDS 等受支持操作，真正的系统 API/工具执行结果与操作后回读才是成功或失败的权威依据；自制启发式、重复验签链、环境服务探测和 advisory preflight 不得把受支持操作提前误判失败、降低安装成功率或把可选组件失败扩大成整次安装失败。只有目标身份、路径穿越、重解析点、输入缺失、哈希/manifest 不一致等确定性安全错误可以在执行前阻断。新增前置门禁必须有明确威胁、官方依据、低误报测试和不可由执行后回读替代的理由；否则保持简单并删除该门禁。
- 普通保存驱动的恢复固定采用“结构安全检查 → 微软 DISM 目录导入 → 批量失败时逐个精确 INF 隔离 → 必要时按目标 DISM 库存确认真实启动路径覆盖”的单一流程。禁止在 DISM 前对每个普通 OEM INF 再造 `SetupVerifyInfFileW`/CAT/`WinVerifyTrust` 组合验签器，禁止因 WinPE 某项验证服务未启动而把整棵驱动树判为不可信，禁止 `/ForceUnsigned` 自动重试。只有由源系统卷全部 `VOLUME_DISK_EXTENTS` 对应磁盘 devnode 的当前 ConfigMgr 父链证明、且设备类属于存储控制器的要求才可承重；HDC/SCSIAdapter/VMD 分类本身不得替代拓扑证明。全部精确 INF 导入成功时立即接受，不得再叠加库存门禁；只有隔离后仍有包失败时，才优先通过 DISM API 的 `DismGetDrivers(TRUE)` 和逐发布名 `DismGetDriverInfo` 读取目标镜像适用候选；精简 WinPE 缺少 `DismApi.dll` 或 API 会话真实失败时，必须回退同一宿主自带的 `dism.exe /English /Get-Drivers /All` 和逐发布名 `/Get-DriverInfo`，禁止复制或混用其它版本的 DISM DLL，也禁止向一次详情命令拼接多个 `/Driver:` 后再依赖错误 87 隔离。命令行详情使用微软文档化的不变英文报告字段并拒绝缺失或未请求的发布名；微软只保证新增第三方包的 `Oem*.inf` 发布名唯一，真实维护后的 Windows 10 `/All` 库存可重复列出 inbox 发布名，因此同名 inbox 只能合并后按该发布名查询一次，涉及任一 out-of-box 包的重复仍须拒绝。源设备任一完整 Hardware ID 或 Compatible ID 与候选 `DismDriver.HardwareId`、`DismDriver.CompatibleIds` 或对应命令报告字段完整、不区分大小写等值匹配即证明覆盖；严禁退化成 PCI VEN/DEV、前缀、后缀、SUBSYS/REV 截断或其它部分匹配。目标 inbox、相同 OEM 包或其它适用版本都必须接受；`BootCritical`、签名状态和源/目标 INF 名只能用于诊断。旧版或缺少设备实例拓扑证据的清单不得制造致命缺失；目标包详情查询不完整时，只能把尚未匹配的真实启动路径要求判为未知。打印机、虚拟机、网卡、Wi-Fi 等非启动存储可选包的真实 DISM 失败只做有界汇总并跳过；目录结构错误、零 INF（用户明确要求恢复时）和 DISM 无法启动仍须按实际影响失败。不可逆边界后的真实启动路径缺失或未知不得在 BCDBoot 之前提前返回，必须先完成镜像、引导和最小系统配置，再保留认证暂存与诊断、抑制自动重启并进入失败终态。随包固定 VMD/Windows 7 资源继续按发布锁文件、固定哈希和确定签名者验证，不得把该确定性供应链校验泛化到用户导出的普通驱动。
- 自动数据分区容量必须采用一份贯穿选择、创建和阶段回读的确定预算：精确累计实际会写入数据分区的镜像全部 span 或 XP 树、在线 OEM 驱动包、PCA、版本化用户驱动和 UefiSeven 文件逻辑大小，最后只加一次固定 2 GiB 操作余量。禁止只按镜像大小、百分比、固定 8–12 GiB、磁盘总容量比例或另一层“安全保留”猜测；禁止为了统计驱动大小先复制到临时目录。Windows 7–11 正常端在 DISM 前可枚举 `%Windows%\INF\oemN.inf`，用 Vista+ 的 `SetupGetInfDriverStoreLocationW` 直接定位现有 Driver Store 包目录，去重后只读文件元数据形成选盘预估；真实 Windows 11 已证明 `/Online /Export-Driver` 的物化树可能合法大于该 Driver Store 视图，因此不得把差值本身当成损坏。所需导出完成后必须用普通非重解析树的实际逻辑字节原子替换同一预算中的驱动项，并立即用 `GetDiskFreeSpaceExW.lpFreeBytesAvailableToCaller` 复核当前卷仍能容纳所有尚未物化的预算项和原封不动的固定 2 GiB 操作余量；权威可用空间查询失败时必须停止并保留底层 Win32 错误，查询成功且空间足够时继续，只有查询成功但实际剩余空间小于剩余确定预算时才以容量不足在目标系统写入前停止。禁用当前任务的驱动导出/恢复时，即使产品数据目录残留旧 `drivers` 树也不得把它纳入本次 authenticated manifest；普通驱动、用户驱动和 XP 树中的零字节普通文件必须以 `SHA-256(empty)` 正常锁定、认证和复制，镜像、secret、marker、安装包等承重对象的非空要求由各自语义边界单独执行。
- 自动暂存不得把不同基本分区末尾或不同磁盘上的不连续空闲 extent 相加后伪装成一个可创建的普通分区。单个基本分区的容量上限只能来自包含本次创建范围的单一当前 provider free extent；禁止为暂存自动转换动态磁盘、创建 simple/spanned/striped 动态卷、Storage Spaces 或依赖 WinPE/Windows Setup 不稳定支持的跨区卷。若未来支持多卷暂存，必须使用多个独立随机 locator、逐 artifact 根绑定和逐事务回收，且任何单文件必须完整落在一个已经认证且容量足够的卷中。
- ViaPE 安装目标的跨重启重绑定必须保持简单：正常端在用户选定卷根写入固定文件名、内容为本次 CNG 随机且已经由 LRHC1/HMAC 配置认证的 `SessionId`；PE 只统计内容与本次值逐字节完全相同的卷。其它卷上同名但内容不同、格式损坏、不可读或为重解析点的文件全部静默忽略，不得记录为安装错误；零个匹配才报告找不到目标，两个及以上不同卷精确匹配才因歧义停止。禁止再用正常端缓存的磁盘号、分区号、整盘布局摘要、容量、卷标或可用空间跨重启筛选安装目标；唯一匹配后只允许在首次写盘边界对刚解析出的当前卷做一次同对象确认，防止盘符瞬间换绑，不得在后续每个安装阶段重复做可变库存校验。

### 磁盘目标与恢复要求

- 破坏性操作必须携带最小且与该操作直接相关的授权：ViaPE 安装使用随机 marker 唯一匹配结果并在紧邻首次写入处确认当前 extent；Direct、备份 CAS、扩缩容和自动暂存清理各自使用其明确事务授权。不得把磁盘总容量、可用空间、卷标或完整库存缓存成通用门禁。
- 不得仅根据 UI 中缓存的盘符执行不可逆操作。
- 多步骤流程必须保留原有回退和清理语义；新增步骤应考虑重复执行、取消、中断和进程崩溃。
- 安装失败的自动回退只允许发生在原系统仍完整的可逆阶段：目标系统分区尚未开始删除或格式化，且镜像引擎尚未收到目标写入请求时，必须自动清理本次受认证 config/marker、PE 启动项、私有 payload 和能够以 canonical 身份安全证明的自动暂存分区。调用删除或格式化 API 前、以及未格式化安装把目标交给镜像引擎前，必须先进入不可逆状态；从这一刻起即使 API 返回失败也要按“原系统可能已部分破坏”处理，严禁恢复旧系统文件、旧引导、旧分区或声称已回退，只能保留诊断/失败材料并明确提示重新安装或人工恢复。不得用文件快照、路径猜测或 best-effort BCD 拼接伪造系统级回退。
- 不能确认安全状态时，应停止并向用户显示简洁错误，同时在日志中保留足够诊断信息。

### 跨重启任务真实性与机密性

- 安装、备份和无损扩容都是跨重启破坏性任务，必须各自使用 `lr-core::handoff_auth` 的独立 operation domain 和 LRPE4 受保护会话记录。正常端必须为每次会话生成唯一 HMAC key，把严格 LRHC1 capsule、authoritative exact config 和 LRHM3 manifest 注入本次私有 boot.wim 的固定路径，再把该 WIM 作为普通非重解析文件发布到已经通过 owner、受保护 DACL 和锁定根复核的 `SecurePeDirectory`；LRPE4 capsule、私有 WIM 和同级承重 artifact 必须经 `lr-core::scoped_temp_file` 的 `CREATE_NEW` 安全描述符边界创建。磁盘上的 journal 只负责 BCD/payload 事务与清理，不得作为 WinPE 获取 key 的 trust anchor。key 不得出现在数据卷 marker、磁盘 journal、日志、错误、Debug、命令行、环境变量或新系统合并日志中。WinPE 必须先从当前实际加载到 `X:` 的私有 boot.wim 固定位置有界读取并严格解析 LRHC1；公开卷不再保存或提供 authoritative INI/LRHA1，operation、配置和 manifest 只能来自 X:。每次任务在 manifest 中携带独立 256-bit `DataLocatorToken`，安装另携带不同的 256-bit `InstallTargetToken`；磁盘根目录只保存对应 token 的固定 marker。任一缺 capsule/key、MAC 或 manifest binding 不一致、locator 零匹配或多卷精确匹配都必须在首次写盘前失败关闭；同名异内容、损坏、不可读和重解析 marker 必须静默忽略。
- HMAC 只提供真实性和完整性，不提供加密。公开数据卷配置严禁包含账户密码、恢复密钥、token、HMAC key、自定义无人值守敏感正文或其它 secret；确需跨重启的 secret 必须作为 LRPE4 受保护 secret artifact 单独保存、最小化读取并及时清零，否则对应选项必须失败关闭。公开配置可以携带受保护 artifact 的非敏感标识与摘要，但不得把 tag 描述为“加密”。
- HMAC 认证配置文本并不自动认证配置引用的外部文件。安装镜像的全部 span、XP 目录树、自定义无人值守/XP answer、保留驱动与用户驱动树、PCA 包、UefiSeven、允许的更新包以及自动创建分区删除 marker，必须由已认证配置中的严格 manifest 绑定每个安全相对路径、普通非重解析类型、长度、SHA-256、确定性顺序和角色；WinPE 必须重新枚举并拒绝缺失/额外项，从拒绝写入和删除共享的句柄或受保护私有快照消费到最后一次使用。公开 data 卷和安装目标卷均只按各自已认证随机 locator 唯一匹配，不得用磁盘号、GUID、容量、卷标或整盘布局参与跨重启定位；自动暂存删除仍是独立拓扑事务，继续使用其 source/temporary canonical pair。
- 既有 WIM/ESD 的 replace/append 只能通过 `lr-core::backup_atomic_publish::ExistingBackupPreparation` 的 move-only 边界：从一个请求 `DELETE`、拒绝 write sharing 但允许 WinPE rename 所需 delete sharing 的旧目标普通文件句柄观察长度、SHA-256 与 file ID，并从同一句柄复制到同卷 SYSTEM/Administrators 私有会话固定 staged 槽；Append 必须在保持该旧句柄到 CAS 完成的同时只修改同一个 staged file ID，Replace 必须按精确句柄删除初始副本后构造完整新 staged，禁止按路径追加或原地修改旧目标。完整镜像语义验证后，PREPARED journal 必须先以受限大小、规范 schema、SHA-256 checksum 和 `FlushFileBuffers` 持久化，再以 Windows Vista/7 已支持的 `SetFileInformationByHandle(FileRenameInfo)`、`ReplaceIfExists=false` 依次执行 old→previous 与 staged→target。若且仅若该句柄重命名在已复现的 WinPE 文件系统路径返回 `ERROR_SHARING_VIOLATION`，允许回退到同样受 Windows 7 支持的同卷 `MoveFileExW(MOVEFILE_WRITE_THROUGH)`，但禁止 `MOVEFILE_REPLACE_EXISTING` 和跨卷复制；调用前必须重开 source 路径并与 retained file ID/对象类型精确绑定、证明目标父目录同卷且目标不存在，调用成功后必须立即证明 source 消失且 target 精确绑定同一 retained file ID。系统已报告移动但任一回读不确定时必须按“可能已变更”进入精确回滚或保留 PREPARED，不能按失败前状态猜测。允许 delete sharing 不授权路径对象：每次 mutation boundary 仍须按 retained file ID/hash、路径回读和 no-replace 结果收束，外部换名、删除挂起或目标抢占必须失败关闭并按 journal 恢复；任何目标名抢占都必须保留 previous+journal。正常端与 PE 生产执行器必须复用该边界；正常系统端公共 CLI 可发起 direct WIM/ESD create/replace/append，PE 端不得提供公共 CLI。

  真实 WinPE 已进一步证明，`FileRenameInfo` 与 `MoveFileExW` 都可能以 `ERROR_SHARING_VIOLATION` 阻断，即使前者允许 delete sharing、后者已在 PREPARED 后释放本程序源句柄。兼容回退不得把长期句柄永久改成允许写入；只能在 PREPARED 已持久化、source file ID/类型、目标父目录同卷和目标不存在均完成回读后瞬时释放。两个 rename API 都精确返回共享冲突时，仅对普通文件允许尝试 Windows XP+ 的同卷 NTFS `CreateHardLinkW`，不得用于目录、ReFS/不支持文件系统或目标已存在场景；成功后必须证明 public/private 两个名字绑定同一原 file ID，按精确句柄删除 private 名，重新取得 public 拒写句柄，并由调用者再次全量匹配原长度/SHA-256。崩溃导致两个名字同时存在时，恢复只能在两者 file ID 完全相同且字节匹配 journal 时删除 public 名并回滚；不同 ID 或任一回读不确定必须保留 PREPARED。任一 API 失败后只有重新锁定 source 且 file ID 未变才可按“未移动”处理；无法重锁或路径/ID/字节不确定都视为可能变更。这个受控瞬时释放只兼容真实系统共享差异，不授权按路径猜测对象。

- 备份 data locator 必须位于备份目标卷，严禁写到捕获源卷根目录；备份 exact config 只存在于私有 boot WIM。正常端 Direct 与 PE 的 WIM/ESD create/replace/append 必须继续复用同卷 CAS、durable committed receipt 和句柄绑定恢复边界。

### 当前 PE typed-task 与受检存储端点

- `正常系统端/src/core/install_config.rs` 只把 authoritative config/manifest 交给私有 boot WIM；不得再发布 `LetRecovery_Install.ini`、`LetRecovery_Backup.ini`、`LetRecovery_Expand.ini` 或 LRHA1 公共副本。内置 Administrator 启用时，INI 中的密码字段必须保持空值，真实密码只可序列化为 manifest 精确绑定的单个 `ProtectedAdministratorSecret`，由同一私有 boot WIM 注入。data 卷根写 `LetRecovery_Data.marker=DataLocatorToken`；安装目标卷根写 `LetRecovery_Target.marker=InstallTargetToken`，两个 token 必须独立生成。普通重装与双系统在首次写盘前通过锁定对象精确删除目标 marker；全盘模式只关闭已验证 marker 句柄，由紧接着的 checked clean/delete 删除旧分区，不得把逐文件删除变成新门禁。
- `PE端/src/main.rs` 不提供破坏性命令行执行；严格单参数的 `/PEINSTALL`、`--pe-install`、`/PEBACKUP` 与 `--pe-backup` 必须大小写不敏感地在日志、驱动探测、LRHC1 discovery、语言/UI 初始化、MessageBox、guard 转移或会话清理之前确定性无副作用退出。安装、备份和扩容只有 `/AUTO` 与默认原生进度界面可消费 move-only authenticated task；独立 `maintenance` purpose 只在认证后 best-effort 用恢复密码解锁当前锁定卷，绝不解密或改变保护器，并隐藏 LetRecovery_PE 任务窗口让用户使用 PE 桌面。已停用的旧 `run_cli_mode` 不得恢复为生产职责。
- `PE端/src/core/config.rs` 负责把当前 X: 的 LRHC1/config/LRHM3 一次性转换成 typed task；先按 `DataLocatorToken` 唯一定位 data 卷，再从该卷锁定 manifest artifacts，安装另按独立 `InstallTargetToken` 唯一定位目标。安装只允许从固定 `X:\LR_AdministratorSecret.txt` 的拒写/拒删句柄读取 manifest 精确绑定的单个 `ProtectedAdministratorSecret`，并且只有公开 INI 密码为空、启用状态与 secret 数量严格一致时才可把密码注入内存中的 typed config；`maintenance` 不建立磁盘 typed task，只允许从固定 `X:\LR_BitLockerKeys.txt` 拒写/拒删句柄读取 manifest 精确绑定的单个 `ProtectedBitLockerSecret`。每个候选只允许以最终持有的普通非重解析锁定句柄打开和比较一次；同名异内容、损坏、不可读或重解析项必须静默忽略。不得读取公共 INI、LRHA1 或根据其存在选择 operation。manifest 文件必须从拒写/拒删的同一持有句柄完成一次内容哈希认证并报告有界进度；后续阶段只复核祖先、路径绑定和同一句柄长度，禁止在首次 UI 状态之前反复整文件哈希。严禁把保存驱动、用户驱动或 XP 树复制进容量有限的 X: RAM 盘；消费者只能使用 typed task 持有的 exact artifact 集，不得按公共目录发现 manifest 外输入。
- `PE端/src/app.rs` 的安装 worker 只能使用 typed task；目标由 typed task 的唯一随机 marker 匹配结果提供，首次写盘前仅复核该盘符仍指向刚匹配的同一卷，不得在跨重启或后续每阶段用缓存磁盘号/整盘布局重复筛选。格式化时以当前 VDS 对象 exact extent 与操作后文件系统/卷标回读为权威，不得把易变 GPT/MBR token 或可选 StorageDeviceId 的临时缺失升级成格式化后的失败；不格式化时只在把目标交给镜像引擎前确认一次。LRPE4 persistent payload 和目标 marker 清理只能发生在全部输入和 preflight 成功后、首次目标写入前。自动暂存回收仍必须把 manifest `auto_staging` 的 source/temporary canonical pair 转移给 checked storage transaction，失败前保留重试材料，禁止用未认证 marker 猜测待删分区。镜像和启动存储属于承重结果；普通驱动附带 CAB、用户驱动及非关键高级选项失败只能有界 warning 后继续，全部高级项关闭时不得加载 hive，安装完成写日志也不得重新盘点目标身份并倒打一票。
- 安装已释放镜像并完成引导后，会话 marker、诊断文件或自动暂存分区清理失败只允许返回完成带 warning 并禁止自动重启，不得把可启动的新系统改判为安装失败。这一规则取代旧职责文字中“临时分区回收失败必须进入失败终态”的过时要求。
- `PE端/src/core/disk.rs` 的自动暂存回收必须重读并同时匹配 source/temporary 的完整 canonical layout，证明 exact adjacency 后调用 checked delete/extend，并回读最终 extent；盘符只能是与授权 extent 精确相等的临时访问别名。
- `PE端/src/core/expand_move.rs` 当前生产 LRPE4 路径只开放 authenticated canonical target 后方既有连续未分配空间的 checked VDS 扩展。右移和左移供体在 canonical target、供体 layout、同一 PhysicalDrive 句柄与可恢复 consume journal 尚未贯穿 shrink/raw-write/delete/create 全阶段前必须明确失败关闭，不得退回裸盘符授权。
- `lr-core/src/install_source_lock.rs` 除镜像/XP源守卫外，还提供 manifest 单文件的普通非重解析锁定、带字节进度的首次完整 SHA-256、拒写/拒删句柄存续期内不重算内容哈希的轻量路径/长度绑定复核、整套 SWM/GHO/GHS 按连续 ordinal 从原持有句柄复制到受保护 writer，以及 engine-visible 目录的 exact missing/extra inventory 检查；受保护多分卷会话目录的每一个即时子项都必须是 manifest 中的普通非重解析 span，不能只检查规范命名的 sibling 而忽略会被外部引擎发现的非规范或无关文件。调用方不得在 guard 存续期按未认证目录重新发现额外输入。

## 兼容性与配置边界

- 正常系统端发布物只允许生成一份覆盖 Windows 7、8、8.1、10、11 的 x64 EXE：Release 使用 Rust `x86_64-win7-windows-msvc` 最低装载 ABI、`.cargo/config.toml` 中仅对该目标生效的静态 CRT 和受控 `build-std` 构建，避免未安装 UCRT 平台更新的 Windows 7 出现 `api-ms-win-crt-*` 装载依赖；不得按系统拆分版本或把现代能力全局降级。共享兼容层必须在 Windows 10/11 上优先动态调用现代 WinAPI，仅当导出确实不存在时使用有文档依据的 Windows 7 回退。PE 端只运行在 Windows 10/11 系 WinPE，必须继续使用普通 `x86_64-pc-windows-msvc` 目标，禁止套用 Win7 目标、静态 CRT 配置或正常端的装载期 ABI 桥接。
- 正常端主程序及随包自有运行库的静态和延迟导入必须全部存在于未安装平台更新的 Windows 7 SP1；发布检查发现 `combase.dll`、Windows 8+ API-set 或其它 Win7 不存在的装载期模块时必须阻断打包。`CoTaskMemFree` 等在 Windows 7 已由传统系统 DLL 导出的兼容入口必须绕过可能被新版 SDK 重定向的 import library，运行时从其最低系统导出（当前为 `ole32.dll!CoTaskMemFree`）解析，不得接受新版 SDK 静默把同一符号改绑到较新的模块。运行时动态解析的现代 API 继续保留 Windows 10/11 优先路径和 Windows 7 有文档回退，禁止为消除静态依赖而全局关闭现代能力。
- 当前服务端入口是代码内固定 HTTPS 地址，普通用户不可在 UI 中修改；不得引入私密凭据或隐藏后门配置。
- 正常端读取固定 `v3/index.json` 时优先使用系统 TLS 与代理；仅当该固定 HTTPS 目录请求失败时，允许使用内置 WebPKI 根和禁用代理的 Rustls 客户端重试。重试不得放宽证书、主机、响应状态、JSON 架构或目录内容校验，也不得影响用户输入 URL 和其它下载入口；双路径错误必须完整写入日志。
- LetRecovery 发布回调只允许原子更新服务端 `v3/index.json` 的 `data.pe`、生成时间和 PE 计数；不得再写入 LetRecovery v1/v2 目录文件。正常端在线目录读取也必须以 `v3/index.json` 为唯一入口，v3 请求、解析或结构校验失败时直接失败并保留上下文，不得回退 v1/v2 或把部分目录伪装为加载成功。回调必须把 HTTPS URL、文件名、MD5、SHA-256 和字节数全部纳入签名绑定。`data.system_image_mode`（兼容根级字段及 `mode` 别名）只允许 1、2、3：1 表示每次程序启动或人工刷新时从微软 Update Metadata Service 获取 MCT `products.cab` 并使用官方长期 ESD，2 表示只使用 `data.system_images`，3 表示优先合并两者；字段缺失时固定默认为 2。模式 3 的微软目录临时失败可在 API 目录非空时安全降级到 API，模式 1 和两边都为空时必须失败关闭。
- 微软官方系统镜像目录不得通过网页 ISO connector 生成 24 小时签名链接。Windows 11 必须使用 HTTPS 请求 `fe3.delivery.mp.microsoft.com/UpdateMetadataService`，严格校验唯一更新身份、唯一 `products.cab`、声明大小、官方 delivery 主机和服务端 SHA-256；CAB 只能通过共享 `SetupIterateCabinetW` 边界解出唯一 `products.xml`。Windows 10 可附加使用微软固定 HTTPS fwlink；其当前 22H2 旧目录只声明 SHA-1 时必须验证该字段格式，但不得把 SHA-1 冒充 SHA-256、不得显示为已完成 SHA-256 校验，下载后仍须经过完整本地镜像校验。XML 只接受 `zh-cn`、x64、`CLIENTCONSUMER_RET`、无查询参数的 `dl.delivery.mp.microsoft.com` 长期 ESD，且文件名、大小、Build 和版本标识必须完整一致；Windows 11 还必须具有有效 SHA-256。重复卷条目只可在全部元数据相同时折叠，任何冲突、越界或非官方重定向都必须失败关闭。此目录只属于正常系统端的在线下载入口，PE 端和 `lr-core` 安装策略不复制该网络发现逻辑；下载后的既有本地镜像复核和交接安全边界保持不变。
- 简易模式目录允许只提供 WIM/ESD/ISO URL 而省略卷列表；系统安装页也允许用户手工输入这些格式的直链。直接 WIM/ESD 优先用两个有界的精确 HTTP Range 读取标准 WIM 头和 XML；ISO 优先按 ECMA-119 用有界 Range 读取 ISO 9660/Joliet 卷描述符和 `sources/install.esd|wim` 的单段或多段 extent，再把内嵌镜像视为连续逻辑字节流读取头和 XML，禁止在元数据探测请求中吞下完整 `200 OK` 响应体。Range 探测的 `206 Partial Content` 必须范围精确匹配；畸形 `206`、内容编码、范围/extent 越界、实体大小、最终重定向地址或 ETag/Last-Modified 在请求间变化仍须失败关闭。若服务器明确忽略 Range 并返回 `200 OK`，只把它分类为“需完整下载”，不得把整份响应读入探测器；正常下载完成后必须重新用本地镜像检查器完整读取或挂载文件，过滤 WindowsPE、Windows Setup、Setup Media、WinRE 和无法确认的条目，默认选择本地确认的第一个可安装分卷并返回安装页让用户确认后再次开始安装。最多跟随五次经过 URL/传输策略验证的重定向，不得改写输入框或持久化配置中的原始链接。Range 成功时只显示 XML 中明确为 Client/Server、具有版本元数据的可安装卷，并按实际版本 6.1/6.2/6.3/10.0 与产品名区分 Windows 7/8/8.1/10/11。简易模式启用时隐藏在线下载和工具箱入口。在线目录已选定具体卷时，下载后必须比对该卷索引、版本、Build、架构和安装类型；只有 Range 不可用、下载前无法取得卷表时才允许使用“本地第一个可安装分卷”规则。
- PE 元数据必须继续兼容现有 MD5 字段；可选 SHA-256 存在时优先使用 SHA-256。
- 联网下载和受管 PE 缓存声明校验值后，计算失败或不匹配必须失败关闭。“未声明校验值”和“计算出错”必须是不同状态。服务端提供 MD5 或 SHA-256 时必须随目录项一起传递到 UI、下载器和缓存校验边界；服务端未声明哈希时 UI 不得显示“已校验/已验证”。随发布包提供的 `bin/pe` 是明确的用户管理边界，允许用户替换或修改 WIM，因此不强制匹配远端哈希，但仍必须限制为安全文件名和普通文件；联网文件不得下载到该目录来绕过校验。
- 正常端只允许为已经通过完整 libwim 校验的单文件 WIM/ESD 保存有界的本机 BLAKE3 验证缓存；再次校验必须在拒绝写入和删除共享的文件句柄仍被持有时重新计算完整 256 位指纹，只有指纹完全相同才能复用先前结果。不得仅依赖路径、大小或时间戳；缓存缺失、损坏、超限、指纹不匹配、读取或持久化失败都必须回退完整 libwim 校验，SWM 分卷不得使用该快路径。
- 自动目录等非手工下载默认只允许 HTTPS。用户在系统安装页手工输入 `http://` 镜像直链视为仅对该链接的明确兼容授权；不得据此放宽其他下载来源，也不得把原始链接静默改写成重定向后的地址。
- Windows 兼容性入口点故障发生在主程序启动和日志初始化之前，不得声称能由主程序自身记录；发布流水线必须通过 `.github/scripts/verify-win7-imports.ps1` 检查正常端产物，阻断 Windows 8+ API、API-set/UCRT 或 `combase.dll` 装载依赖。发布包根目录只允许携带正常端主程序这一份 EXE，文档和安装器不得要求已停用的临时开发诊断工具。
- 正常端的本地发布构建与 Release 流水线必须统一调用 `.github/scripts/build-win7-normal.ps1`：固定 Rust 1.88.0、`x86_64-win7-windows-msvc`、`rust-src`、`-Z build-std=std,panic_abort` 与静态 CRT，并在返回成功前执行 `verify-win7-imports.ps1`。README 不得再用 `cargo build --workspace --release` 冒充 Win7 发布构建；PE 端必须继续以普通 `x86_64-pc-windows-msvc` 单独构建。可丢弃安装矩阵需要正常端夹具时必须显式传 `-CiAutomation`，生成包含 feature、目标、长度、SHA-256 和 Win7 导入结果的原子回执，并由宿主在启动 VM 前与实际 EXE 回读一致；普通发布调用不得启用该 feature。
- 正常系统端和 PE 端已有配置格式需要向后兼容；新字段应有安全默认值，并为旧配置增加解析测试。
- 历史配置字段 `disable_reserved_storage` 只能使用微软公开支持的在线 DISM Reserved Storage 接口：`/Set-ReservedStorageState /State:Disabled` 与 `/Get-ReservedStorageState` 从 Windows 10 2004（build 19041）起可用且只支持 online image。两端严禁再写离线 `ReserveManager` 的 `ShippedWithReserves`、`PassedPolicy` 或其它内部注册表值；只有已通过目标 `ntdll.dll` 版本确认属于 Windows 10/11 build 19041+、使用 LetRecovery 内置 unattend 且固定脚本已原子暂存并回读一致时，才可在 specialize/SYSTEM 阶段执行固定在线命令，并通过 typed DISM PowerShell 对象核对最终状态，禁止解析本地化命令输出。两个固定 DISM 子进程必须显示英文阶段并共享 120 秒总期限，只按各自返回的 `Process` 对象有限等待，禁止用 `Start-Process -Wait` 无限等待后代树；超时只能收束本次直接进程。自定义 unattend、禁用无人值守、版本未知/过旧、脚本暂存、DISM 执行、超时或回读失败必须写结构化 warning、退出 0 并继续安装，不得弹框、伪装成功或阻断 Windows Setup。
- Win32 OneDrive 只允许在已确认 Windows 10/11 的内置 unattend specialize/SYSTEM 阶段调用微软公开支持的 `OneDriveSetup.exe /uninstall`。卸载器候选必须固定为目标 `%SystemRoot%\System32\OneDriveSetup.exe` 和 `%SystemRoot%\SysWOW64\OneDriveSetup.exe`，x64 按微软部署文档优先检查 SysWOW64，禁止 PATH、递归或用户目录搜索；执行前必须核对普通文件、非重解析点、有效 Authenticode Microsoft signer、固定 Microsoft 根证书 DER SHA-256 和签名前后文件 SHA-256，任一证据不足即 warning skip。只允许唯一 `/uninstall` 参数，首个可信候选必须在启动前重新完成同一套签名和哈希验证；一旦启动过一个候选，禁止再启动第二个候选，因为 `Start-Process -Wait` 会等待整个后代进程树，而在前一进程树仍活动时重试会造成并发卸载和 specialize 卡死。脚本必须显示固定英文阶段，在 120 秒总期限内只用返回的 `Process` 对象执行有限 `WaitForExit(Int32)` 和固定库存轮询；超时只能终止本次直接启动且仍活动的受检进程、记录 warning 并让 Windows Setup 继续，禁止按进程名枚举或终止无关进程。只有退出码为 0 且 fresh 固定库存为 0 才能报告完成，所选路径、哈希、退出码、超时状态、回读计数和结果都必须写入结构化 JSON。执行后必须 fresh 回读固定 HKLM/HKCU `OneDriveSetup.exe` 卸载项和固定 Program Files/当前 SYSTEM profile `OneDrive.exe` 库存。回读只证明 specialize 当下的机器和当前 SYSTEM profile 固定库存，不得声称覆盖未来用户注册、功能更新或 Office 重新部署。禁止删除 OneDrive 目录、用户数据、注册表键值或使用 MSI/Win32_Product 广泛查询；候选缺失、签名、启动、超时、退出码或回读失败必须写结构化 warning、外层归零并继续安装，自定义 unattend 不得注入。结构化 JSON 的日志路径初始化或持久化失败时，还必须向 Windows Setup 可收集的标准错误输出固定结构 warning，再保持退出 0；禁止空吞日志故障。
- 精确离线 AppX 移除必须优先使用微软自 Windows 8 起提供的 `PackageFamilyNameFromFullName` 与 `PackageIdFromFullName`；正常端运行在 Windows 7 且导出不存在时，只允许按微软 package full name 的五段结构进行严格、无歧义解析，校验名称、四段 `u16` 版本、微软列出的架构（含 `x86a64`）、资源 ID 和 13 字符 Crockford Base32 publisher ID 后再与固定 family allowlist 比较，任何非规范字段都必须 warning skip。`Remove-ProvisionedAppxPackage` 的成功与回读只证明 provisioning 已删除，不覆盖已有用户注册，也不阻止 Store、Office 或功能更新将来重新部署；两端日志必须以结构化 scope warning 明确该边界，GHO/已有用户镜像不得宣称完整卸载。
- SecHealthUI 和 Reserved Storage 的在线 warning-only 脚本只能在已确认目标版本且即将生成 LetRecovery 内置 unattend 时原子暂存并回读；高级项离线阶段、自定义 unattend 和禁用无人值守不得留下在线脚本。脚本必须在 Setup 控制台显示固定英文开始与结束状态，禁止长时间黑窗无反馈；内部结构化 JSON 持久化失败或外层 PowerShell 启动/脚本执行失败时，必须向 Windows Setup 可收集的标准错误输出固定结构 warning，外层仍退出 0 并继续安装，禁止空吞故障或伪装成功。
- 内置无人值守的 `Microsoft-Windows-Deployment/RunSynchronousCommand` 必须统一经 `lr-core::unattend_command` 渲染：`Order` 只能为 1–500，解码后的 `Path` 必须非空且不超过微软规定的 259 个 UTF-16 单元，`Description` 同样不得超过 259。严禁把整段 PowerShell 通过超长 `EncodedCommand` 塞入 `Path`；固定脚本必须使用短启动器，RID-500 准备也必须在账户名最大边界下保持短命令并继续按 `LocalAccount` 与 SID 后缀 `-500` 精确定位。RID-500 的改名、启用和回读属于承重 specialize hook，必须显式使用 `WillReboot=OnRequest`，成功返回 0，任何失败统一返回大于 2 的代码，禁止保留默认 `Never` 让 Setup 忽略 helper 失败；warning-only 美化和可选维护脚本仍须退出 0，不得复用该失败契约。`FirstLogonCommands/CommandLine` 不得超过 1024 个 UTF-16 单元，存在脚本目录清理时必须与脚本调用合并为同一个命令，禁止依赖另一个 Order 项“最后执行”而引入清理竞态。
- ViaPE 必须把安装意图中的 `FormatPartition` 和 `RepairBoot` 原样写入交接配置，PE 的 `/AUTO` 与默认原生进度窗口必须严格遵守；PE 不提供用户 CLI，旧 `/PEINSTALL`、`/PEBACKUP` 固定拒绝。旧配置缺少字段时维持历史上的“格式化并添加引导”，显式关闭 `RepairBoot` 时还必须跳过 PCA/EFI 预检、BootEx/UefiSeven 注入和所有引导写入。
- 内置 Administrator 高级选项只允许在 Windows 7 及以上 WIM/ESD/SWM 的内置无人值守路径启用；不得与自定义无人值守文件、普通“自定义用户名”、GHO/GHS 或 XP/2003 路径并用。账户改名必须按 RID-500 身份定位，不能依赖本地化账户显示名；密码只允许存在于当前安装会话配置和 Windows Setup 无人值守文件中，持久化用户偏好、调试输出和日志必须脱敏，安装清理仍需删除会话文件。
- 历史配置字段 `disable_windows_defender` 的用户语义固定为“深度移除 Microsoft Defender Antivirus 杀毒引擎，并移除 Windows Security UI”。引擎实现只能处理 `WinDefend`、`WdBoot`、`WdFilter`、`WdNisDrv`、`WdNisSvc` 及同目录下随引擎演进的 `WdAiNisDrv`、`WdDevFlt`、`KslD`，以及 Defender 引擎目录、驱动目录和 Defender 自身计划任务；必须保留 `SecurityHealthService`、`wscsvc`、`mpssvc`、UAC、VBS、SmartScreen、System Guard、Web Threat Defense、Pluton 和 Microsoft Defender for Endpoint (`Sense`)。SecHealthUI 只允许通过 fresh DISM/AppX inventory 精确匹配 `Microsoft.SecHealthUI_8wekyb3d8bbwe` 或 `Microsoft.Windows.SecHealthUI_cw5n1h2txyewy`，先调用官方离线 `Remove-ProvisionedAppxPackage` 并回读；内置 Windows 10/11 unattend 在 specialize/SYSTEM 阶段可对上述唯一精确身份导入 `AppxAllUserStore\Deprovisioned\<PFN>` 与按 fresh typed inventory 得到的 `EndOfLife\<SID>\<PackageFullName>` 空键，再调用 `Set-NonRemovableAppsPolicy -NonRemovable 0`、online deprovision 和 all-user removal。`Deprovisioned` 键用途有微软公开文档；`EndOfLife` 属 Windows 内部状态且没有同等级公共契约，因此只能通过碰撞安全、普通非重解析点、大小受限并在导入后删除的 `.reg` 文件，对运行时已经与锁定 Name/PFN 精确匹配的完整包名和有效 SID 使用，最终成败仍只能由 fresh provisioning/all-user inventory 裁决。Windows Update 重新投递 Windows Security app 时，只允许通过 Windows Update Agent 对当前搜索结果中 KBArticleID 精确等于 `5007651` 且非 mandatory 的 update object 设置 `IsHidden` 并立即回读；搜索失败、部分成功、mandatory、设置失败、回读为 false 或当前没有该候选都必须分别记录，禁止宣称形成永久 PFN 黑名单。微软日后更换 UpdateID、尚未同步元数据或强制更新仍属于明确残余边界，不得为追求“永不恢复”改 WindowsApps/AppRepository ACL、启用 AppLocker 阻断系统组件、破坏 Windows Update 服务或反复制造部署错误。禁止通配、前缀猜测、解析本地化输出、删除 WindowsApps/SystemApps 目录或编辑其它 AppxAllUserStore 项。脚本暂存等写盘前确定性失败可以阻断用户明确要求的内置 hook；specialize 已进入后，任一 AppX 或更新抑制失败必须写结构化 warning、退出 0 并让首登进行一次有界重试，禁止用 `WillReboot=OnRequest` 加非 0/1/2 返回码把已释放的 Windows 变成安装失败，也不得把残留伪装成成功。两端必须复用 `lr-core::defender_removal` 和共享 SecHealthUI/AppX 边界；Defender 引擎移除仅允许对完整离线 Windows 目标执行，目标盘、控制集、任务 GUID、重解析点、ACL 修改和删除后状态都要严格验证，所选引擎操作任何一步失败时安装流程必须失败关闭，不能再把旧策略键写入或删除失败伪装成成功。
- 随包存储控制器驱动只允许来自已记录 SHA-256 和 Microsoft Windows Hardware Compatibility Publisher 签名的 Microsoft Update Catalog 包；全部发布文件、大小、SHA-256、控制器 ID 和签名主体由 `docs/STORAGE_CONTROLLER_DRIVERS.lock.json` 固定，Release 必须先验证底包、同步到 PE WIM，再只读挂载最终 WIM 复核，禁止依赖底包碰巧带有正确版本。正常端、PE 端和存储驱动工具必须先通过 SetupAPI 获取当前机器完整 `REG_MULTI_SZ` PCI 硬件 ID，再由 `lr-core::storage_driver_match` 选择唯一匹配目录，禁止对 `bin/drivers/storage_controller` 做整目录递归注入。PE 启动后必须在 BitLocker、标记和分区扫描之前校验锁定包并用微软支持的 `drvload.exe <匹配 INF>` 把同一包只加载到当前 WinPE 会话，使 VMD 后的磁盘可见；枚举、包校验或无法证明控制器可用时必须在磁盘扫描前停止。`drvload` 非零不等于控制器不可用：只允许在重新通过同一次 present SetupAPI 设备枚举取得精确代际硬件 ID，并由 Windows 2000+ `CM_Get_DevNode_Status` 证明每个匹配 devnode 都设置 `DN_STARTED`、未设置 `DN_HAS_PROBLEM` 且没有问题码时降级为有界 warning；零匹配、状态查询失败、未启动或有问题仍必须停止，禁止按退出码、乱码文本或机型名称猜测。当前 `9A0B` 使用 20.2.4.1019 兼容包，`467F/A77F/7D0B/AD0B` 使用 20.2.12.1036；`09AB` 只是 managed/dummy function，单独出现时不得猜测代际。正常端导出当前系统驱动时必须保存已绑定 OEM 启动存储驱动清单，PE 导入后必须回读离线 DriverStore 逐项确认覆盖；缺目录、缺清单、DISM 失败或回读不匹配全部失败关闭。AMD、Apple、VirtIO、仅凭机型名称或无法唯一确认时必须跳过或拒绝。旧通用存储控制器目录不得恢复；恢复的 Windows 7 USB3/NVMe 兼容资源必须由 `docs/WINDOWS7_DRIVERS.lock.json` 固定全部文件、大小和 SHA-256，发布前验证 USB3 WHQL CAT 与微软 NVMe CAB 签名，同步到正常端与 PE WIM，并只读挂载最终 WIM 再次复核。USB3 只能注入与 SetupAPI 当前硬件 ID 和离线目标架构匹配的锁定子目录；目标为 Windows 7 时 UI 默认启用 USB3。NVMe 只允许在同一个 DISM servicing 会话中按固定依赖顺序提交微软 x64 KB2990941/KB3087873 CAB；DISM 成功后还必须通过文件版本 WinAPI 回读离线目标中的 `stornvme.sys` 和 `storport.sys`，确认至少达到对应热修补 GDR 版本，禁止把进程退出码 0 单独当作导入完成。只有 `IOCTL_STORAGE_QUERY_PROPERTY` 明确返回目标物理盘 `BusTypeNvme` 且镜像为 x64 Windows 7 时默认启用；VMD/RAID、未知总线、查询失败和 x86 镜像不得猜测。旧包中不能通过内核签名策略的魔改驱动与散装 NVMe INF/SYS 不得恢复。XP/2003 专用驱动边界保持独立。
- 历史 `win7_fix_acpi_bsod` 仅作为 Windows 7 手工选择的旧式处理器电源驱动兼容尝试保留：它只禁用离线系统中的 `intelppm`、`amdppm` 和 `Processor` 服务，不修改 ACPI 表、`acpi.sys` 或固件，也不得宣传为通用 0xA5 修复。默认必须关闭，目标版本无法确认为 Windows 7 时必须忽略；正常端 Direct、ViaPE 配置和 PE 端必须保持同一语义。任何 ACPI 二进制替换或补丁属于独立高风险功能，未有可审计来源、签名和硬件矩阵前不得接入。
- Windows 7 USB3/NVMe 兼容处理属于自动安装策略，不得再在高级选项中暴露勾选框、自定义目录或浏览按钮。正常端必须在最终安装意图中重新按镜像版本、架构和同一目标磁盘快照的总线类型覆盖旧配置：已确认 Windows 7 自动评估锁定 USB3 包，只有 x64 且目标明确为原生 NVMe 时启用固定 CAB 组合；传给 PE 的自定义路径必须为空。历史 `Win7FixStorageBsod` 只为旧配置可解析且必须归零；`Win7UefiPatch` 继续作为正常端到 PE 的兼容交接字段，但只能由最终安装意图在已确认 Windows 7 x64、可能使用 UEFI 且启用引导修复时自动置位，UI 不得提供手工开关；PE 端不得把该兼容字段当作事实来源，必须在格式化前根据实际所选 WIM/ESD 卷的 6.1/x64 元数据重新判定。UefiSeven 固定资源由 `docs/UEFISEVEN.lock.json` 锁定，优先使用交接目录、否则使用随 PE 发布包提供的 `bin/uefiseven`，并在格式化前和写 ESP 前复验；缺失、篡改或原始 Microsoft 引导文件缺失都必须停止。重装时必须保留本次 BCDBoot 生成的 BCD；确认 VMware 客体时必须绕过 UefiSeven，并把 LetRecovery 既有锁定加载器从微软注册入口和标准固件 fallback 两处事务化恢复为各自保存的原生 Microsoft x64 EFI，不能只修一处或删除恢复副本；其它虚拟机、实体机和 Unknown 环境继续事务化部署 UefiSeven 双入口，任一入口验证、备份、替换或回读失败都必须整体回滚。Windows 7 离线 SYSTEM hive 中所有既存控制集的 `CrashControl\AutoReboot` 必须设为 0 并回读，避免把真实早期 bugcheck 隐藏成循环重启；该设置只改善诊断，不得伪装成 ACPI 或存储兼容修复。`Win7FixAcpiBsod` 仅按上一条的受限手工兼容语义保留。禁止把互不相关的 IDE/AHCI/RAID/NVMe 服务全部设为 Boot Start 来冒充通用蓝屏修复。
- PCA2011/PCA2023、BIOS/UEFI、MBR/GPT、BitLocker 和 XP/2003 路径都是兼容性边界，不得根据单一新系统环境简化掉旧路径。
- 正常端 Direct 与 PE 端必须复用同一套安装引导模式语义：显式 UEFI/Legacy 始终优先，Auto 仅可将已确认的 GPT 映射为 UEFI、已确认的 MBR 映射为 Legacy；分区表为 Unknown 时只能调用已核对的固件 WinAPI 探测，探测失败必须在写入引导前停止，严禁把 Unknown 默认为 Legacy。NT5/XP 路径只能来自已验证的安装意图或镜像元数据，不能通过目标系统缺少 `Windows\Boot` 等目录特征猜测；已确认为现代 Windows 却缺少必要引导资源时必须失败关闭。
- UEFI 安装在固件已撤销 PCA2011 或用户明确选择签名代际时，必须在分区、格式化等目标盘写操作前验证所选镜像卷内存在有效的对应 EFI 引导文件。无法安全预检的 GHO 等不透明格式必须失败关闭；不得把某台机器 ESP、Insider 构建或未经支持矩阵验证的 `bootmgfw.efi` 当作通用升级文件。
- PCA2023 自动升级使用发布包内固定的 x86/x64 离线资源族，不允许安装时联网下载或回退到其他架构。资源 WIM 只可注入 `Windows\Boot\EFI_EX`、`Windows\Boot\FONTS_EX` 和 `Windows\Boot\EFI\boot.stl`；必须验证包大小、普通文件属性、BootEx 微软签名、PE 架构和正常端到 PE 暂存副本的 SHA-256。缺包或验证失败应在写盘前停止。制作、支持矩阵和回滚流程见 `docs/PCA2023_COMPAT_PACKAGES.md`。
- PCA 选择只适用于 Windows 10/11 和 Server 2016+ 的 UEFI 安装；XP/2003、Vista、Windows 7、Windows 8/8.1、GHO/GHS 和 Legacy 安装不显示该选项。Windows 7 x64 UEFI 不得进入 PCA2011/PCA2023：经 CPUID、SMBIOS 和 SetupAPI 任一路正向确认的 VMware 客体使用 BCDBoot 生成的原生 Microsoft x64 EFI 双入口，并事务化清理 LetRecovery 既有 UefiSeven；其它环境继续把锁定 UefiSeven 事务化部署到 `EFI/Microsoft/Boot/bootmgfw.efi` 和固件 fallback `EFI/Boot/bootx64.efi` 两个入口，分别保留并验证 `bootmgfw.original.efi` 和 `bootx64.original.efi`。Unknown 不得猜测成 VMware；任一入口缺失或任一步失败都必须回滚并停止，禁止假定所有固件都会遵循 NVRAM 中的 Windows Boot Manager 项。若完成安装时检测到 Secure Boot 仍开启，PE `/AUTO`、默认原生进度界面以及正常端在 PE 内直接安装都必须明确提示用户进入 BIOS/UEFI 关闭 Secure Boot，并禁止自动重启。Vista、Windows 7 x86、Windows 8/8.1 遇到 Secure Boot 已启用且 PCA2011 已撤销时仍必须在写盘前停止，不能伪装成可升级 PCA2023；XP/2003 保持既有 NT5 专用路径。当前产品工具链只支持 x86/x64，ARM64 镜像必须在写盘前拒绝。
- PCA 写盘前预检不能把 Secure Boot 探测失败等同于“未启用”；只要安装可能使用 UEFI，状态为 Unknown 就必须在任何目标盘写入前停止并给出可诊断错误。

## 代码组织规则

- UI 负责展示和收集用户意图；可复用业务规则、解析和命令构建放入核心模块。
- Windows API、FFI 和动态 DLL 调用应封装在边界模块中，调用方不要复制 `unsafe` 细节。
- 超过约 1,000 行且职责混杂的文件应渐进拆分，但每次只移动清晰边界，保持公开接口和用户行为。
- 不为“看起来更抽象”增加层级。新抽象必须减少真实重复、隔离危险边界或提高可测试性。
- 错误应保留底层上下文；用户文案简洁，日志详细。不要静默吞掉写操作失败。
- 日志不得写入密码、恢复密钥、访问令牌、完整鉴权 URL 或其他敏感值。
- 公共模块和复杂安全判断需要短而准确的注释，禁止无意义逐行复述。

## 国际化与用户界面

- 正常端 Windows 7–11 的主窗口和全部原生工具对话框统一请求系统自带 `Microsoft YaHei`，不得使用 Windows 7 不存在的 `Microsoft YaHei UI` 字体族；工具箱必须按当前宿主系统版本隐藏不可用功能，Windows 7/8/8.1 不得显示 AppX 等 Windows 10/11 专用入口，隐藏后必须紧凑重排且不能留下空槽。
- 正常端日志默认开启，发布模板和规范化脚本必须共同保证 `log_enabled=true`；文件日志必须写 CRLF，使 Windows 7 记事本也能逐条换行。进程启动时必须设置 `SEM_FAILCRITICALERRORS | SEM_NOOPENFILEERRORBOX`，卷容量探测还必须在调用 `GetDiskFreeSpaceExW` 前用 `GetDriveTypeW` 显式跳过空光驱，禁止因已弹出或未插入介质阻塞 UI。

- **严禁装眼瞎，必须严格按照用户的要求来，严禁欺骗用户，严禁戏耍用户，必须保质保量。** 对用户指出的可见问题必须先复现、测量并如实说明验证结果；没有通过截图逐像素检查和真实交互复核时，不得用“已修复”“已完成”或模糊措辞敷衍交付。
- **严禁装眼瞎**：用户提供的截图、程序自行生成的 QA 截图或实机界面中只要仍存在肉眼可见的断线、矩形残影、色差、黑块、白边、颗粒、错位或样式回退，就不得声称“已修复”“验收通过”，更不得继续构建或发布 Release。必须先明确指出仍可见的问题，按原始像素或放大裁图复核，并在交互状态和明暗主题下重新验证；不能用“整体看起来正常”掩盖局部缺陷。
- 正常端字段和命令按钮统一使用 23px 的 96-DPI 高度基线；ComboBox 的 Win10 固定底色覆盖必须读取整控件悬停态，使闭合选区与原生箭头同时变色，弹出列表仍保持系统原生行为。
- 正常端安装页的“引导模式”和“启动签名”必须按当前语言与字体测量两段标签，复用同一标签列宽和标准字段间距，使两个 ComboBox 的左边界严格对齐；不得为字数相同的中文标签配置不同固定宽度。
- 正常端闭合 ComboBox 在 WinPE 中必须由确定性自绘路径完整处理背景擦除和非客户区绘制，不能让精简版 USER32/UxTheme 的库存画刷在控件底部留下亮色横线；Windows 10/11 即使移除 `WS_BORDER`/`WS_EX_CLIENTEDGE` 仍可能保留主题客户区内缩，因此同步闭合重绘必须通过 window DC 先覆盖完整可见字段，再最后绘制唯一外框，禁止只用 `GetDC` 从内缩后的客户区开始绘制而在顶部遗留两像素库存灰白弧线；真实鼠标按下会进入 USER32 的嵌套下拉跟踪循环，默认过程可能在 `WM_LBUTTONDOWN` 返回前重新覆盖闭合字段，因此必须在该嵌套循环内部投递并执行一次确定性同步重画，不能只验证直接发送 `CB_SHOWDROPDOWN` 的程序化路径或把返回后的重画误认为展开期间已生效；`CBS_DROPDOWNLIST` 的内部闭合选区子窗必须在点击、焦点和 UI 状态变化后同步覆盖完整客户区及非客户区底边，且不得与父 ComboBox 重复绘制文字或叠加焦点线；保留展开列表高度的 ComboBox HWND 必须把自身窗口区域裁剪到共享 23px DPI 字段基线，不能采用包含库存非客户区余量的 `COMBOBOXINFO` 高度；首次主题事务、字体/DPI/主题变化及首次展开前必须同时通过 `CB_SETITEMHEIGHT` 固定闭合字段和弹出列表行高，再同步裁剪和重绘，禁止首次深色弹层比切换主题后的弹层更矮或在字段下方留下窄带。弹出的独立 ComboLBox 仍保持系统原生绘制、键盘和无障碍语义；热态只能在进入/离开真正改变时合并失效，禁止因 `WM_MOUSEMOVE` 或 UxTheme `WM_TIMER` 同步整控件重画。不得给保留展开高度的 ComboBox 强加 `WS_CLIPSIBLINGS`，否则它的完整布局矩形会裁掉后续同级控件。所有原生 ListView 必须统一启用 `LVS_EX_DOUBLEBUFFER`；圆角外框必须由位于真实 ListView 上方的同父 STATIC 独占，并用外矩形减去内圆角矩形的空心 HRGN 只保留外框像素，真实 ListView 保持原父级、控件 ID、通知、选择、键盘、滚动和无障碍语义并只缩入 DPI 缩放的外框宽度。覆盖层必须返回 `HTTRANSPARENT`，持续保持在对应 ListView 之上并跟随其 `WS_VISIBLE`、启用、位置、尺寸和父级 DPI 状态，隐藏页面初始化时严禁无条件创建可见 STATIC；带表头的 ListView 覆盖层必须在外框发布后用表头色修补上沿内侧接缝，禁止把表体底色刷到表头顶部形成黑线；滚动消息不得同步重画或失效表体、表头和外框，避免 comctl32 像素滚动把固定外框复制进项目行。表头绘制事务不得反向触发整个 ListView 重绘，避免形成反馈循环和闪烁。单行 Edit 的首帧原生客户区绘制完成后必须补画确定性外框，不能依赖鼠标悬浮才修复边框。
- 带表头的 ListView 覆盖层上沿及两个内圆角必须使用真实表头底色参与确定性抗锯齿，底部两个内圆角继续使用表体底色；覆盖层在真实表头高度内的左右空心边带必须先用表头色清除内侧表体窄带，再按 DPI 恢复最外侧连续描边，禁止在表头两端暴露表体黑线、加粗直边、缺失描边或通过滚动复制该接缝。带 `LVS_EX_CHECKBOXES` 的 ListView 必须保留原生 one-based 状态图像、键盘切换、通知和无障碍语义；共享主题层只替换最终可见图元，并复用普通 `BS_AUTOCHECKBOX` 相同的 13px Windows 11 明暗/DPI/禁用状态资源。状态图元槽必须按真实选中或未选中行背景清除，不得另画方框、勾号或叠出双重复选框。
- 正常端主窗口和全部工具对话框在首次显示前必须完成字体、主题、布局和后创建子控件准备；主窗口从创建到销毁都必须是普通不透明输入窗口，禁止添加 `WS_EX_LAYERED`、`WS_EX_TRANSPARENT`、颜色键或透明首帧屏障，因为 Windows 7/10/11 都可能把真实点击交给后方窗口。显示前必须在隐藏状态完成控件树准备，显示后同步重绘非客户区、客户区及全部子控件，并在进入消息循环前再次确认扩展样式不含任何点击穿透标志；发现异常必须销毁窗口并失败关闭。禁止让 WinPE 先呈现白色 Edit、ComboBox、ListView 或按钮中间帧后再靠定时器、鼠标悬停修复。语言切换等整树文本事务必须在 `WM_SETREDRAW(FALSE/TRUE)` 之间完成，并通过一次 `RedrawWindow(RDW_FRAME|RDW_ALLCHILDREN|RDW_UPDATENOW)` 发布完整帧，不能暴露逐控件更新的混合语言中间帧。

- 正常端单行 Edit 必须保持普通不透明、非分层子 HWND；禁止 `WS_EX_LAYERED`、颜色键和依赖系统保存的离屏图像。USER32 继续独占文本缓冲、光标、选择、IME、键盘命中与无障碍语义。单行 Edit 没有原生垂直对齐接口，必须保留原控件 ID 和直接父子关系，以当前字体 `tmHeight` 把真实 Edit 垂直居中到 23px DPI 字段行，并由同一父窗口下无 ID、无通知的同级 STATIC 只承载完整高度的底色与圆角外框；严禁重新引入父级包装 HWND、消息代理、`EM_SETRECT`、`WM_NCCALCSIZE` 或自绘正文。水平 `EM_SETMARGINS` 必须在字体和 DPI 确定后设置；首次显示、语言和主题事务不得依赖鼠标或计时器才显现。普通短 `SS_LEFT` 标签保持库存顶对齐，仅 `SS_CENTERIMAGE` 标签垂直居中。主窗口不得使用 `WS_EX_COMPOSITED`。
- 正常系统端深色主题的引导按钮、选中导航和 ListView 真实选中行必须共享用户实测的 Windows 11 强调色 `#4CC2FF`；悬停和按下态可作克制明度变化，但静止填充不得漂回系统蓝或旧深青色。
- 正常系统端在线资源目录状态栏必须由 `CatalogueState` 统一映射；刷新请求进入 `Loading` 时显示进行中，成功替换目录行并进入 `Ready` 后必须立即显示完成文案，失败时显示控制器保存的具体错误，禁止目录已可用而仍遗留“正在刷新”状态。
- 正常端“小白模式”开关必须立即隐藏并重排左侧“在线下载”和“工具箱”导航 HWND，关闭时立即恢复；不得只阻止点击、只改变安装页或等到重启后才同步可见性。已经移除的“修改引导命令”、DiskPart 脚本复选框和脚本目录入口不得创建隐藏控件、保留命令分发或占用布局位置。主窗口每次启动必须按实际显示器工作区重新居中，不得复用或推断上一次拖动位置。
- 正常端安装页的“开始安装”可用状态必须来自同一份完整安装意图校验。PCA/EFI 只读探测临时挂载或卸载 ESP 引发的设备变更不得与分区库存扫描并行：探测期间只记录刷新请求，探测终态后合并执行一次；刷新待处理、进行中或重新触发 PCA 探测时必须保持安装按钮禁用，所有状态稳定后再一次性发布最终可用状态，禁止先按旧目标快照短暂启用再回退。设备变更后的刷新失败必须持续失败关闭，直到人工或后续成功刷新接受新库存；不得恢复使用旧目标快照。若已启用的按钮因校验状态变化被禁用，状态栏和日志必须保留具体校验原因，不得仍显示“检测完成”掩盖阻断条件。
- 新增或修改用户可见中文字符串时，必须同步更新 `assets/release/lang/en-US.json`。
- 正常端可分发测试包必须显式启用 `dev-build` feature，并由共享构建信息模块的编译期 `DEV` 常量统一控制；测试包主窗口标题和关于页必须持续显示“测试版”“测试软件”“仅供测试使用”，正式构建不得读取外部配置临时开启或误带这些标识。
- 正常端和 PE 端关于页、启动日志、诊断支持包及 Windows 文件属性中的唯一用户可见版本必须来自对应 `build.rs` 本次构建产生的固定格式 `BUILD_VERSION=vYYYY.MM.DD`；Cargo.toml 的内部 SemVer 与 `CARGO_PKG_VERSION` 不得再显示为第二个“包版本”。构建脚本必须监听端内 `src`、`Cargo.toml`、共享 `lr-core` 和 `SOURCE_DATE_EPOCH`，禁止源码已经重新编译却复用旧日期。设置 `SOURCE_DATE_EPOCH` 时保持可复现 UTC 日期，未设置时使用实际构建时刻的 UTC 日期。
- 两端相同文案应保持键和值语义一致；不允许只更新正常系统端或只更新 PE 端语言文件读取逻辑。
- `zh-TW` 是正常端和 PE 端共同内置的 `繁體中文 - 中國台灣` 选项：缺失外部词条时必须通过 `lr-core` 的 Windows NLS `LCMapStringEx(LCMAP_TRADITIONAL_CHINESE)` 和繁體中文常用术语表从简体源文案确定性生成，不得回退简体；可选 `zh-TW.json` 只覆盖同名词条，不能成为 PE 可用性的前置条件。
- `en-US`、`ja-JP`、`ko-KR`、`fr-FR` 和 `de-DE` 的完整发布词表必须同时内嵌到正常端和 PE 端 EXE，并由 `assets/release/lang` 的同名 JSON 作为唯一源文件；外部同名文件只覆盖已有键，缺失、无法读取或解析失败时必须使用内嵌词表。CI 必须验证五套词表与 `en-US` 的键集合一致，再同步到正常端 `pkg/bin/lang` 和 PE WIM 的程序语言目录；词条增删时必须测试键完整性、非空值和格式占位符一致性。
- UI 布局必须适应不同 DPI、分辨率和中英文文本长度，不使用会造成明显空白或裁切的固定宽度。
- 主窗口最小跟踪尺寸在高 DPI、低分辨率下必须直接钳制到显示器 `rcWork`，不得在已经排除任务栏的工作区上再次扣减 DPI 放大的标题栏余量；关于页等自然内容较高的页面不能与稳定状态栏和命令栏重叠。
- 长任务必须保持进度、取消和错误状态稳定，后台任务不得阻塞原生窗口消息线程；安装与备份总进度必须按预计耗时加权，镜像释放/捕获占绝大权重，校验、格式化、引导和清理等快速步骤不得按步骤数量平均分配后在长耗时阶段前跳到约 50%。步骤切换可把当前步骤进度从 100% 重置为 0%，但总体进度在任何正常、重复或迟到消息下都不得倒退；已暂存软件在 Windows 首次登录时的真实安装不属于离线阶段，禁止伪造无法观测的进度，选择预装软件时离线终态必须明确说明后续仍会在首次登录继续安装。PE 备份进度窗必须按其六个步骤使用独立的紧凑首选/最小高度，不能沿用安装页为更多步骤预留的高度而在高 DPI 下留下大段空白；安装页现有高度和所有工作区钳制规则保持不变。
- 正常端复制安装源到 PE 数据分区时，复制、落盘、复读 SHA-256、镜像结构校验和原子发布必须位于同一条 0–100% 进度带内，并在复读与结构校验期间持续响应取消；不能在复制结束后先显示 100%，再无反馈地执行长时间同步或重复哈希。单文件 WIM/ESD 只有在禁用持久校验缓存的本次完整 wimlib 校验、复制流 SHA-256、目标落盘复读 SHA-256 和原子发布后立即取得的拒写/拒删锁全部覆盖同一份字节身份，且该锁持续持有到 manifest 发布、回执 identity 与 manifest identity 完全一致时，才可能签发 `SourceImageVerified=true`。新 PE WIM 必须用受既有缓存哈希认证的 `Program Files\LetRecoveryPE\handoff-capabilities.txt` 精确声明 `source-image-verified-v1`；正常端只有确认该能力时才序列化 `true`。旧/自定义 WIM、ISO、能力缺失或读取失败时必须完全省略该可选字段并让 PE 执行原完整校验，不得写 `false` 破坏旧严格解析器兼容性。PE 端只有在重新锁定 manifest 精确文件并回读长度/SHA-256 完全一致后才可继承结果并停止重复全量解压校验；缓存命中、源即目标、SWM、GHO 和 XP 均不得签发回执。正常端安装任务运行期间左下角状态槽保持为空，仅在取消、失败或终态需要明确提示时显示文案。
- 用户明确选择导出驱动时，Direct 流程必须在任何目标格式化之前完成在线或离线源系统驱动导出，ViaPE 暂存也必须验证目标目录至少包含一个普通非重解析 INF；导出/遍历失败或用户明确 SaveOnly 时的零 INF 必须失败关闭并保留诊断。AutoImport 的普通保存驱动恢复以标准 DISM 的真实结果为准：批量目录导入失败后逐个精确 INF 重试，非启动存储可选包最终失败只记录有界摘要并继续，只有拓扑证明的启动路径要求在目标 DISM 库存中也找不到适用候选时才可升级，禁止把可选 Wi-Fi、网卡、打印机或虚拟机驱动的失败扩大成整次系统安装失败。离线源导出若无法把当前源系统卷的真实存储父链与 DISM 导出的适用 OEM 包完整对应，SaveOnly 仍可保存完整驱动树，但不得生成虚假的承重清单；AutoImport 后续只能将相关失败报告为覆盖未知。ViaPE 的 SaveOnly 必须在清理数据分区前把整棵驱动目录复制到目标系统 `LetRecovery_Drivers` 下由受检 SessionId 唯一命名的会话子目录，拒绝源、目标及其既有父链中的重解析点，所有目标文件只允许全新创建、不得覆盖既存文件或硬链接，并双向枚举确认目录集合完全相同、逐文件回读大小和 SHA-256；目录缺失、零文件、既存或额外目标项、复制或回读不一致都必须保留任务材料并进入失败终态。PE 加载圈不得依赖低优先级 `WM_TIMER` 产生动画帧；应由高精度 waitable timer（不支持时安全回退）向窗口投递合并后的单帧消息，并保证队列内最多一个待处理动画帧。
- 详细硬件检测工具必须保持只读并在后台线程收集数据：CPU 指令能力使用 Rust 的 CPUID intrinsic，主板/BIOS/内存模块使用 `GetSystemFirmwareTable('RSMB')` 返回的 SMBIOS 表，显卡使用 DXGI 并按 `AdapterLuid` 去重，磁盘容量与分区数使用标准磁盘 IOCTL，NVMe 标识和健康数据使用 `IOCTL_STORAGE_QUERY_PROPERTY` 的标准协议通道。不得为了显示厂商私有 S.M.A.R.T. 或 SPD 字段加载内核驱动、直接访问硬件端口、绕过系统访问控制，无法从标准接口取得的字段必须明确显示为无法读取，不能以零值或空白伪装成功。
- 不为工程质量任务顺便重写无关界面，不改变已有安装、备份和工具箱工作流。

## 构建、测试与提交要求

仓库要求 Rust 1.88 或更高版本，并提交应用型 workspace 的 `Cargo.lock`。没有真实依赖变化时不得重建 lockfile。

从仓库根目录运行：

```text
cargo fmt --all --check
cargo check --workspace --all-targets --locked
cargo clippy --workspace --all-targets --locked --features "LetRecovery/non-elevated-tests,letrecovery-pe/non-elevated-tests" -- -D warnings -A clippy::uninlined_format_args
cargo test --workspace --no-run --locked --features "LetRecovery/non-elevated-tests,letrecovery-pe/non-elevated-tests"
cargo test -p lr-core --locked
cargo test -p letrecovery-pe --locked --features non-elevated-tests
cargo test -p LetRecovery --locked --features non-elevated-tests
```

`clippy::uninlined_format_args` 是 Rust 1.88 与较新受支持工具链之间表现不一致的纯样式 lint，因此单独放行；其他 Clippy 警告仍由 `-D warnings` 阻断。CI 与本地验证必须使用相同参数。

`non-elevated-tests` 只允许测试程序以非管理员清单启动，并跳过正常端启动时的管理员检测和 `runas` 自动提权，供 UI 迭代和纯逻辑测试使用；release 构建会拒绝该 feature。读取真实 Windows 安装或宿主磁盘状态的测试必须带原因标记 `#[ignore]`，只能在可丢弃 VM 中手动运行。

`dev-build` 用于生成带正常管理员清单和 release 优化的可分发测试包；它只改变明确的构建身份与用户可见测试标识，不得跳过权限、安全校验或危险操作边界。正式发布构建必须省略该 feature。

提交前还必须：

- 如果仓库存在 `.git`，每次修改完成并通过相应检查后都必须创建 Git 提交；提交信息应准确描述本次修改。只能暂存本次任务范围内的源代码、测试和文档，严禁把 `pkg/`、QA 截图、日志、压缩包、构建输出或用户文件加入提交，也不得顺带提交、覆盖或回滚用户无关的未提交修改。若目标文件已混有无法安全拆分的用户修改，应先报告冲突而不是假装已提交。
- 运行 `git diff --check`；
- 检查 `git status --short`，不得误提交 `pkg/`、本地 7z、`.cloudpe-work/`、构建输出或用户文件；
- 工作区存在用户维护的 `pkg/` 时，每次影响运行产物的修改完成并通过相应检查后，都必须执行受影响端的 release 构建并把产物同步到 `pkg/`：正常端原子替换根目录 `LetRecovery.exe` 并运行 Win7 导入边界检查；语言和固定发布资源只在源文件实际变化时同步，PE 端变化按本文“发布包 WIM 维护”授权流程更新 `bin/pe/LetRecovery_PE.wim`；同步时还必须删除发布包根目录中正常端主程序以外的 EXE。替换前后必须复核绝对路径、文件大小和 SHA-256，构建或校验失败时保留旧包。`pkg/` 仍属于本地发布产物，不得因此加入 Git 提交。
- 记录未运行测试和真实环境阻塞，不得把“无法执行”写成“测试通过”；
- 第三方 DLL 变化时同步更新 `docs/THIRD_PARTY_BINARIES.md`、许可证和 SHA-256；
- 不覆盖、不回滚用户未提交的修改。

PCA2023 离线资源必须从已维护的微软官方介质或动态更新包制作：可用 `.github/scripts/build-pca2023-pack.ps1` 从已维护的 `boot.wim` 提取，也可用 `lr-core/examples/build_pca2023_resource_pack.rs` 验证并捕获已经安全展开的固定白名单资源目录。两条路径都必须按 `docs/PCA2023_COMPAT_PACKAGES.md` 和 `docs/PCA2023_RESOURCES.lock.json` 记录来源、源哈希、关键文件与最终发布 WIM，并完成虚拟机矩阵。正式 release 会检查三份资源并把它们同时放入桌面端和 PE WIM；不得为了让流水线通过而使用空 WIM、某台机器的 ESP 备份或未经验证的 Insider 文件。

## 常见扩展应该修改的位置

### 新增安装高级选项

通常需要同时检查：

- `正常系统端/src/core/advanced_options.rs` 与 `正常系统端/src/core/ui_state.rs`：输入、默认状态、序列化兼容和离线应用边界；
- `正常系统端/src/core/install_config.rs`（扩展入口）：传递给 PE 的配置结构和向后兼容默认值；CLI 的可丢弃虚拟机终态策略必须作为布尔字段进入同一 LRHC1/HMAC 认证配置，不得使用公开 marker、环境变量或未认证 PE 参数旁路；
- `正常系统端/src/core/native_install_controller.rs`、`native_install_executor.rs` 与 `native_install_backend.rs`：正常端安装意图、执行状态机和生产后端；安装意图必须把挂载光驱中的有效镜像路径与其原始 ISO backing 路径分别传递，Direct 必须在任何目标写入和格式化前完成镜像校验并检查两者及全部后续输入，即使关闭格式化也不得跳过；每个后续目标写阶段都必须按磁盘号、偏移和长度重新解析盘符；
- `PE端/src/core/config.rs`（扩展入口）：PE 配置解析；
- `PE端/src/ui/advanced_options.rs` 或 `PE端/src/app.rs`：离线应用；
- `assets/release/lang/en-US.json`：英文翻译；
- 本文档：职责或文件变化说明。

### 新增工具箱功能

通常在 `正常系统端/src/core/tool_types.rs` 或对应 `native_*` 控制器定义状态，在 `core/native_tools_controller.rs` 接入入口，在独立核心文件实现业务，在 `native_ui/tools/` 放原生对话框。危险操作必须下沉到可测试核心边界，不能直接在按钮回调中拼命令。

### 新增在线下载类型或服务端字段

检查 `download/config.rs`、`download/server_config.rs`、下载管理器、缓存完整性、HTTPS 策略、旧配置默认值和 UI 展示。服务端输入一律视为不可信。

### 新增镜像引擎或第三方二进制

优先扩展 `lr-core/src/wim_engine.rs` 的统一入口，保留现有回退；记录来源、版本、许可证、SHA-256、可复现补丁、构建脚本和打包路径，不在调用方直接加载未验证 DLL。LetRecovery 自定义 wimlib 的并行读取必须保持有界内存、每个工作线程独立 Windows 文件句柄、按原顺序交付数据，并只在调用线程执行进度与安装回调；校验读取窗口必须计入同一内存预算，低内存时缩小或关闭，不得通过嵌套 chunk 并行、镜像索引或 CPU 厂商硬编码绕开通用路径；自动线程数必须遵守 Windows 处理器组/进程 affinity 与 Linux affinity。损坏镜像的 SHA-1 失败和取消语义不得因并行化弱化。

### 新增多步骤操作、断点或自动重试

- 状态、步骤定义、检查点和重试策略优先放在 `lr-core/src/operation/`，端内只做环境适配和消息映射。
- 每个步骤必须显式声明是否幂等；格式化、分区移动、镜像释放/捕获、引导写入等写操作默认视为非幂等，不得因进程崩溃自动续跑。
- 检查点必须使用同目录临时文件和原子替换，写入失败不能覆盖已有有效检查点，也不能阻断既有操作流程，除非该检查点本身是执行安全的前置条件。
- “断点记录”不等于“断点续做”。当前 PE 观察器只记录步骤、失败和中断，重启后保留诊断材料并从既有入口重新开始；以后实现恢复执行时必须增加目标指纹复核和专门测试。
- 自动重试仅用于明确幂等且被分类为瞬时失败的操作，必须有次数上限和退避；非幂等操作、校验失败和永久错误必须立即停止。
- 支持包只能包含脱敏环境摘要、检查点摘要和受大小限制的文本日志尾部，不得收集配置文件、源路径、密码、令牌或 BitLocker 恢复密钥。

### 新增 Rust 文件

选择最小职责目录，接入对应 `mod.rs`，增加针对性测试，并在下面的职责目录中添加条目。若文件承担多个不相关职责，应先重新划分边界。

## Rust 文件职责目录

以下目录应覆盖仓库当前全部 Rust 文件。描述的是主要职责，不代表可以跳过阅读调用点和测试。

### `lr-core` 共享核心

- `lr-core/src/lib.rs`：共享库根模块，声明并导出两端共用能力。
- `lr-core/src/bounded_failure_summary.rs`：批量诊断失败的纯逻辑有界摘要与增量收集器；完整统计总数，只保留并格式化调用方请求且受硬上限约束的少量样例，逐样例按 UTF-8 字节边界截断并把换行、NUL 和其它控制字符收敛为单行，同时显式报告剩余数。PE 逐 INF 与可选 CAB 导入只允许记录一条该摘要，禁止在内层隔离器与外层工作流重复逐包打印或为生成日志把全部错误保留在内存；摘要不得改变标准 DISM 的真实结果、可选包隔离或启动存储驱动最终回读语义。
- `lr-core/src/data_staging.rs`：ViaPE 数据暂存盘的纯选择策略与 `StagingPayloadBudget`；调用端必须先精确累计镜像全部 span/XP 树、OEM 驱动、PCA、版本化用户驱动、UefiSeven 和本次已实际下载的预装软件，再由本模块只加一次固定 2 GiB 操作余量并返回现有卷、目标卷缩分区或不可用计划。不得再叠加按镜像比例、系统卷比例或固定容量的隐藏保留；SSD/HDD、内外置和物理磁盘关系只影响候选排序，不改变容量公式。模块不探测磁盘也不执行写操作；`shrink_is_safe` 只能由调用端在核对文件系统、介质和稳定 BitLocker 状态后设置。
- `lr-core/src/software_install.rs`：预装软件选择的两端共享数据契约、URL-safe 配置编码、文件名/URL/数量/长度约束和静默安装命令解析；只允许下载文件本身或 `msiexec.exe` 作为参数化子进程，`{installer}` 必须唯一且作为完整参数出现，禁止把服务器命令交给 shell。VMware Tools 使用服务端 `vm_tools=true` 身份，但仍走同一受检安装包契约。
- `lr-core/src/install_handoff.rs`：安装配置兼容语法与固定 volume-locator marker 的精确字节格式；data/target marker 内容只能是严格 64 位小写十六进制 token，同名异内容只返回“不匹配”，不得升级成错误。
- `lr-core/src/personal_files.rs`：不格式化分区重装时的个人文件保留与旧系统快速删除事务。预检只接受离线目标每个普通本地 profile 的 `Desktop`、`Documents`、`Downloads`、`Pictures`、`Music`、`Videos`，遇到重解析、EFS、offline 或 recall-on-access 数据时必须在任何移动前停止；六类目录用同卷 `MoveFileExW(MOVEFILE_WRITE_THROUGH)` 搬入本次 SessionId 唯一根，全部成功前的失败逆序搬回，回退不完整不得声称可逆。进入不可逆边界后只删除固定旧 Windows 顶层 allowlist，目录枚举使用 `FindFirstFileExW(FindExInfoBasic/FIND_FIRST_EX_LARGE_FETCH)`，删除优先 `FileDispositionInfoEx` 并为 Win7 回退 `FileDispositionInfo`，不得调用 shell、遍历重解析目标或把未知根目录纳入清理。首登录恢复不能把旧 SID 的 owner/DACL 随同卷 rename 或 Windows 8+ 的 `CopyFileExW` 带入新 profile：普通文件必须通过 `OpenOptions::create_new` 在当前 token 的真实 Known Folder 中创建新对象，由 Windows 在创建边界直接赋予当前账户 owner 与父目录继承 DACL，再从已经打开并回读为同一普通文件的保留源句柄流式写入、flush、`sync_all`、回读目标类型和长度，最后才删除保留源。创建或写入失败必须删除未提交目标、保留源后失败；不得为了修补复制来的 owner 而新增 `WRITE_OWNER` 门禁。重解析点只作为叶对象同卷移动，禁止为复制而跟随其目标。保留 Desktop 中 `.lnk` 优先通过 `IPersistFile::Load + IShellLinkW::GetPath(SLGP_RAWPATH)` 分类；精简 PE 缺少 Shell Link COM 注册、COM 未返回文件目标，或 COM 把原目标跟踪到非目标卷（例如 PE 的 `X:`）时，只允许按微软 MS-SHLLINK 2.3 严格、有界读取 `LinkInfo` 的本地绝对路径；COM 已明确定位到旧系统卷 `Users` 时不得由二进制回退推翻。禁止猜测 IDList、网络、环境变量、PropertyStore 或 TrackerData。PE 可能重映射离线系统盘符，因此原始目标位于绝对 `C:\` 或当前已经认证的离线目标卷、且规范化后不在该卷 `Users` 下的链接随旧系统进入不可逆阶段删除，其他盘、UNC、环境变量、相对路径、无文件目标或解析失败一律保留并计数，禁止猜测。
- `lr-core/src/progress_raster.rs`：正常端、PE 端与首登 Shell 共用的圆角进度条纯 BGRA 光栅器；固定 4× 超采样几何并公开 PE dark palette，调用端只负责把同一像素结果提交到各自 Win32 DC。不得在任一端复制一套近似圆角、轨道或填充算法后再声称“同款”。
- `lr-core/src/data_staging.rs` 补充约束：不得以固定 8 GiB 或其它经验阈值把原本满足精确 payload 预算的既有卷升级成破坏性 Shrink；“全部文件逻辑大小 + 一次 2 GiB”的同一预算是唯一容量门槛。
- `lr-core/src/handoff_auth.rs`：跨重启 LRHC1/HMAC、16 字节 SessionId 和 32 字节 LocatorToken 的共享 typed 边界；安装、备份、扩容和手动 PE 维护使用四个相互独立的 purpose domain。两类随机值均由 Windows CNG 生成并使用规范小写 hex。LRHC1 绑定 purpose、session 与 exact config，key 单一持有、Debug 脱敏、Drop 清零。`LocatorToken` 非 secret，但只能通过已认证 LRHM3 取得；data/target 必须各自独立生成。LRHA1 parser 仅保留历史格式单元测试/兼容解析，不再是生产公开卷授权或发现入口。该文件同时维护私有 PE 暂存命名空间的纯文件名分类器：只接受固定 boot 文件、规范 boot GUID token 和 `ScopedTempFile` 的十进制 PID/唯一序号格式，journal、目录、重解析点和任意近似名称不得因此获得删除授权。
- `lr-core/src/handoff_manifest.rs`：LRHM3 跨重启清单；携带独立 256-bit data locator、安装专用独立 target locator、外部 artifact exact-set，以及仅用于自动暂存恢复的 canonical post-shrink source/temporary pair 与 exact pre-shrink source length。PE 必须按受认证的缩前、缩后实际长度差扩回，不能用临时分区长度猜测 provider 的真实 Shrink 回收量。维护 purpose 不使用磁盘 locator，只允许零个或一个私有 `ProtectedBitLockerSecret`；其它 purpose 不得携带该 secret 角色。清单通过 authoritative config 的长度/SHA-256 binding 进入 LRHC1 信任链；普通 data/target 定位不得再携带 canonical 磁盘库存。
- `lr-core/src/install_source_lock.rs`：正常端 Direct 与 PE 端从镜像校验前贯穿释放完成的安装源句柄与替换检测边界；拒绝源路径任意祖先重解析点，持有规范父目录祖先的身份句柄，对 WIM/ESD、连续 SWM 和 GHO/GHS 全套文件逐个只允许额外读取并记录长度/SHA-256，应用入口必须紧邻复核全部祖先身份、重枚举分卷并对同一打开句柄复核，镜像引擎只能接收该守卫返回的规范路径。ViaPE 多分卷复制必须按 ordinal 从这些原句柄逐一写入安全会话目录，不能按源路径重新打开；PE 必须把实时严格枚举与 LRHM3 ordered paths 完全比较，额外 late SWM/GHS 与缺失、乱序、非连续 span 一律拒绝。`LockedInstallTree` 对 XP/2003 目录树逐级持柄、拒绝重解析并在复制前后按清单复核；PE 端递归消费者只能读取经受保护 writer 生成并双端回读的 X: 会话快照，不得重新枚举公开数据目录。部分 Windows 文件系统允许已打开父目录被重命名，因此本边界不得被描述为从密码学上消除了最后一次复核与第三方按路径打开间的全部竞态；不得把“验证后再按原始盘符路径打开”或只锁首个分卷视为同一来源保证。
- `lr-core/src/install_log_handoff.rs`：按安装 `SessionId` 绑定的正常端日志中继和最终合并边界；正常端只有在真实日志落盘屏障完成后才可把脱敏、限长、SHA-256 绑定的快照发布到数据分区，正文必须使用内容寻址的不可变 blob，最后原子替换 manifest 作为提交点，禁止用可被中断覆盖的两个固定文件冒充事务；marker 观察、`FlushFileBuffers` 和快照读取必须保持同一已打开文件对象，禁止屏障后按路径重新打开。屏障扫描必须对有界候选逐项容错并保留最后诊断，不得让首个损坏或不可读 `.log` 遮挡真正活动日志，超限日志只能保留带明确截断标记的完整行尾部；PE 必须在清理数据分区前把该快照复制到当前实际可写的 RAM 盘日志目录，并在格式化前持有一次性终态发布器：镜像或 XP 文件树一旦可用就立即向新系统固定入口 `LetRecovery\Logs\LetRecovery-install.log` 发布首个检查点，后续任何失败再次刷新目标日志，完整成功在清理及重启意图记录后再生成最终合并版和会话名副本；只有目标系统尚不可用时才把失败日志发布到已经稳定绑定的数据分区诊断目录。该失败回退必须同时接受 typed task 已持有的当前 volume GUID 根和普通盘符根，不得为了写日志给隐藏暂存卷另分配盘符；发布前后都通过共享单 extent IOCTL 边界回读同一稳定卷身份。任一端日志缺失或最终校验失败时必须写明确占位并继续发布另一端日志；承重的跨重启中继目录、manifest 和 blob 仍要求 ACL 收紧，最终新系统中的已脱敏诊断日志不是授权输入，只需拒绝现有 reparse point、原子替换并逐字节回读，ACL 收紧失败记录 warning，不得叠加易竞态的祖先身份门禁而丢失全部日志。日志中继、校验或合并失败只允许写 warning，不得弹框、覆盖原安装错误、改变安装结果或阻止重启；禁止复制配置、恢复密钥和密码材料。VDS `CreatePartitionEx.ullSize` 必须先按微软字节单位原值提交；只有真实 `E_INVALIDARG`、空 async 指针且规范布局回读证明无变化时，才可按当前设备逻辑扇区做有界容量兼容重试，禁止在调用前用“一边向下、一边向上”的取整比较误拒绝非整扇区的镜像容量下限。`IVdsVolumeMF::AddAccessPath` 后置回读必须接受微软明确允许变化的 basic-data `GPT_BASIC_DATA_ATTRIBUTE_NO_DRIVE_LETTER` 单一位，同时继续要求 extent、分区类型和 GPT PartitionId 不变；不得把该文档化属性副作用误报为分区替换，也不得放宽其它 attributes 或 MBR 元数据。
- `lr-core/src/unattend_account.rs`：内置 RID-500 Administrator 高级选项、敏感字符串脱敏与非持久化边界、账户名/密码验证，以及两端复用的 `AdministratorPassword`、短命会话账户和 AutoLogon 无人值守片段生成。内置账户模式必须按本次 32 位十六进制会话 ID 派生唯一 `LrOOBE-*` 临时管理员并写入同一 `UserAccounts/LocalAccounts`，落实微软“显式创建至少一个账户即可跨版本跳过 OOBE 账户创建”的确定契约；AutoLogon 必须先指向该临时账户，并利用微软已记录的 `LogonCount` 加一行为保留第二次自动登录。真实 Windows 11 已证明 OOBE 会撤销 specialize 阶段成功完成的 RID-500 改名，且第一次自动登录会消费并删除 `Winlogon\DefaultPassword`；因此不得再在 specialize 预先准备 RID 500、直接 AutoLogon 到目标名称或假定明文密码首登后仍存在。specialize 只调用固定原生 helper，把已认证暂存 secret 转存为本地 `L$` LSA private data 并立即删除明文文件；目标名称必须留给 OOBE 完成后的原生两阶段首登事务。普通无人值守本地用户名必须复用其 Windows 语法与系统保留身份校验，禁止把 SYSTEM、TrustedInstaller、DWM/UMFD 虚拟账户等服务令牌写成待创建用户，同时不得用宽泛前缀误拒绝普通用户名。
- `lr-core/src/unattend_command.rs`：两端共享的 Windows Unattend 命令契约、必需内置 hook 的写盘前可用性校验、required specialize 失败码和 XML 片段渲染；用户明确要求的预装 AppX/Windows Security UI 最终收束只有在启用内置无人值守、没有自定义应答文件且不是 GHO/GHS/XP 来源时才可进入破坏性流程。模块集中执行 specialize `RunSynchronousCommand` 的 Order 1–500、Path/Description 259 UTF-16 单元上限，并为承重 helper 固定 `WillReboot=OnRequest` 与大于 2 的失败码，以及 FirstLogon `CommandLine` 1024 UTF-16 单元上限和非空/XML 字符校验，再统一转义文本。该模块只验证微软确定性字段契约，不替代针对实际目标 WIM/catalog 的 Windows SIM 全量 schema 验证。
- `lr-core/src/first_logon.rs`：Direct 与 PE 内置无人值守共享的固定首登收尾器，以及私有 Wi-Fi profile 的长度/SHA-256 绑定和目标暂存边界；另把当前已验证 LetRecovery EXE 以碰撞安全临时文件复制、哈希回读为首登阶段原生账户 helper。内置 Administrator 模式使用两阶段事务：PE/Direct 只把规范 secret 文件放入固定暂存根，specialize 的 SYSTEM helper 通过 LSA private-data API 加密存入本地 `L$LetRecoveryBuiltinAdministratorPassword`、逐值回读后删除明文；临时 `LrOOBE-*` 首次登录完成 OOBE 后，helper 捕获其 level-4 SID，发布规范 marker，按 RID 500 必要改名并启用，从 LSA secret 取回密码但绝不记录，原子配置仅剩一次的目标账户 AutoLogon，再安排原生重启；没有资料保留时继续使用固定根 HKLM RunOnce，启用资料保留时则必须注册精确目标 SID 的 Task Scheduler 2.0 LogonTrigger，固定为 `TASK_LOGON_INTERACTIVE_TOKEN`/`HighestAvailable` 并回读规范 XML。第二次登录必须先证明当前账户就是目标 RID-500 名称，然后按 marker 中原 SID 删除临时 SAM 账户和 profile，完成全部首登工作后才删除 AutoLogon 值、LSA secret 和 marker。正常端 Direct 与 PE 生成首登脚本都必须通过 `BuiltinAdministratorTransitionAccounts` 成对传递目标名、临时名和脱敏密码引用；安装后 helper 可能是正常端或 PE EXE，两端 `main.rs` 都必须分派相同 store/begin/finish/retire、Shell gate、原生进度 Shell 和 pre-Explorer restore 私有路由，禁止只接一端或退回可独立为空的生产参数。任一阶段失败必须保留暂存、marker 和非零终态，launcher 检测到未完成 marker 时不得清理目录，禁止遗留第二个管理员、错误 profile 名或明文自动登录材料却报告完成。不依赖可能被 OEM key 抑制且不检查退出码的 `SetupComplete.cmd`，也不得把包含重定向、条件判断和清理语义的长命令直接塞进 FirstLogon XML；无人值守只调用目标卷固定 `LetRecovery-first-logon.cmd` launcher，launcher 隐藏启动 PowerShell 收尾器、保留并传播退出码，只在成功后删除敏感暂存目录，最后自删。资料保留不得再等 Explorer 出现后才恢复：微软明确不支持在 Windows 10 OOBE 前设置自定义 Shell，因此第一次 FirstLogon 只能在 OOBE 后原子保存并回读既有 `Winlogon\Shell`、注册精确 SID 的最高权限交互任务和一次性第二登录；上述状态全部持久化回读后必须走仅限内部 OOBE 过渡的 `bForceAppsClosed=TRUE` 原生重启，避免无用户文档的首登 helper 把流程卡在关机阻塞页，禁止扩大到普通桌面重启。第二次登录由已验证并哈希回读的 staged `LetRecovery-account-helper.exe --internal-personal-restore-progress-shell <SessionId>` 直接暂代 Winlogon Shell；它必须创建 `WS_POPUP | WS_VISIBLE` 的原生全屏顶层窗口，并在 Winlogon 可能通过 `STARTUPINFO` 覆盖首次 show state 时再次显式显示，禁止再依赖 console host、`GetConsoleWindow`、`IsWindowVisible` 或任何嵌套 CMD/PowerShell 窗口。普通用户进度 Shell 只能只读观察 SYSTEM 创建的受认证状态和回执，不得写 `LetRecovery_Scripts` 或 `ProgramData`；创建窗口、GDI、timer 或消息循环失败时必须保留全屏错误页或置顶错误框，绝不能静默退出为黑屏。精确 SID 的最高权限交互任务在同一用户 profile 中完成资料恢复，原样恢复并回读原 Shell、退役 AutoLogon、发布完成和 Shell-release 回执，然后由同一高权限任务参数化启动系统目录实际 `explorer.exe`；只有同一会话稳定 `GetShellWindow` PID 被写为 `SessionId:ShellPid` 回执后，原生进度 Shell 才退出。诊断写失败不得变成重复启动 Explorer 或黑屏；任务注销失败只形成有界 warning，因为成功父收尾器会删除其固定 action 目标。收尾器必须先完成资料恢复和 Shell 放行，再复跑 SecHealthUI、预装 AppX、预装软件、Wi-Fi 和用户脚本，网络/WSUS 或可选维护不得延迟资料可见性。所有子进程退出码、WLAN reason code 和回读仍须检查，禁止回退 `netsh`。SecHealthUI 首登重试必须显式启用当前 KB5007651 候选抑制，并给包含同步 WUA 搜索的子进程设置明确总超时；超时后终止该子进程、记录 warning 并继续其它首登收尾。ViaPE 的含密钥 XML 只能作为受认证私有 boot.wim 的固定文件传递，公开数据卷和日志只允许出现非机密诊断，PE 必须在写盘前持有拒绝写删共享的文件句柄并按 HMAC 配置中的长度/摘要核对。PowerShell 进程不得把删除当前正在执行的脚本目录当成成功；唯一 FirstLogon `cmd.exe` 父命令必须在 PowerShell 退出后读取其退出码、静默有界重试删除并复核 `LetRecovery_Scripts`，清理失败才写固定错误并返回非零。
- 首登进度外观职责：正常页面只保留居中“正在恢复个人文件”和 `lr-core::progress_raster` 与 PE 共用的圆角绿色活动条，不得再次堆叠阶段说明、关机提示、桌面说明或独立蓝色 marquee。标题和 10px 逻辑进度条必须随当前显示器 DPI 缩放；动画脏矩形必须像 PE 进度窗口一样先在兼容内存位图中完整合成，再以单次 `BitBlt` 发布，禁止直接向屏幕 DC 先清背景再补画进度条。`non-elevated-tests` 可通过正常端专用预览路由显示同一 renderer，但正式 release 不得把预览路由当成生产授权入口。
- 资料恢复补充职责：旧的 Explorer-stage 入口只作为兼容恢复路径，不再是新安装的承重流程。新流程不能用 `FirstLogonCommands` 的“桌面前”描述推导无限阻塞；微软 `RequiresUserInput=false` 的默认值最多只保证约两分钟，因此普通账户必须在首次命令内完成门禁/任务/一次性自动登录回读后立即重启，内置 Administrator 则复用已有账户切换重启。Task Scheduler 最低 Vista，项目 Win7 下限可直接使用；`RegisterTask` 必须采用 Task Scheduler 1.2 XML、`TASK_CREATE_OR_UPDATE`、精确用户 SID、空密码、`TASK_LOGON_INTERACTIVE_TOKEN`，并回读 task path、enabled、SID、`InteractiveToken`、`HighestAvailable` 和固定 launcher。不得改用组 SID 配 `INTERACTIVE_TOKEN`，微软明确说明该组合虽可注册但不会运行；不得退回 `schtasks.exe`。恢复时旧 Known Folder 中大小写任意的普通 `desktop.ini` 只属于旧目录显示元数据，必须在保留源中精确删除，不得覆盖新 profile 的同名元数据或生成可见的 `desktop (from ...).ini`；同名目录、重解析点或其它非普通对象仍须停止。遇到非持久 profile 或恢复失败必须保留受认证源、pending、Shell gate 和精确任务供后续登录重试，failure marker 只用于等待界面诊断，绝不能代替完成回执；禁止未经目标 SID 绑定新增全用户启动项或无界重启循环。旧 Explorer waiter 若被兼容路径调用，仍必须要求 `IMAGE_STATE_COMPLETE`、同一 Shell PID 稳定和 `GetProfileType` 持久 profile，不得叠加 `OOBEInProgress`/`SystemSetupInProgress` 必须存在且等于零。固定根 cleanup launcher 必须在 helper 退出后再删暂存目录，调用 `cmd /S /C` 时必须通过 Rust Windows `CommandExt::raw_arg` 保留整段 command string 的外层引号；launcher 不得依赖父 PID 托管等待，必须有界重试删除仍被当前 helper 映像/lease 占用的暂存目录并记录尝试次数。CI 自动关机必须给 cleanup 与 profile 落盘留出明确的 300 秒有界时间，并使用 `bForceAppsClosed=FALSE` 让 User Profile Service 和交互会话正常收尾，禁止用强制关机制造资料丢失假象。
- 预装软件补充职责：只消费 `software_install.rs` 生成并暂存的参数化 JSON 计划；Windows PowerShell 5.1 对顶层 JSON 数组必须显式进行 pipeline 枚举，禁止把多软件数组作为单个 `System.Object[]` 条目处理。首登暂存根必须从绝对 `%SystemRoot%` 提取卷根后组合，禁止把仅含 `C:` 形式的 `%SystemDrive%` 直接传给 `Path.Combine` 生成随进程当前目录变化的 drive-relative 路径；软件、Wi-Fi、清理脚本和用户首登脚本必须共享同一绝对暂存根。计划逐项用 `Start-Process -ArgumentList` 隐藏执行；每项最多有界尝试三次，退出码 `0/1641/3010` 视为该项完成，其它退出码、超时或启动失败在重试耗尽后记录有界 warning 并继续下一项及 Wi-Fi/用户脚本。成功项删除对应安装包，统一收尾器最后删除并复核整个 `LetRecovery_Scripts`；软件安装失败发生在系统已释放且可启动之后，不得倒打镜像与引导成功结果。只有版本化 CLI 显式启用并经 LRHC1/HMAC 配置认证的 `automation_shutdown_on_terminal` 才能在该收尾器终态调用原生 `InitiateSystemShutdownExW`；必须先写完结果日志，给父 `cmd.exe` 留出清理时间，使用计划安装原因码，并在失败时保留非零终态，GUI 默认行为不得改变。
- 首登资料门禁状态界面补充职责：Winlogon 可能通过 `STARTUPINFO` 覆盖 GUI 进程第一次 `ShowWindow` 的显示命令，因此进度 Shell 必须在 `CreateWindowExW` 创建时就带 `WS_VISIBLE`，并再次显式显示；`CreateWindowExW` 返回空句柄、`SetTimer` 返回零和 `GetMessageW` 返回 `-1` 必须分别按微软契约处理，GDI brush/font 在窗口生命周期结束后释放，timer 在成功退出和 `WM_DESTROY` 都显式停止。失败 marker 必须显示持久错误页，禁止把有意门禁表现成无提示黑屏。状态界面只能观察受认证的 completed/released/verified marker；绿色条是无数值的活动动画，不得显示或记录猜测的文件百分比，也不能替代完成回执。Explorer 不得由批处理或普通用户 helper 反复启动；最高权限交互收尾器必须从 `GetSystemDirectoryW` 推导 Windows 目录、参数化启动 `explorer.exe`，并在同一会话读到稳定的 `GetShellWindow` PID、原子发布回执后才允许进度 Shell 成功退出，失败则保持可见错误页并重试。聚焦 CI 必须枚举 helper 的真实顶层 HWND，验证可见、未最小化、矩形与显示器相交、PID 位于同一交互 session 且 Explorer 数为零；固定 `console_visible=true`、仅验证进程存在或未分析像素却声称截图非黑屏均禁止作为证据。
- 首登 Shell/RunOnce 命令生成必须写入 Windows 真正的双引号字节；Rust 原始字符串中的 `\"` 会把反斜杠原样持久化到注册表，导致 Winlogon 无法启动 gate。回归测试必须在含空格的 launcher 路径下精确比较完整命令，并明确拒绝反斜杠引号，禁止只用 `contains(cmd.exe)` 一类弱断言。
- `StorageDeviceIdProperty` 合法不可用时只允许每个当前 disk number 在进程内记录一次 warning，说明仍由同句柄容量、GPT/MBR 分区表 token 和精确 extents 收束；每次快照底层返回的 `ERROR_INVALID_FUNCTION`、`ERROR_INVALID_PARAMETER` 或 `ERROR_NOT_SUPPORTED` 只进入 debug，禁止同一可选能力缺失在 canonical 多次回读中刷屏。其它错误、描述符损坏和 RAW 盘约束不得因此降级。
- `lr-core/src/bl_passthrough.rs`：手动 PE 维护的 `LRBL1` 会话恢复密码载荷；只接受并规范化有界数量的 48 位 RecoveryPassword，去重且拒绝注释、卷标、盘符、空项、重复和非规范字节，返回值使用清零容器。
- `lr-core/src/boot.rs`：共享 XP/2003 Legacy 引导写入；在修改引导扇区前验证 Bootsect、NTLDR、NTDETECT 和 boot.ini，命令启动或非零退出必须失败关闭，并提供可注入命令执行器的无写盘测试边界。已经移除 UI 授权入口的 `repair_boot.txt` 不得在两端恢复执行。
- `lr-core/src/boot_pca.rs`：PCA2011/PCA2023 签名与 EFI 架构识别、固件信任评估、ESP 访问与临时挂载、模式决策、BCDBoot 调用、完整 BootEx 兼容回退，以及按目标 `ntdll.dll` 版本分流的 Vista/7/8/8.1 标准 UEFI 引导与 Windows 7 x64 锁定 UefiSeven 校验、微软注册入口和固件 fallback 双入口事务部署/回滚及 VMware 原生双入口事务恢复；共享只读探测先使用卷 GUID 快路径，隐藏 ESP 无法映射时才进入带作用域的 VDS 临时盘符回退。临时 ESP 守卫必须区分“本次创建”与“原本已有”的盘符，已有盘符只借用不移除；分配前必须捕获包含稳定磁盘标识和精确分区偏移的规范布局快照，守卫持有同一快照，身份确认失败、显式关闭和 Drop 清理都只能通过 checked 访问路径边界撤销盘符，任何快照变化或清理失败都要显式报告。EFI 签名优先使用宿主 `WinVerifyTrust` 完整验证；Windows 7 仅在返回纯链信任错误时才允许核对 WinTrust provider 中无其它错误的签名与时间戳链，并要求精确包含微软发布 SHA-256 的 `Windows UEFI CA 2023` 证书，摘要错误、无签名、过期、吊销、用途错误和非 PCA2023 签发全部失败关闭。
- `lr-core/src/cached_artifact.rs`：缓存文件的安全查找、常规文件约束、元数据解析和完整性验证状态。
- `lr-core/src/command.rs`：类型化进程请求、执行结果、系统/dry-run 执行器，以及把进程启动失败转换为共享操作错误的入口。
- `lr-core/src/defender_removal.rs`：两端共享的离线 Microsoft Defender Antivirus 引擎白名单移除边界；调用方以 `RegLoadKeyW` 加载并认证 `SYSTEM`/`SOFTWARE` 后，本边界只保留 hive 祖先目录 pins，严禁为了重复“锁定”而再次 `CreateFileW` 打开已加载 hive 文件；每个实际白名单文件对象仍以拒绝 delete sharing 的普通非重解析句柄固定，按 volume/file ID 在枚举、ACL、属性与删除前后复核身份，ACL/owner、只读属性和 `FileDispositionInfo` 删除均作用于同一保留句柄；拒绝 junction、符号链接、多硬链接、身份替换、未枚举并发子项和删除后的同名抢占。仍只处理 8 个引擎服务、固定引擎目录/驱动/任务，删除后逐项复核，并显式排除安全中心、防火墙、SmartScreen、UAC、VBS 和 Defender for Endpoint 等保留组件；两端必须把这一清理的普通失败降为结构化 warning 并继续完成已经释放和修复引导的系统。
- `lr-core/src/diskpart.rs`：已停用任意 DiskPart/批处理脚本的配置兼容守卫；缺失或空目录安全跳过，发现 `.txt/.cmd/.bat` 时列出文件并失败关闭，严禁重新启动 `diskpart.exe`、`cmd.exe` 或把任意脚本文本伪装成可等价转换的 WinAPI 操作。
- `lr-core/src/download_integrity.rs`：MD5/SHA-256 选择策略、哈希验证、HTTPS/HTTP URL 策略和下载文件名验证。
- `lr-core/src/driver.rs`：SetupAPI 驱动枚举、严格导出、在线导入及 DISM 离线驱动服务的共享 Windows 实现；还通过 Windows 2000+ 的 `GetSystemWindowsDirectoryW` 枚举共享系统目录中的已发布 `oemN.inf`，用 Vista+ 动态 `SetupGetInfDriverStoreLocationW` 定位现有 Driver Store 包，去重后只读普通非重解析文件元数据，为数据分区提供无需临时复制的导出选盘预估并包含最终启动存储清单字节；该视图不是 DISM 最终物化树的大小上界，生产端必须按上文在真实导出后重基线并复核剩余空间。按官方两次调用契约读取完整 `REG_MULTI_SZ`，SetupAPI 以字节返回的 Unicode 长度必须转换到对齐的 `u16` 缓冲区并复核返回边界，禁止把 `Vec<u8>` 强转为 UTF-16；设备信息集由 RAII 保证所有错误路径都释放，设备枚举只有 `ERROR_NO_MORE_ITEMS` 可正常结束；需要判定当前控制器是否已经可用时，使用同一 `SP_DEVINFO_DATA.DevInst` 动态调用 Windows 2000+ `CM_Get_DevNode_Status`，`ulFlags` 固定为零，只有 `CR_SUCCESS` 下的 `DN_STARTED` 且无 `DN_HAS_PROBLEM` 才可报告已运行，每个节点必须独立保留 CR，只有精确匹配的承重控制器查询失败才阻断，不得让无关设备的状态异常扩大失败。已绑定驱动的 INF 名称必须读取 `DEVPKEY_Device_DriverInfPath`，禁止把 `SPDRP_UI_NUMBER (0x10)` 当作 INF 路径；Vista 及以上必须使用 `DiInstallDriverW` 完成真实安装，只有明确探测为 pre-Vista 的系统可采用 `SetupCopyOEMInfW` 暂存兼容路径，`DiInstallDriverW` 只能传 `0` 或 `DIIRFLAG_FORCE_INF`，真实安装失败不得用仅暂存 INF 的回退伪装成功。在线 OEM 导出失败不得伪装成部分成功，离线导入/导出统一调用标准 DISM，禁止手工拼装 DriverStore、猜服务注册表、按 FileRepository 目录名猜测第三方包或增加普通 OEM 包的重复自制验签门；共享驱动树枚举必须拒绝根目录和任一子项的重解析点并传播遍历/元数据错误，零个 INF 是可区分的完整枚举结果而不是默认成功；批量 DISM 失败时只按精确 INF 标准重试，不得使用 `/ForceUnsigned`。另负责生成版本 2 启动存储清单：每项必须保存源设备实例、Hardware IDs 与 Compatible IDs；版本 1 清单仅兼容读取且永远不具备拓扑证明，不能成为 PE 失败门禁。导出后验证只要求每个设备至少一个完整 ID 被对应包覆盖，禁止要求所有 ID 同时出现。
- `lr-core/src/dism_driver_inventory.rs`：通过运行中系统的绝对 `%SystemRoot%\System32\DismApi.dll` 动态调用 Windows 7+ DISM API，只读枚举离线镜像已暂存且适用于该镜像的驱动模型，并提供同语义的 DISM `/English` 包列表和逐包详情纯解析器供精简 PE 回退。`DismInitialize` 必须严格按微软契约在进程内只执行一次并保持串行复用到进程结束，禁止每次查询 `DismShutdown` 后再次初始化；每次库存调用独占一个 `DismOpenSession`、`DismGetDrivers(TRUE)`、逐 `PublishedName` 的 `DismGetDriverInfo`、全部非空成功或失败输出的 `DismDelete` 和 `DismCloseSession` 生命周期，所有返回字符串必须在释放前复制。命令报告解析必须要求匹配的发布名及模型 Architecture→Hardware ID/Compatible IDs 顺序；同名 inbox 列表项按微软文档化的发布名详情入口合并查询，out-of-box 重复、详情重复和未请求发布名仍失败关闭。单包详情失败保留为有界 partial inventory，其它包候选仍可证明覆盖，初始化、会话、列表、结构边界、释放和关闭失败仍须保留完整错误。覆盖只比较源设备完整 Hardware/Compatible ID 与目标 `DismDriver.HardwareId`/`DismDriver.CompatibleIds` 或对应英文报告字段的完整值，不得做前缀、后缀或 PCI VEN/DEV 降级，也不得用 `BootCritical`、签名或 INF 名制造第二套门禁。
- `lr-core/src/driver_trust.rs`：仅用于 WinPE 当前会话加载仓库锁定的启动存储/VMD 驱动前初始化固定 Microsoft 信任链；只允许把源码内 DER 与固定 SHA-256 完全一致的 Microsoft Root Certificate Authority 2010、Microsoft Windows Third Party Component CA 2012 和续期 Microsoft Time-Stamp PCA 2010 分别通过 CryptoAPI 加入 WinPE 本机 ROOT、CA 存储，使用 `CERT_STORE_ADD_USE_EXISTING` 保持幂等并逐字节回读返回的证书上下文。不得导入用户驱动目录携带的证书、不得写入离线目标系统证书库，也不得把本模块用于普通保存驱动的逐包预检或作为 `/ForceUnsigned` 授权。
- `lr-core/src/encoding.rs`：Windows GBK 与 UTF-8 转换；控制台输出先保留合法 UTF-8，否则回退 GBK，避免 DrvLoad 等本地化系统工具诊断乱码。
- `lr-core/src/format_command.rs`：共享格式化请求的盘符、文件系统和卷标纯验证；只描述意图，不构造或执行 `format.com`，实际格式化统一进入 `windows_storage`。
- `lr-core/src/fveapi.rs`：动态加载 FVEAPI 的 BitLocker 卷访问、状态、解锁和恢复密钥格式处理。
- `lr-core/src/hash.rs`：流式 SHA-256、兼容 MD5、普通及可失败/可取消进度回调、规范化和比对。
- `lr-core/src/image_meta.rs`：不依赖 DLL 的标准 WIM 头/XML 资源描述符边界校验、UTF-16LE XML 解码、镜像元数据解析、名称整理和镜像类型判断；本地与远程读取必须共同复用同一偏移和大小规则。
- `lr-core/src/custom_install.rs`：全盘重装与双系统共享强类型协议、`TOTALBYTES - HARDLINKBYTES + 2 GiB` 镜像容量预算、全盘布局偏好、既有暂存范围检查、Session/locator 配对和 provider free extent 选择纯逻辑；既有或显式 provider 几何不得被固定 MiB 对齐阻断。全盘 GPT 基础设施按 Windows 7–11 的共同兼容范围规划：ESP 请求 300 MiB，实际功能最低值由 PE 按当前权威 logical-sector 查询区分 512/512e 的 200 MiB 与 4Kn 的 300 MiB，MSR 使用 128 MiB；每一步都必须为后续承重分区保留功能最低预算。
- `lr-core/src/operation/mod.rs`：多步骤操作基础设施的公开导出和统一毫秒时间戳。
- `lr-core/src/operation/checkpoint.rs`：安装、备份、扩容等操作的严格状态机、步骤顺序、目标指纹、原子 JSON 检查点和事务式 journal。
- `lr-core/src/operation/error.rs`：可序列化的统一错误类别、错误码、用户/日志消息和显式可重试属性。
- `lr-core/src/operation/retry.rs`：区分幂等/非幂等的有界重试、退避策略、可注入 sleeper 和纯单元测试。
- `lr-core/src/operation/support.rs`：自包含 JSON 支持包、操作摘要、日志尾部限制、文件名隔离和凭据/恢复密钥脱敏；共享文本脱敏还必须覆盖常见签名下载 URL 的 query 凭据，只保留与鉴权无关的诊断参数。
- `lr-core/src/offline_international.rs`：两端共享的已释放离线 Windows 国际化默认值只读回退；使用进程与序列绑定的临时 hive 名加载 SYSTEM/DEFAULT，解析活动控制集、安装语言、系统/用户区域、首选键盘和时区，通过 Windows NLS 转换 LCID，任一关键值缺失、格式异常或卸载失败都保留诊断且不得伪装为宿主机默认值。
- `lr-core/src/offline_appx.rs`：两端共享的预装 AppX 精确清理边界；固定身份集合覆盖用户选项承诺的 Quick Assist、Sound Recorder、OfficeHub、Clipchamp、News、Phone Link、Solitaire、Clock、Weather、Microsoft PC Manager 及既有 Sticky Notes、Family、Teams、To Do，并明确保留新 Outlook、OneDriveSync 与 Win32 OneDrive。旧系统仍保留独立 `Microsoft.Getstarted` 身份；不得为了移除当前由共享且不可移除 `MicrosoftWindows.Client.CBS` 承载的“入门”而删除整个系统包。离线候选必须来自 fresh DISM 库存并由 Package Identity API 精确核对，在线 specialize 与首登复核必须按精确 Name/PFN、运行时完整包名和有效 SID 导入窄范围 Deprovisioned/EndOfLife retirement marker，再同时撤销 provisioning 和所有用户注册；最终 fresh 库存两者均为零才算该项成功，仍存在或库存未知必须写有界 warning。禁止通配、前缀猜测、WindowsApps/SystemApps 目录删除和把 `NonRemovable` 伪报为成功；单个包失败不得用 specialize 非零退出阻断整个安装，并由首登执行一次有界重试。
- `lr-core/src/offline_update_control.rs`：保留的旧版可逆离线“禁用自动更新”状态与恢复边界；不再接入新安装流程，只服务于 `update restore` 对 LetRecovery 旧 schema manifest 的所有权校验、compare-and-swap 恢复和兼容迁移。管理员、域、MDM 或其它程序已改变的状态必须保留，禁止把旧 manifest 当成新的组件移除授权。
- `lr-core/src/offline_windows_update_removal.rs`：两端共享的 Windows 7–11 Windows Update 活动组件离线移除边界；按目标 `CurrentBuildNumber` 选择 NT 6.x legacy WUA 或 Windows 10/11 modern USO 两套配置，差异集合来自一次性 NTLite 实验镜像 7601、19045、22635、26100 和 28000，并在首次写入前要求所选配置至少存在一个权威服务锚点和一个活动文件锚点，未知未来 build 或不匹配布局失败关闭。仅删除当前镜像中实际存在的精确活动路径；本地化资源只能在 System32、SysWOW64 和 PolicyDefinitions 的一层语言目录中按固定文件名发现，必须拒绝重解析点和非普通文件。legacy 配置删除 wuauserv、旧 WUA 活动文件、事件通道和 `FilesNotToBackup\WUA`；modern 配置删除活动 System32/SysWOW64/UUS 路径、UsoSvc/WaaSMedicSvc、精确任务缓存和稳定运行时注册，同时保留 CBS/WinSxS 组件存储、BITS、DoSvc 与 wuauserv。活动文件可与 WinSxS 合法硬链接，因此只能通过保留句柄身份删除当前路径，禁止删除其它链接或降低 ACL 安全边界；不得照搬 NTLite 对整个 `Control\Ubpm` 的宽泛删除，只允许删除名称已明确属于更新编排器的值。已有 `SettingsPageVisibility` 必须按 `hide:`/`showonly:` 语义合并并回读，禁止覆盖其它管理员页面策略；`NoAutoUpdate=1` 与设置页隐藏必须在独立批量移除前写入并回读，保证单个 edition/language 文件、任务或服务删除失败只形成有界 warning，不会截断其余项目并留下无策略的半完成状态；目标身份、配置单元、路径逃逸和上述两项稳定收束失败仍是 error。NTLite 仅为开发期一次性差异 oracle，发布包和运行时不得依赖、调用、分发或要求用户购买 NTLite。历史配置字段 `DisableWindowsUpdate` 仅为序列化兼容保留，其当前 UI 语义是“移除 Windows Update（Windows 7–11）”。
- `lr-core/src/onedrive_removal.rs`：保留的独立 Win32 OneDrive 官方卸载器边界，目前不再由“移除预装应用”选项接线；未来若新增单独且明确的 OneDrive 选项，只能原子暂存固定 PowerShell 脚本，核对 System32/SysWOW64 两个 `OneDriveSetup.exe` 候选的普通文件、非重解析点、Microsoft Authenticode signer、锁定 Microsoft 根证书 DER SHA-256 和签名前后 SHA-256，再以唯一 `/uninstall` 参数启动一个候选。不得让旧配置或预装应用清理隐式调用它。
- `lr-core/src/windows11_shell.rs`：两端共享的 Windows 11 离线 Shell 默认值边界；只在目标 `ntdll.dll` 明确为 Windows 11 时通过共享 Win32 注册表边界写入并逐项 typed readback。隔离用户明确要求的 `ForceEffectMode=2` VM 圆角兼容开关，不得宣称它是微软稳定策略或扩展到旧系统。Windows 11 OEM `LayoutModification.json` 只能追加 OEM 固定项，不能删除微软默认固定项；因此预装应用移除流程不得再写该文件或把纸牌/电脑管家占位仍在误报为固定项清理成功，只能记录明确的 `not_supported` warning。禁止复制、解析或修改 opaque、随 build 变化的 `start2.bin`，禁止为已移除或不存在的包制造死固定项，也禁止用 `Start_IrisRecommendations`、`PreInstalledAppsEnabled`、`ContentDeliveryAllowedOverride`、`DisableWindowsConsumerFeatures` 等宽泛内容开关代替精确应用/推荐项移除：26100 虚拟机已证明这些值会让 `start2.bin` 中仍存在的动态默认固定项无法解析，却不能移除独立的“入门”推荐。
- `lr-core/src/reserved_storage.rs`：两端共享的系统保留空间在线控制边界；只为已确认 Windows 10/11 build 19041+ 的内置 unattend 原子暂存固定 specialize/SYSTEM 脚本，并以只能通过同一 19041+ 版本门槛构造的不透明支持 token 约束无人值守渲染接口，禁止退化为由调用者自由传入的布尔值；脚本显示固定英文阶段，在 120 秒总期限内有限等待微软支持的 online DISM Set/Get 直接进程、以 typed DISM 对象回读、写结构化结果并始终退出 0。模块不得提供无限进程树等待、离线注册表回退、解析本地化输出或接入自定义 unattend。
- `lr-core/src/sec_health_ui.rs`：Defender 高级选项专用的 Windows Security UI 两阶段强制移除边界；复用 `offline_appx` 对两个锁定 PFN 撤销离线 provisioning，并原子暂存固定 specialize 脚本。在线阶段只按 typed inventory 的精确 Name/PFN、完整包名和用户 SID，先通过受检临时 `.reg` 导入精确 Deprovisioned/EndOfLife retirement marker，再调用微软 DISM `Set-NonRemovableAppsPolicy -NonRemovable 0`、official online deprovision 与 all-user removal；specialize 不得启动无超时的在线更新搜索，只把更新抑制状态标记为延后。共享首登收尾器的有界重试才可通过 Windows Update Agent 搜索当前未安装软件更新，仅对 KBArticleID 精确等于 `5007651` 的非 mandatory 对象设置 `IsHidden`，并以属性回读为唯一当前候选抑制成功依据；整个子进程必须有明确超时并在超时后终止，不能拖死首登。最终 fresh provisioning 与所有用户注册均为零才在结构化日志中标记移除完成，更新搜索/隐藏结果以独立 `update_suppression` 状态记录，不得把“没有当前候选”或一次隐藏成功宣传为永久黑名单。中途 API 错误但最终权威 AppX 状态已为零只保留诊断；仍存在或库存未知必须明确记录 warning，并由共享首登收尾器再执行一次，但 specialize 脚本始终退出 0，避免 Windows Setup 在已破坏边界后因返回码 3 中止并跳过后续命令。不得复制第三方脚本的 Defender 驱动/服务删除、通配符和宽泛目录/注册表删除，也不得删除 WindowsApps/SystemApps、改变 WindowsApps/AppRepository ACL、使用 AppLocker 阻断系统更新部署或把残留伪装成成功。
- `lr-core/src/pca_compat.rs`：按目标 WIM 架构和启动资源族选择内置 PCA2023 离线 WIM；运行时内嵌解析 `PCA2023_RESOURCES.lock.json`，对 WIM 大小/SHA-256、索引、白名单路径、内部 BootEx 精确 SHA-256、PE 架构和 PCA2023 签发者执行全链路复核，再完成安全暂存及 EFI_EX/FONTS_EX/boot.stl 白名单注入，不能依赖宿主信任库是否已更新。
- `lr-core/src/pca_preflight.rs`：PCA2011/PCA2023 写盘前只读策略，检查受支持系统版本和 x86/x64 架构，提取并验证所选 WIM/ESD/SWM 卷的 EFI 引导源，对不可预检镜像失败关闭；WIM 管理器必须在验签和删除共享临时目录前释放文件句柄。
- `lr-core/src/reboot.rs`：结束 PE 的 `pecmd.exe`；名字为历史兼容，模块本身不执行系统重启。
- `lr-core/src/registry.rs`：通过参数化 Win32 Registry API 管理在线/离线注册表配置单元、键和值，提供带类型校验的字符串/DWORD/二进制/递归只读查询、子键枚举、键存在性判断及删除后复核，并为 Windows 7 离线 SYSTEM hive 提供逐既存控制集关闭崩溃自动重启且回读验证的诊断边界；装卸全局 hive 必须启用并核对备份/还原权限。短生命周期只读库存优先使用 Vista+ `RegLoadAppKeyW` 句柄相对访问：不进入 HKLM/HKU 全局 namespace、不要求备份/还原权限，最后一个根/子键句柄关闭时自动卸载；Windows 7 同进程只能同时加载一个 application hive，调用方不得嵌套。只有 `.reg` 文件导入保留受限的 `reg.exe import` 兼容边界。
- `lr-core/src/sam.rs`：复用共享 Win32 Registry 边界完成离线 SAM 账户枚举、清空密码和启用账户，包含严格边界检查的二进制结构解析。只读库存必须通过 `RegLoadAppKeyW` 打开真实 SAM；成功加载但根键为空的官方 generalized 模板表示 0 个账户，不能误报读取失败。已初始化 SAM 必须逐 RID 解析用户名/状态，并与 `Users\Names` 索引大小写不敏感地精确互证；缺项、额外项或异常根布局一律返回不确定而不是猜成全新。
- `lr-core/src/backup_atomic_publish.rs`：完成态 WIM/ESD 的 Win7 兼容同卷 create/replace/append 原子发布、move-only 既有文件准备与崩溃恢复边界；用共享 SYSTEM/Administrators 私有目录及安全文件创建 API 保存 staged/previous 和有界 checksummed PREPARED journal，从单一拒绝 write sharing、允许 WinPE rename 所需 delete sharing 的旧目标句柄复制、绑定长度/SHA-256/Win7 file ID 并持续持有到 CAS，区分 Append 同对象修改与 Replace 精确重建，再依次尝试 `SetFileInformationByHandle(FileRenameInfo)` no-replace handle rename、仅对共享冲突启用同卷且禁止覆盖/复制的 `MoveFileExW(MOVEFILE_WRITE_THROUGH)`，以及仅在两者均共享冲突时启用普通文件专用的同卷 NTFS `CreateHardLinkW`。硬链接发布必须在目标不存在时创建 public 名，证明 public/private 是同一原 file ID，再按精确句柄删除 private 名并重锁 public；崩溃遗留双链接只能按完全相同 ID 回滚公共名，不支持文件系统或任何歧义保留 PREPARED。目标父目录自身和私有会话目录的 retained handle 必须共享删除，no-delete-share 祖先链只能从目标父目录的上级开始，避免阻断 WinPE 合法的 child-name mutation，同时每个 mutation boundary 必须按路径重开并与 retained file ID 重新绑定；Append 捕获前的 catalog 检查必须通过 `ExistingBackupPreparation` 对复制出的 staged file ID 和字节前后复核，并在回调期间持有拒绝写入/删除的只读句柄，不能退回未绑定路径读取；恢复按实时 old/new hash+file ID 与带 mode 的 durable committed receipt 提交或回滚，抢占或不明组合保留 previous+journal 并失败关闭。模块不捕获、追加或修改镜像内容；正常端和 PE 调用者负责构造并语义验证完整 staged 镜像。
  上述“持续持有到 CAS”以句柄 rename 成功为首选；仅在已复现的 WinPE 双 API 共享冲突回退中，模块会按前述 PREPARED + source ID + no-replace + target ID + 全量 hash 规则瞬时释放并重新取得拒写句柄，除此以外不得放宽。
- `lr-core/src/backup_image_catalog.rs`：正常端和 PE 共用的 WIM/ESD 完成态 catalog 纯语义校验；Create/Replace 必须恰有一项且 name/description 与请求精确相等，Append 必须在 checked count `old+1` 下逐 ordinal 保持全部旧项 name/description 不变并只增加一个精确尾项，空基础、溢出、缺失、额外、重排或元数据变化一律失败关闭。
- `lr-core/src/backup_handoff.rs`：正常端与 WinPE 共用的 LRBK2 备份交接协议；把 SessionId、源/目标 canonical 卷身份、严格 Windows 相对输出路径、显式 create/replace/append 策略和 replace/append 既存基础文件完整长度与 SHA-256 纳入严格 schema，并以配置全文 SHA-256 绑定 marker。未知、重复、缺失字段，DOS 设备名/ADS/穿越路径，零长度或缺失基础身份，完整物理库存中零个、多个或克隆匹配都必须失败关闭；两端不得回退盘符选择或根据文件是否存在猜测输出策略。
- `lr-core/src/scoped_temp_file.rs`：碰撞安全临时普通文件和目录、名称验证、Drop 清理、显式所有权移交、同目录原子替换、受限大小且不跟随最终重解析点的控制文件读取，以及在 `CREATE_NEW`/`CreateDirectoryW` 时原子安装安全描述符的文件与目录边界：交互式秘密使用“当前令牌用户 + SYSTEM + BUILTIN\Administrators” protected DACL，LRPE4 capsule、私有 WIM、私有 snapshot 根及其相对子目录使用 owner/group=BUILTIN\Administrators、protected DACL 精确为 SYSTEM+BUILTIN\Administrators full control 的 custody。严格 custody 必须通过同一打开句柄回读对象类型、非 reparse、owner、group、DACL protection 和精确 ACE 集，用户 owner 即使 DACL 看似受限也必须拒绝，且不得修改父目录 ACL；私有目录 guard 必须在整个生命周期持有全部既存父祖先的 no-delete-share identity pin，并保留自身及相对子目录的精确句柄身份，每次创建相对子目录和 Drop 前后复核，只能复用同一 guard 已创建且仍由 retained handle 证明身份的前缀，禁止采用磁盘任意 existing 目录。需要继续使用句柄重命名或删除的调用者必须直接接管 guard 原先已验证且具备 `DELETE` 权限的句柄；自身目录句柄必须声明 `FILE_SHARE_DELETE`，兼容 WinPE 文件系统在 `SetFileInformationByHandle(FileRenameInfo)` 中内部重开目标目录，调用者则在每次变更边界把当前路径重新绑定到 retained file ID，不能用阻断系统内部重开的共享模式冒充身份认证。Drop 只有在当前路径仍精确绑定原目录时才可清理内容并按 retained handle 删除根；身份不一致必须保留并记录 warning。控制文件的承重调用还必须先 pin 全部既存目录祖先、拒绝 junction/reparse，并在读写/回滚前后复核祖先身份。TokenUser 缓冲必须用对齐安全读取，不能把 `Vec<u8>` 直接解引用为 `TOKEN_USER`。WIM 提取目录清理可在不跟随重解析点的前提下清除只读/系统/隐藏属性后重试，并详细记录最终清理失败。固定临时脚本、备份暂存和驱动解包目录不得绕过本模块自行拼接易冲突路径。

安全 manifest 与诊断日志的路径操作必须复用本模块的目录祖先 pin：使用公开 `CreateFileW` 目录句柄语义，以 `FILE_FLAG_BACKUP_SEMANTICS | FILE_FLAG_OPEN_REPARSE_POINT` 打开每个既存祖先并拒绝重解析点，持有期间不得共享删除；创建缺失目录前后必须分别 pin，一旦无法建立完整句柄链就保守停止该次 manifest/日志发布。Update manifest 的每个 allowlist 值还必须独立持久化 `applied_by_letrecovery` 所有权：只有注册表写入、fresh 回读和 manifest 提交三者均成功才可置位；提交失败应尽力回滚刚才的注册表写入，在线恢复必须同时满足安装绑定、所有权位和 CAS，禁止仅凭当前值恰好等于禁用值推测所有权。PE 失败日志的数据分区回退必须在守卫建立时捕获完整 `StableVolumeIdentity`，发布前 fresh 回读并严格相等；查询失败、盘符消失或身份变化只记 warning 且不写入未经证明的路径。
- `lr-core/src/storage_driver_match.rs`：把 SetupAPI 当前 PCI 硬件 ID 纯函数映射到第 11 代或当前 Intel VMD 随包目录；`09AB` 单独出现时报歧义而不猜包，严格匹配 `VEN/DEV` 边界，AMD、Apple、VirtIO、相似前缀和未知控制器返回空选择；同时按固定大小和 SHA-256 验证两套发布包，公开同一精确代际匹配供 DrvLoad 后 PnP 状态复核，并提供有界 INF 树硬件 ID 覆盖检查供导出、离线注入和工具箱回读复核。对已经由 DISM 单包导入成功的认证 INF，还提供仅限单个普通非重解析 INF 的 ID 关联读取，使调用方可直接接受该权威成功结果，不因随后无关目标包详情查询失败而误判。
- `lr-core/src/win7_driver_package.rs`：解析编译期内嵌的 Windows 7 USB3/NVMe 锁定清单，逐文件验证普通文件属性、成员集合、大小和 SHA-256；按离线目标 x86/x64 架构与 SetupAPI 当前硬件 ID 只选择匹配 USB3 子包，并只向 x64 目标返回固定顺序的两个微软 NVMe CAB，缺失、篡改、额外文件和架构不支持均失败关闭。
- `lr-core/src/traditional_chinese.rs`：正常端与 PE 端共享的 Windows NLS 简体转繁体转换和繁體中文常用界面术语归一；保留 ASCII 占位符，NLS 失败时返回明确的繁体错误文案，不得回退显示简体源文案。
- `lr-core/src/windows_accounts.rs`：通过 NetAPI `NetUserEnum`/`NetUserGetInfo`/`NetUserSetInfo` 枚举当前本机普通账户、清空指定账户密码并仅清除禁用标志；RID 定位使用 level 20 的 `usri20_user_id`。RID-500 准备必须用唯一会应用名称字段的 level 0 / `USER_INFO_0` 完成必要改名，以 level 1 读取当前标志、level 1008 只清除 `UF_ACCOUNTDISABLE`，最后按同 RID 精确回读名称和启用状态；任一步失败都返回承重错误，禁止把“命令已启动”当成账户已准备。两阶段首登通过 `GetUserNameW` 证明当前 token 所属账户，通过 `NetUserGetInfo` level 4 / `USER_INFO_4.usri4_user_sid` 捕获临时账户 SID并用 `ConvertSidToStringSidW` 规范化；清理只允许对已验证的本次 `LrOOBE-*` 精确名称与同一 SID 调用 `NetUserDel(servername=NULL)`，回读 `NERR_UserNotFound` 后再以 `DeleteProfileW(sid,NULL,NULL)` 删除精确 profile，并确认对应 ProfileList 键消失。RID-500 首登凭据只允许使用 Windows XP+ 已支持的 `LsaOpenPolicy`、`LsaStorePrivateData`、`LsaRetrievePrivateData` 和 `LsaFreeMemory` 操作固定本地 `L$` secret；`LSA_UNICODE_STRING.Length`/`MaximumLength` 必须按字节填写，返回字符串不得假定 NUL 终止，NTSTATUS 必须经 `LsaNtStatusToWinError` 保留诊断，所有句柄、LSA 缓冲和密码副本必须有界释放/清零，删除后必须以 `STATUS_OBJECT_NAME_NOT_FOUND` 回读收束。逐缓冲按 `NetApiBufferFree`/`LocalFree` 契约释放，保留“密码已清空但启用失败”的部分完成状态，不解析 `net.exe`、PowerShell 或 WMI 输出。
- `lr-core/src/windows_accounts.rs` 的 OOBE 残留补充职责：普通 fresh/Easy 安装只允许清理精确名称、禁用、非当前且 RID 1000+ 的唯一 `defaultuser0`；必须先捕获 SID，再按上述 `NetUserDel(NULL,name)` / `DeleteProfileW(SID,NULL,NULL)` 契约回读账户不存在和 ProfileList 键消失，禁止按前缀、显示名或目录名扩大删除范围。正常端与 PE 必须无条件暂存账户 helper，不能因为没有启用资料保留或内置 Administrator 而跳过这项首登运行时。
- `lr-core/src/windows_cabinet.rs`：两端共享的 SetupAPI CAB 枚举与解压边界；严格按通知类型返回 `FILEOP_*` 或 Win32 错误码，拒绝非普通 CAB、路径穿越、绝对/UNC 条目、重解析目标和分卷包，并在返回成功前核对请求数、解压数及每个输出文件。
- `lr-core/src/windows_compat.rs`：正常系统端的 Win7-11 单文件兼容边界；运行时优先解析 Windows 10/11 的每窗口/系统 DPI、DPI awareness 与扩展固件变量 API，缺失时分别回退 GDI/System-DPI 与 Windows 7 固件变量 API，并通过 `GetSystemDirectoryW` 获取可信宿主系统工具目录。仅在 `x86_64-win7-windows-msvc` 正常端目标内提供 windows-core 所需 WinRT/COM 装载桥接，并在新系统上转发真实实现；普通现代目标和 PE 端不得编译这些桥接符号。
- `lr-core/src/windows_file_copy.rs`：基于 `CopyFileExW` 的普通文件与递归目录复制边界；拒绝跟随源重解析目录或覆盖目标路径上的重解析点，并以 `COPY_FILE_FAIL_IF_EXISTS` 保证承重复制不会覆盖任何既存目标文件或硬链接，逐文件保留错误上下文并供 XP/2003 文件准备和 ViaPE SaveOnly 驱动保留复用；承重复制完成后可逐文件双向枚举并回读长度与 SHA-256，禁止只依赖复制 API 成功返回。
- `lr-core/src/windows_file_version.rs`：使用 Version Information API 读取文件固定版本块，校验返回长度与 `VS_FIXEDFILEINFO` 签名后提供两端共享的版本四元组。
- `lr-core/src/windows_firmware.rs`：优先动态解析 Windows 8+ 的 `GetFirmwareType` 直接判断当前系统实际以 UEFI 还是 Legacy BIOS 启动，保持 Windows 7 可加载；API 不存在时才严格启用并核对 `SeSystemEnvironmentPrivilege`，使用微软文档规定的空变量名/全零 GUID `GetFirmwareEnvironmentVariableW` 探针。目录、ESP、盘符、环境变量和本地化命令输出不得参与固件模式推断，所有无法确认的错误必须向调用方失败关闭。
- `lr-core/src/windows_hardware.rs`：两端共享的只读机器身份探测；用 x86/x64 CPUID intrinsic 解码 CPU vendor/family/model、hypervisor bit 和 hypervisor vendor，用 `GetSystemFirmwareTable(RSMB)` 有界读取 SMBIOS Type 1，并复用 SetupAPI 当前硬件 ID 作第三路交叉识别。只有三路探测完整且均无虚拟化特征才可确认实体机；VMware 等虚拟机即使透传或伪装为新款 Intel CPUID 也必须优先分类为虚拟机，失败状态保持 Unknown。Windows 7 x64 UEFI 策略只能在确认 VMware 时绕过 UefiSeven，实体机、其它虚拟机和 Unknown 均保留兼容加载器。
- `lr-core/src/windows_diagnostics.rs`：两端共享的 Windows 错误日志交互边界；用 `TaskDialogIndirect` 显示带自定义“打开文件”按钮的终态错误弹窗，用后台 STA 中的 `SHParseDisplayName` 与 `SHOpenFolderAndSelectItems(cidl=0)` 打开资源管理器并选中已经落盘的绝对日志路径，PIDL 必须先释放再反初始化 COM。禁止拼接 `explorer.exe /select` 命令行；warning、成功带警告和取消状态不得触发错误日志弹窗。
- `lr-core/src/windows_shutdown.rs`：严格启用并核对 `SeShutdownPrivilege`，通过 `InitiateSystemShutdownExW` 安排带计划安装原因码的本机重启或显式自动化关机，禁止忽略权限未分配或调度失败；普通桌面重启和会在首次登录后写入用户资料的 CI 收尾必须使用 `bForceAppsClosed=FALSE`。只有不承担用户/profile 写入持久性验证且由用户明确启用的可丢弃虚拟机终态操作、该自动化在 PE 交接后给控制器留出有界证据持久化时间的重启，以及已经完成账户、AutoLogon、Shell gate、计划任务和回执持久化回读且仍未放行 Explorer 的内部 OOBE 过渡重启，才可设置 `TRUE`；不得因一个阻塞进程把全局交互重启改成强制。
- `lr-core/src/windows_storage.rs` 的磁盘样式补充约束：当前 MBR/GPT 样式必须从 `IOCTL_DISK_GET_DRIVE_LAYOUT_EX` 返回的 `DRIVE_LAYOUT_INFORMATION_EX.PartitionStyle` 读取，不得用可能滞后的 `VDS_DISK_PROP.PartitionStyle` 决定 BIOS/UEFI、Bootsect 或活动分区路径；Legacy 路径在当前 IOCTL 回读不是 MBR 时必须在写启动文件前停止。
- `lr-core/src/windows_storage.rs` 的隐藏 ESP alias 补充约束：完整磁盘视图必须按微软契约跨全部 software provider/pack 查询；同一候选只有在其不透明 locator 经 `IOCTL_STORAGE_GET_DEVICE_NUMBER` 回绑当前物理盘，且 `IVdsAdvancedDisk::GetPartitionProperties` 在 canonical byte offset 返回 GPT ESP 后才可用于 `AssignDriveLetter/DeleteDriveLetter`。多个通过这些条件的 VDS disk object 只是同一 `disk+offset+role` 的管理 alias，必须记录后选择一个并继续，禁止把 alias 数量误报为多个物理目标；零个可用候选、当前 layout 改变或操作后盘符 extent 不匹配仍须失败关闭。
- `lr-core/src/windows_storage.rs`：两端共享的参数化 Windows 存储管理边界；为兼容 Windows 7/WinPE 使用 VDS COM 完成分区创建/删除、带文件系统、卷标、分配单元大小及快速/完整选项的 NTFS/FAT32/exFAT 格式化、卷扩缩、盘符分配和 MBR 活动标记。Shrink 必须保持 VDS 为首选；仅当 VDS 在返回有效 `IVdsAsync` 之前失败时，Windows 8+ 正常端才可回退 `ROOT\Microsoft\Windows\Storage` 的 `MSFT_Partition.Resize`，Windows 7 继续只走 VDS。拿到异步对象以后发生的 `Wait`、`Refresh` 或回读错误可能代表已经部分缩卷，严禁再调用第二套 provider。Storage Management 入口必须用同一盘符的 canonical disk/extent 精确重绑，并在调用前后回读；先按 VDS desired reclaim 换算出的字节级最终大小执行真实 `Resize`，只有微软定义的 `4097 Size Not Supported` 且 canonical 回读证明范围完全未变时，才允许调用 `GetSupportedSize`，按 `SizeMin/SizeMax` 字节范围和原 minimum reclaim 有界重试一次。服务存在性和支持大小查询都不得替代真实写入结果，两个 provider 都不可用时必须合并保留两边错误，不得回退 PowerShell、DiskPart 或要求调用方猜测。VDS 与 WMI 持有结构必须先声明 COM interface、后声明 `ComApartment` guard，使 Rust 的字段声明顺序析构先释放接口、最后执行 `CoUninitialize`；禁止把 guard 移到接口之前，避免成功写入后在 COM Release 阶段访问冲突崩溃。其余路径使用受文档支持的磁盘/卷 IOCTL 完成 RAW 盘初始化、分区表/MBR 签名、卷物理范围查询，并通过两阶段 `IOCTL_STORAGE_QUERY_PROPERTY` 读取目标物理盘 `STORAGE_DEVICE_DESCRIPTOR.BusType`；`StorageAccessAlignmentProperty` 还提供不带固定 fallback 的当前 logical/physical sector 与 sector-alignment offset，短结构、零值、矛盾字段或查询失败必须返回错误。`CreatePartitionEx` 的 desired offset/size、raw/provider/caller hard authorization、功能最低容量与 canonical observed extent 必须分离；provider 实际几何只要仍在当前授权范围内、角色正确且满足最低容量就不得因 desired end 或逐字节差异误报。`VDS_S_UPDATE_BOOTFILE_FAILED` 只有在唯一 canonical delta 完整回读成立后才可降级为 warning。完整物理盘候选枚举必须合并所有 software provider/pack 的 `QueryDisks` 与 `IVdsService::QueryUnallocatedDisks`，排序去重后一次性返回，任何 provider、enumerator 或完整磁盘属性查询失败都必须整体失败，禁止遗漏无盘符、离线或未初始化磁盘后继续首次写入。安装专用受检格式化入口必须在打开 VDS 对象前后核对完整预期卷范围，成功后通过 `GetVolumeInformationW` 回读同一物理卷的文件系统和卷标。稳定卷身份必须使用 GPT DiskId/PartitionId 或 MBR signature+精确 extent，并在可取得时叠加 `StorageDeviceIdProperty` 中 `StorageIdAssocDevice` 的规范化设备 ID；Port/Target association 不得进入身份，设备 ID 不可得时必须明确记录退化风险。跨正常系统与 WinPE、跨重启保存的磁盘布局摘要只能来自本模块共享的 `IOCTL_DISK_GET_DRIVE_LAYOUT_EX` 规范快照，按 offset/length/type/token 排序并排除盘符、partition number、VDS 枚举顺序等易变字段；已打开物理盘句柄还可通过同一共享边界一次性复核 layout、capacity 和设备 ID，避免重开句柄混合不同磁盘代际的观察；VDS 只负责操作，不得生成跨环境摘要。COM 返回内存固定运行时解析 Windows 7 已有的 `ole32.dll!CoTaskMemFree` 后释放，禁止经新版 SDK import library 把正常端静态依赖提升为 `combase.dll`。无盘符普通卷的只读访问优先使用 `FindFirstVolumeW`/`FindNextVolumeW` 枚举卷 GUID 路径，再以 `IOCTL_VOLUME_GET_VOLUME_DISK_EXTENTS` 唯一匹配磁盘号和起始偏移，枚举句柄必须由守卫在全部路径调用 `FindVolumeClose`；不得假设隐藏 OEM/ESP 在 Windows 7 上一定具有可枚举的卷 GUID。checked 盘符分配/删除必须先从 canonical token 区分普通卷与隐藏分区：普通基础数据分区必须唯一绑定精确 extent 的 `IVdsVolume` 并使用 `IVdsVolumeMF::AddAccessPath/DeleteAccessPath`，缺少唯一对象时失败；canonical GPT ESP 必须直接通过 `IVdsAdvancedDisk` 按精确磁盘号和字节偏移分配、查询和删除盘符，不得因无关 WinPE RAM disk、已删除或不可解析 VDS volume 误阻断，也不得把普通分区错误降级到只支持 OEM/ESP/Unknown 的 AdvancedDisk。隐藏分区盘符必须使用 `IVdsAdvancedDisk::DeleteDriveLetter` 并通过 `GetLogicalDrives` 回读确认消失；`AssignDriveLetter` 已返回 `S_OK` 但 Refresh、盘符 extent 或布局回读失败时，只有保留的 AdvancedDisk、精确 offset、`GetDriveLetter` 和完整 extent 仍唯一证明本次 target+letter 才可对称删除，换绑、矛盾或不可证明时保留现场并报告 partial state。临时盘符还必须先用卷物理范围确认仍对应预期磁盘与偏移、记录 `QueryDosDeviceW` 返回的当前目标、请求 VDS 强制删除，并只在盘符仍残留时以 `DefineDosDeviceW(DDD_RAW_TARGET_PATH | DDD_REMOVE_DEFINITION | DDD_EXACT_MATCH_ON_REMOVE)` 精确移除同一 DOS 映射，禁止无目标或前缀匹配删除。只有明确的 `BusTypeNvme` 才能驱动 NVMe 默认值，查询失败或其他总线不得猜测。离线块移动后重建普通 GPT 基础数据分区时允许显式保留原 partition GUID、attributes 和 name，普通新建分区必须继续生成新 GUID。异步 VDS 操作必须同时校验 `Wait` 调用与操作 HRESULT、核对输出类型，调用方必须在写入前复核稳定磁盘指纹并在写入后重新枚举验证，当前 Windows 卷只能由 `GetWindowsDirectoryW` 和卷范围确定，不得回退写死 `C:`；所有临时盘符选择复用本模块的 `GetLogicalDrives` 掩码并在 API 失败时停止。驱动清单的只读拓扑证明还必须从所选盘符的全部 `VOLUME_DISK_EXTENTS` 出发，对每个当前磁盘号唯一绑定 present `GUID_DEVINTERFACE_DISK`，再由对应 `SP_DEVINFO_DATA.DevInst` 沿 `CM_Get_Parent` 父链枚举；多 extent 与同一父节点 alias 必须去重，循环、深度溢出、磁盘接口歧义或任一父链查询失败均不得伪造完整证明。
  `StorageDeviceIdProperty` 属于可选的 SCSI VPD page 0x83 证据，必须先用 `PropertyExistsQuery` 探测；`IOCTL_STORAGE_QUERY_PROPERTY` 在探测或 descriptor header 阶段返回微软契约列出的 `ERROR_INVALID_FUNCTION`、`ERROR_INVALID_PARAMETER` 或 `ERROR_NOT_SUPPORTED` 时，GPT/MBR 盘只记录退化并继续使用同句柄 capacity、分区表 token 和精确 extents。其它错误、短结构或损坏 descriptor 仍失败关闭，RAW 盘缺少设备级 ID 仍不得进入破坏性路径；不得因 VMware/WinPE 合法缺失该可选属性而阻断已经由 canonical layout 授权的暂存删除与扩回。
  `IVdsVolumeMF::AddAccessPath` 必须传入带结尾反斜杠的盘符根；除 `S_OK` 外，微软明确规定的 `S_FALSE` 表示访问路径已经成功添加、但辅助 GPT no-drive-letter 属性或默认共享更新可能不完整，不能按失败回滚已创建分区。两种成功状态之后都必须有界等待 Mount Manager 可见并通过该盘符句柄的 `IOCTL_VOLUME_GET_VOLUME_DISK_EXTENTS` 回读本次实际创建的完整 extent；回读换绑或始终不可读才作为访问路径失败。该 warning 只属于 `AddAccessPath`，不得泛化到 `AssignDriveLetter`、`DeleteAccessPath` 或其它 VDS API。
  对 provider 已明确返回的 free extent 或既有 partition extent，offset/length 原值是当前会话事实；显式 offset 不得取整，未给 offset 时 1 MiB 只能作为创建位置偏好且必须回退 provider 原始起点。`IVdsCreatePartitionEx::CreatePartitionEx.ulAlign` 是可选的额外对齐请求：所有普通和 caller-authorized 创建都传 `0` 让 VDS/provider 决定，禁止把 `BytesPerLogicalSector`、`BytesPerPhysicalSector`、`VDS_DISK_PROP.ulBytesPerSector`、固定 512 B/4 KiB/1 MiB 或数值 `1` 填入该参数。权威 logical sector 只用于把 desired/minimum `ullSize` 收束成完整扇区字节数。`VDS_DISK_PROP.PartitionStyle` 同样只是 provider 库存；创建参数 union、预期 token、初始化完成态和 MBR active 路径必须统一使用当前 `IOCTL_DISK_GET_DRIVE_LAYOUT_EX.PartitionStyle`，VDS 样式矛盾只记诊断。微软契约允许 VDS 把请求 offset 向上或向下取整；自动布局只能以操作前 raw free extent 的真实起点为创建后下界，显式 caller envelope 的起点仍是不可越过的授权下界，严禁把 desired offset 或 `QueryFreeExtents(0)` 的建议起点误当成 provider 不得向下取整的硬门禁。desired `offset + size` 是创建偏好，不是独立的授权上界；创建后的硬上界只能来自操作前 raw extent、provider extent 与显式 caller envelope 的合法交集。普通新 GPT 分区只以 partition type 判断本次角色，随机 partition ID 和非承重 attributes 不得伪装成角色门禁；离线块搬移明确要求保留的 GPT metadata（partition GUID、attributes 和 name）仍须独立精确回读，错误恢复必须绑定首次实际回读到的完整 token 与 extent。VDS 实际创建结果可以与请求略有差异，只要是唯一新增范围、完整位于选定 free extent、容量不少于最低需求并通过操作后回读。`QueryMaxReclaimableBytes` 只允许作为诊断估计，真实 Shrink 及当前 extent 回读才决定是否满足最低容量；不得要求 query、异步 `ullReclaimedBytes` 与当前 extent 推导值逐字节相等。`create_partition_checked` 的当前会话并发保护比较即时分区布局，不得因可选设备 ID、容量或跨环境稳定 token 抖动制造新门禁。
  物理磁盘只读库存必须从 SetupAPI `GUID_DEVINTERFACE_DISK` 的 present interface 取得并保留不透明 device path，与同句柄解析的当前 disk number 配对，禁止退回固定范围猜测 `PhysicalDriveN`。容量依次使用该 exact path 的 `IOCTL_DISK_GET_LENGTH_INFO`、同句柄 `IOCTL_DISK_GET_DRIVE_GEOMETRY_EX`；两者都失败时只允许以同一当前磁盘号的 VDS 容量作为容量字段回退并保留原错误，布局仍必须从 exact path 的动态 `IOCTL_DISK_GET_DRIVE_LAYOUT_EX` 成功取得。没有调用方精确授权 envelope、需要共享层自行发现创建位置的普通建分区，必须同时读取 `IVdsDisk::QueryExtents` raw free extent 与 `IVdsDisk3::QueryFreeExtents(0)` provider-default extent 并只使用交集，`S_FALSE` 表示空集合。Shrink 后由同盘同起点的 canonical extent 前后差值已经给出精确 reclaimed-tail envelope 时，VDS 的两个库存查询即使 `Refresh` 后仍可能滞后，禁止再把它们叠加为第二道门禁；此时只用即时 canonical 布局证明 envelope 未与现有分区重叠，随后以真实 `CreatePartitionEx` 结果和唯一 canonical delta 回读收束。`IOCTL_DISK_UPDATE_PROPERTIES` 只可作为缓存收敛提示，失败记录 warning，不得代替真实操作或新增阻断。`CreatePartitionEx.ulAlign` 固定为 provider-default `0`；当前物理盘 `StorageAccessAlignmentProperty.BytesPerLogicalSector` 只用于 desired/minimum size 的完整扇区表示。2026-08-18 Hyper-V 现场已证明先前 2048 字节报告来自误绑定的光驱 0，而正确硬盘对象会拒绝错误传入的 `ulAlign=512`。若 desired size 恰好占满非 provider 对齐的 caller envelope，真实调用可因 provider 调整后的末端越界立即返回 `E_INVALIDARG`；只有异步指针为空且即时 canonical 布局逐项未变时，才允许在同一 envelope 内用仍满足功能需求、且已经按 logical sector 向上表示的 caller minimum 有界重试一次，后续失败必须同时保留两次错误。
  当前会话内的破坏性存储入口必须从盘符卷句柄读取单一 extent，再枚举 Volume GUID 并证明存在唯一同 extent 卷，随后通过该 extent 的当前 disk number 选择所有 present SetupAPI opaque disk interface；每个 alias 都必须用自身路径打开、回读当前 device number、capacity、canonical layout 和 `StorageIdAssocDevice`，并把目标 extent 唯一映射到当前 partition。多个可读 alias 的 capacity、disk identity、partition layout、device ID 或 `STORAGE_DEVICE_DESCRIPTOR.BusType` 任一矛盾时进入 `UntrustedStorage`，禁止 Shrink/Delete/Create/Format/Wipe；单个接口不支持 legacy device-number IOCTL 时只可调用 Windows 10+ `IOCTL_STORAGE_GET_DEVICE_NUMBER_EX` 并验证真实返回结构，两个请求都失败时不得制造 Disk 0。`MSFT_Partition.DiskNumber/PartitionNumber` 仅为本次调用 locator，必须同时与当前盘符、canonical extent 和 Size 唯一匹配，跨重启不得持久化。SMBIOS、HWID、WMI 品牌字符串和 BusType 只能用于辅助诊断或默认值，不得替代上述当前卷闭环；正常端闭环无法成立时不得保存 DiskNumber 供 PE 沿用，而应由 PE 按本次认证 marker 从零枚举。
- `lr-core/src/service_diagnostic.rs`：只通过参数化 Win32 SCM 查询 `defragsvc` 是否不存在或被禁用，并在权威 Shrink provider 返回 `ERROR_NOT_SUPPORTED` 后追加通俗诊断；服务仅未运行不算损坏，不得把诊断变成写盘前阻断，也不得阻止 `windows_storage` 先执行真实 VDS/Storage Management 路径或回退 DiskPart/PowerShell。
  上述完整物理盘 inventory、稳定 token 和 canonical layout 能力只用于确实需要全盘候选证明的备份重绑、分区拓扑变更、扩缩容和恢复事务；不得被 ViaPE 普通安装格式化入口调用来替代随机 marker，安装格式化也不得因可选设备 ID 在 VDS 刷新前后由 Some 变 None 而在目标已抹除后误判失败。
- `lr-core/src/wimgapi.rs`：动态封装 Windows WIMGAPI 的镜像 apply、capture、元数据和进度回调；消息编号严格使用 `WM_APP + 0x1476` 契约，安装取消仅在官方规定的 `WIM_MSG_PROCESS` 回调中返回 `WIM_MSG_ABORT_IMAGE`，避免跨 FFI 展开或猜测非标准中止方式。
- `lr-core/src/wimlib.rs`：动态封装 `libwim-15.dll` 的打开、校验、释放、捕获、拆分和进度回调；为事务备份在完整 integrity verify 后一次性读取按 ordinal 排列的全部 image name/description catalog，供共享完成态语义边界使用；打开 WIM 后优先启用自定义 DLL 的有界并行解压与并行校验扩展，显式把当前可提交内存的四分之一（最高 2 GiB）作为并行工作预算，但当前可用提交内存不足 2 GiB 或内存探测失败时必须选择扩展契约规定的单线程上游路径，因为该预算只覆盖块缓冲而不覆盖线程栈、codec 状态、索引数组和 WinPE 自身；旧 DLL 缺少可选导出时保持串行兼容。镜像应用与校验接受调用方持有的原子取消标记，并由 libwim 进度回调返回中止状态以真正停止当前操作。SWM apply 必须接收共享严格枚举器或 authenticated typed task 给出的有序精确 span 路径，调用 `wimlib_reference_resource_files` 时固定 flags=0，禁止让 libwim 在受认证集合之外展开目录 glob。
- `lr-core/src/wimlib_dll.rs`：把内嵌 `libwim-15.dll` 与运行目录副本逐字节比较，在首次加载前通过同目录临时文件回读验证和原子替换同步新版本，避免旧 DLL 遮蔽并行扩展。
- `lr-core/src/wim_engine.rs`：wimlib/WIMGAPI 运行时选择、统一调用和失败回退；可取消的应用流程在阶段边界检查取消，libwim 路径可中止进行中的操作，取消状态不得触发另一引擎回退；WIMGAPI 进行中的调用仅在有官方可验证的中止接口后才可扩展，禁止猜测回调常量。SWM apply 无论用户选择何种引擎都必须走支持精确 resource-file 列表的 libwim，WIMGAPI 不得自行发现 sibling。捕获到既有 staged WIM/ESD 时，WIMGAPI 任一错误都必须假定文件可能已被修改并直接失败，禁止再让 libwim 对同一 file ID 追加；该私有 staged 会话只可由外层事务丢弃，旧公开目标继续由 held handle 保留。只有原先不存在 staged 文件的 create/replace 捕获才可在清理私有半成品后回退另一引擎。
- `lr-core/src/xp.rs`：XP/2003 x64 的 GPT+UEFI 驱动注入、服务注册和引导文件准备；用户启用的 XP 驱动目录缺失、`.sys` 缺失、文件复制失败、必需服务或 CriticalDeviceDatabase 写入失败必须失败关闭，不能只记录日志后继续安装。
- `lr-core/src/xp_i386.rs`：XP/2003 Legacy/MBR 文本模式硬盘安装、NT5 文件准备、应答文件和活动分区设置；活动位写入前必须按目标卷的磁盘号和精确偏移捕获规范磁盘布局，并通过 checked 共享边界完成；文件树复制复用 `CopyFileExW` 共享边界，覆盖根引导文件前通过文件属性 API 只清除只读/系统/隐藏位；提供不写盘的 I386/AMD64 目录名及关键文件完整性预检，供正常端暂存前和 PE 格式化前共同失败关闭。
- `lr-core/src/xp_textmode_drv.rs`：解析存储驱动 INF，并把 AHCI/NVMe 驱动集成到 XP 文本安装阶段。
- `lr-core/examples/build_pca2023_resource_pack.rs`：验证固定白名单中的微软 BootEx 资源签名、架构和字体集合，并通过内置 wimlib 从普通目录生成和复验离线资源 WIM；不挂载或维护系统镜像。

### 正常系统端入口与核心

- `正常系统端/build.rs`：生成 Windows 资源、程序清单、包含 16 至 256px 多 DPI 帧的 PNG 派生 ICO 和可复现的构建日期/版本信息；日期版本随端内源码、清单和 `SOURCE_DATE_EPOCH` 重新求值，避免新二进制复用旧关于页版本；并把固定 Windows 11 `Aero.msstyles` 提取的复选框明暗及 96/120/144/192 DPI 状态 PNG 转换为编译期 BGRA 图元；单选框不再读取已预合成且丢失透明覆盖率的捕获 PNG；release 禁止测试权限 feature，并在正常端 release 未使用 `x86_64-win7-windows-msvc` 时于链接前给出统一构建脚本提示并拒绝产生误标为 Win7 兼容的 EXE。
- `正常系统端/src/build_info.rs`：正常端编译期构建身份和用户可见构建标签的唯一入口；`DEV` 仅在显式启用 `dev-build` feature 时为 `true`，测试包的主窗口标题、关于页标题、产品名、版本和说明必须由此统一显示测试身份，正式构建不得出现测试标识。
- `正常系统端/src/main.rs`：桌面端进程入口、权限与依赖检查、窗口显示前并行完成系统/硬件摘要及正式版分区只读预加载、安全 CLI 分派和原生 Win32 窗口启动；启动日志必须只记录 `BUILD_VERSION` 的 `vYYYY.MM.DD` 构建日期版本，并记录源系统、固件、Secure Boot、运行平台、物理磁盘与分区数，使反馈者无需手工抄写环境；PCA 固件兼容性只读探测必须与启动预加载同时开始，通过一次性接收器在 HWND 创建后接入消息循环，既不得阻塞首窗显示也不得因窗口稍后创建而重复探测；所有危险公开 CLI 必须在管理员边界之后分派，历史 `/PEINSTALL`、`--pe-install`、`/PEBACKUP`、`--pe-backup` 正常端兼容参数必须在配置加载、管理员请求和 GUI 初始化之前固定拒绝，源码不得保留另一套未受稳定身份保护的格式化、镜像释放、备份捕获、NT5 猜测或引导写实现；固定且无参数的 `--restore-windows-update` 维护入口只能由已经提权的管理员控制台单独调用，必须通过共享 WinAPI 确定当前 Windows 盘符并严格验证精确盘根、Windows 目录和 SYSTEM/SOFTWARE hive 后，才从该系统盘 `ProgramData` manifest 执行在线 compare-and-swap 恢复；该入口不得弹框或自动提权，部分恢复必须写结构化日志并以非零状态退出，`non-elevated-tests` 必须拒绝执行。网络目录由窗口异步加载并回传错误，正常端不再声明或链接 egui 模块，`non-elevated-tests` 下使用隔离互斥锁并禁止危险 CLI 入口、联网和安装目标分区预加载；该测试 feature 专用的 `--ui-preview`/`LETRECOVERY_UI_SKIP_PRELOAD` 入口只跳过单实例和供应商 WMI/SetupAPI 只读预加载，用真实配置、原生控件和消息循环提供确定性视觉回归；同 feature 的 `--ui-progress-preview` 必须复用正式进度页切换、布局和绘制路径，固定展示非 0/100 的运行中安装状态，不创建控制器或 worker；`--ui-pe-maintenance-preview` 必须直接打开工具箱及正式 PE 维护准备对话框，只运行动画计时器，不进入 PE/BCD/BitLocker/重启控制器；release 中不得存在这些入口。
- `正常系统端/src/win7_import_compat.rs`：正常端 x64 Windows 7-11 通用产物的进程加载兼容边界；在 EXE 内提供 SDK 依赖所引用的 `__imp_CoTaskMemFree` 导入槽并动态转发到 Windows 7-11 均存在的 `ole32.dll!CoTaskMemFree`，防止现代 SDK 把单个调用重定向为 Windows 7 不存在的 `combase.dll` 硬依赖；不得扩展为伪造系统 DLL、吞掉释放或影响独立的 Windows 10/11 PE 构建。
- `正常系统端/src/native_ui/mod.rs`：正常端原生 Win32 UI 模块边界和窗口运行入口。
- `正常系统端/src/native_ui/redraw.rs`：主窗口和工具对话框共用的合成重绘事务；页面切换只暂停当前可见顶层根窗口，冻结期沿用既有子控件显隐和布局逻辑，恢复时用带 `RDW_ERASE|RDW_ALLCHILDREN` 的异步 `RedrawWindow` 排队一个完整客户帧，避免逐子 HWND 的 `WM_SETREDRAW` 产生空重定向表面，也避免点击处理同步等待整树绘制；连续 `WM_SIZE` 的纯几何布局同样只在全部 `MoveWindow(..., repaint=false)` 完成后排队一个不带 `RDW_UPDATENOW`/`RDW_FRAME` 的完整客户区子树帧，使 USER32 合并尺寸突发；主题、语言和首次显示等必须立即稳定的事务仍使用同步完整树发布。主窗口禁止使用 `WS_EX_COMPOSITED`。
- `正常系统端/src/native_ui/controls.rs`：原生子控件创建、UTF-16 字符串、库存下拉框空选择哨兵与无偏移索引校验、按 Inno DFM 的 23px 高/75px 最小宽基线缩放的 DPI 尺寸，以及按钮、分隔线和进度条的明暗/悬停/按下/禁用/单焦点边框状态绘制；ComboBox 保持 Common Controls 6.0 的库存字符串、键盘、无障碍和原生弹层语义，不得全局强加 `CBS_OWNERDRAWFIXED` 或在每个弹出项重绘时同步读取文本和分配缓冲；所有自绘按钮在共用子类中用 `TrackMouseEvent(TME_LEAVE)` 跟踪悬停，只局部重绘自身并复用 normal/hot/pressed/disabled 调色板，跟踪失败、取消模式、隐藏复用和禁用时立即清除热态，不因焦点增加双边框；按钮、长任务进度条及闭合字段/列表外框使用限域抗锯齿绘制，圆弧厚度必须随 DPI 与直边同步，重复重绘使用绝对调色板颜色以避免颗粒、断线和逐帧变暗；单行编辑框从创建期保留 `WS_CHILD|WS_VISIBLE|WS_TABSTOP|ES_AUTOHSCROLL` 和 `WS_EX_NOPARENTNOTIFY`，移除宿主版本会复活的 `WS_BORDER`/`WS_EX_CLIENTEDGE`，文本、光标、选择、IME 与无障碍仍完全由原生 Edit 负责；布局子类按当前字体 `tmHeight` 和 DPI 把真实 Edit 垂直居中到 23px 字段行，由同一父窗口下无 ID、无通知的同级 STATIC 保持完整字段几何，禁止父级包装 HWND 改变通知路由或造成嵌套 Edit 宽度坍塌；ListView 的固定圆角外框同样由无 ID、无通知的同父 STATIC 承载，使用空心 HRGN 保留真实列表的表体与滚动条命中区，并由布局子类同步可见性、位置、尺寸、启用、DPI 和相对 Z 序，真实报告缩入外框但不改变通知和滚动语义；外层闭合字段视觉与 ComboBox 共享确定性 Win11 圆角框，闭合 ComboBox 箭头必须使用子像素覆盖率 BGRA 图元；直接创建的安装镜像和高级选项字段也必须接入，不得用 `EM_SETRECT`、`WM_NCCALCSIZE` 或自绘文字伪造居中；进度条保持深色轨道、统一 Inno 绿色与 5px 内克制圆角，并在同一 BGRA 像素表面按覆盖率合成背景、轨道和填充；最外抗锯齿层必须与轨道同色以匹配 PE 的完整 10px 胶囊，不得另画会吃掉上下可见厚度的独立描边，填充圆角外部不得以矩形轨道色回贴并留下黑块。
- 正常端工具箱的 PE 维护准备对话框必须直接复用 PE 端 Cloud-MGR `ProgressRing` 的 16×16、半径 7、1.5px 圆头线宽、两秒线性关键帧和解析覆盖率绘制；浅色前景固定 `#005FB8`，深色前景固定 PE 同款 `#4CC2FF`，不得误用正常端深色按钮的低对比 `accent_fill`，不得回退为 Unicode 半圆字符。弹窗使用 500×220 紧凑壳、48px 内容预算、16px 圈与 10px 状态间距，动画使用独立约 16ms UI 定时器且不改变 PE/BCD/BitLocker/重启控制器语义。
- 正常端客户区只使用普通不透明明暗主题，不得重新引入全客户区 DWM 背景或与之绑定的透明控件分支。原生 Edit 继续独占文本缓冲、光标、选择、IME、键盘命中和无障碍语义；普通短 `SS_LEFT` 标签保持库存顶对齐，只有明确带 `SS_CENTERIMAGE` 的单行标签才垂直居中。
- `正常系统端/src/native_ui/layout.rs`：正常端原生窗口共享的纯几何布局层，统一 100% 至 200% DPI 的外边距、紧凑/普通/分节间距和控件基线；标签、编辑框和闭合下拉框在奇数剩余像素时统一向下取整到视觉中心，避免 23px 字段与 24px 行之间出现一像素上偏；用当前窗口实际 Microsoft YaHei 字体测量中英文单行、换行文字与按钮宽度，决定标签/字段同行或堆叠、库存列表 3 至 8 行的自然高度以及隐藏控件零占位，不创建控件、不读取磁盘或产生业务副作用。
- `正常系统端/src/native_ui/dialog.rs`：Inno 风格原生模态/非模态对话框壳、主动按当前明暗主题绘制且把内容容器收到的命令、通知、颜色及 `WM_HSCROLL` 原样转发给所有者、ComboBox `WM_DRAWITEM` 和 ListView 表头的内容容器、按 DFM 约 46px 基线收紧的命令栏、工具弹窗同级命令按钮保持 10px 逻辑间距、以 75px 为下限并按实际微软雅黑 UI 字体/DPI 测量译文扩宽的命令按钮、仅响应 `BN_CLICKED` 且完整抑制擦除白闪的稳定自绘按钮、已有非模态窗口激活、按显示器工作区约束的高 DPI 尺寸、长说明实测换行与内容自然高度收紧、工作区钳制和消息转发；非模态命令默认仍关闭窗口，但允许具体工具把刷新/重新分析设为原位命令，并在主请求通过自身校验后显式隐藏，标题栏取消始终关闭；首次显示前统一为遗漏字体的后创建子控件补充微软雅黑并预先应用 Edit/ComboBox/ListBox/ListView/按钮明暗主题，主题变化通过共享整树重绘事务一次发布；顶层对话框保持普通不透明命中测试且不得使用分层 alpha 暂存，工具弹窗专用窗口类的 `hIcon/hIconSm` 固定为空且“关闭”操作只保留标题栏 X（取消操作仍保留命令按钮），主窗口独立类继续使用发布图标；异步结果不得清除尚未消费的关闭/命令结果或复活已隐藏窗口，不承载业务副作用。
- `正常系统端/src/native_ui/driver_transfer_dialog.rs`：驱动备份/恢复的专用 Inno 原生对话框，恢复导出/导入单选模式、按桌面当前系统/PE 首个离线系统语义选择的 Windows 分区、条件路径标签、目录浏览意图和本地输入校验，并为单选、标签、字段和状态统一应用微软雅黑 UI 与当前明暗主题；按可见内容紧凑布局，空状态不占位，100% 至 200% DPI 保持行距和命令区稳定；只产生强类型意图，不打开文件对话框或运行驱动命令。
- `正常系统端/src/native_ui/tool_dialogs.rs`：网络信息、软件清单、GHO 密码读取、镜像完整性和文件 SHA-256 的 Inno 原生对话框控件，按字段/滚动报告角色应用 DPI 与明暗主题，并完成紧凑状态展示与无副作用意图映射；GHO、镜像校验和文件哈希的浏览按钮是不会关闭或隐藏窗口的专用内容命令，必须复用普通 Inno 次要按钮的统一自绘、悬停、按下、禁用和焦点状态，并在窗口最终显示布局后仍与对应路径 Edit 同行且紧邻右侧，命令栏只保留实际执行操作；软件清单恢复迁移前的启动即加载和“名称/版本/发布者”三列 ListView，保存结构化记录并生成旧格式 TXT，不显示无业务接线的筛选编辑框，TXT 导出按钮按微软雅黑译文与 DPI 测量最小宽度；GHO 仅在有效且确有可解码密码时产生独立复制意图，镜像校验运行态提供显式取消意图，镜像与哈希结果保留大小、镜像数、分卷数和匹配状态等结构化字段；所有工具窗口标题只使用工具名称，不追加产品名。
- `正常系统端/src/native_ui/tool_dialogs_mutating.rs`：其余工具箱入口及安装/备份 BitLocker 解锁门禁的 Inno 原生输入、只包含真实库存且空选择不插入提示伪项的下拉/多选列表、按控件角色覆盖明暗主题、枚举库存复核、风险提示和二次确认对话框；门禁按可见字段、库存行数和状态文本自然收紧，隐藏凭据行不保留空槽；危险操作只产生经确认的意图，不在窗口过程中执行。
- `正常系统端/src/native_ui/tools/mod.rs`：需要专属状态与安全边界的原生工具对话框模块声明。
- `正常系统端/src/native_ui/tools/appx.rs`：移除预装 UWP/APPX 的专属 Inno 原生目标下拉和应用复选列表，桌面默认当前系统、PE 默认首个离线系统并异步加载对应库存，目标下拉只包含真实库存且空选择保持为空，保留全选、全不选、反选、计数与刷新；按钮只接受 `BN_CLICKED`、目标下拉只接受 `CBN_SELCHANGE`，程序批量写入 ListView 复选状态时必须屏蔽同步 `LVN_ITEMCHANGED`，避免半完成状态反写业务选择；只接受库存值并产生加载、确认或关闭意图，不提供目录/部署模式输入或执行删除。
- `正常系统端/src/native_ui/tools/boot_repair.rs`：一键修复引导的专属 Inno 原生 Windows 分区下拉、版本/架构详情、刷新和状态展示；下拉只包含真实 Windows 库存并直接映射索引，桌面优先当前系统分区、PE 优先首个离线系统，不暴露 Auto/UEFI/Legacy 输入，只产生刷新、确认或关闭意图，不写引导。
- `正常系统端/src/native_ui/tools/batch_format.rs`：批量格式化的专属 Inno 原生安全卷复选表、文件系统/容量/可用空间展示及全选、全不选、反选、刷新状态；严格生成固定 NTFS 与“新加卷”卷标的既有强类型确认意图，不枚举或格式化卷。
- `正常系统端/src/native_ui/tools/bitlocker_manage.rs`：BitLocker 管理的专属 Inno 原生加密卷状态表和条件操作区；按锁定、解锁、加密中、解密中状态流式排列实际可见的密码、恢复密钥、警告与操作控件，隐藏条件行不占位；恢复密码/恢复密钥解锁、读取与导出恢复密钥、挂起/恢复保护及解密选择，敏感值调试输出脱敏，只产生刷新、强类型操作、导出或关闭意图，不读取保护器、不写文件、不修改卷。
- `正常系统端/src/native_ui/tools/expand_c.rs`：无损扩大 C 盘的专属 Inno 原生分析结果、目标容量输入、明暗一致的双缓冲抗锯齿自绘滑块和确认意图；生产目标上限固定为受认证目标卷后方已有连续未分配空间形成的 `no_move_max_mb`，分析只发现需要移动空间时禁用提交，绝不向用户提供超出纯扩展边界的滑块范围或“稍后进 PE 搬移”的假选项；滑块使用安全范围内的绝对十分之一 GB 值，拖动时单调换算并同步目标容量文本，初始化或异常的零位置通知必须钳制到安全最小值；“重新分析”是保持同一对话框可见的原位命令，只有经过校验的纯扩展请求才显式隐藏窗口进入确认；按分析结果紧凑流式布局，无有效分析或状态时不保留空槽，状态切换和自适应改高后按对话框首帧相同路径整窗重绘，避免保留 USER32 的白色静态区及命令按钮底面；只展示只读分析并产生请求，不枚举磁盘、不写扩容配置、不安装 PE 引导或重启。
- `正常系统端/src/native_ui/tools/hardware_inspector.rs`：详细硬件检测的专属非模态 Inno 原生窗口，使用左侧六类导航和双缓冲三列 ListView 展示后台只读快照，刷新通过带代次的窗口消息接收结果并保持界面响应；状态或错误文字变化时必须按当前字体、可用宽度和 DPI 重新测量换行高度并缩放列表，禁止用固定单行高度裁掉后续文字；语言切换同步更新对话框壳、导航、列名、状态和当前内容，只产生刷新或关闭意图，不直接读取硬件或执行系统修改。
- `正常系统端/src/native_ui/tools/time_sync.rs`：系统时间校准的无输入 Inno 原生确认对话框，按微软雅黑 UI 与 DPI 为标题、空行及五个固定可信 NTP 回退地址保留完整高度，不得裁掉最后一项或说明文字；只产生确认或关闭意图，不联网或设置系统时间。
- `正常系统端/src/native_ui/tools/network_reset.rs`：重置网络的无输入 Inno 原生确认对话框，逐项披露既有后端的 Winsock、TCP/IP、DNS 缓存和 Windows 防火墙重置命令及真实风险，只产生确认或关闭意图，不执行命令。
- `正常系统端/src/native_ui/tools/nvidia_removal.rs`：英伟达显卡驱动卸载的专属 Inno 原生目标下拉、启动时仅展示检测到的 NVIDIA 显卡型号和实际全量清理范围；零/单显卡状态使用紧凑只读文字，多显卡才显示按实际数量自适应高度的列表；目标标签与下拉紧凑对齐，下拉只包含真实库存而不插入“请选择”伪选项，桌面默认当前系统、PE 默认首个离线系统且只接受对应 Windows 库存；不提供会被后端忽略的硬件 ID/CPU/内存明细、设备多选、软件卸载或自由输入，只产生加载、刷新、确认或关闭意图。
- `正常系统端/src/native_ui/tools/password_reset.rs`：密码重置的专属 Inno 原生目标下拉与按真实账户数保持 3 至 7 行的紧凑单选账户列表，目标下拉只包含真实库存并直接映射索引，桌面默认当前系统、PE 默认首个离线 Windows，操作固定为清空一个账户密码并启用该账户；异步库存带目标防串回校验，只产生加载、刷新、确认或关闭意图，不提供批量语义或执行修改。
- `正常系统端/src/native_ui/tools/partition_copy.rs`：分区对拷的专属 Inno 原生源/目标库存下拉与明细表，打开时立即由宿主异步预载库存并保留不会关闭窗口的手动刷新重试，恢复容量、已用、卷标、系统状态、断点续传状态、明暗一致的扁平进度和 100,000 字节受限日志；对话框被显示器工作区钳制后必须从真实内容客户区重新分配两张明细表和日志的弹性高度，底部刷新/开始命令始终保留完整可见；下拉和明细表只包含真实库存并双向排除另一侧已选分区，过滤后的控件索引必须重新映射到原库存盘符，默认以 USER32 空选择表示未选分区且不引入索引偏移；只产生刷新、既有强类型 `PartitionCopyRequest` 确认或关闭意图，不枚举磁盘、不复制文件。
- `正常系统端/src/native_ui/tools/quick_partition.rs`：一键分区的专属 Inno 原生物理磁盘下拉、BIOS(MBR)/UEFI(GPT) 单选、已有/新规划分区明细和紧凑低圆角分区图；分区图必须是一个连续长方形，分区按容量紧贴切块、只以竖线分隔并双行显示卷标/类型与大小，不得恢复分离卡片或过高留白。每个非特殊、非当前运行 Windows 的 NTFS 卷，只要自身可缩小、右侧有连续未分配空间或右侧紧邻另一可移动 NTFS 数据卷，右边界就显示小型竖向圆角拖柄：右侧是未分配空间时仍在该卷与未分配空间之间调整；右侧是另一数据卷时，向任一方向拖动都必须直接预览“左卷与右卷的总容量守恒转移”，不得在两卷之间插入虚假的未分配块。拖柄抗锯齿圆角的外侧像素必须分别与分割线左右真实分区底色做不透明合成，禁止把透明角、窗口底色或黑块覆盖到分区条上；柄内竖线必须按柄的实际像素宽度严格居中。拖动提交消息必须在子类过程返回后异步交给父窗口处理，禁止同步重入并在仍持有分区图独占状态时重绘或改写同一模型。连续拖动必须以当前暂存布局为基线：右侧卷先吸收全部或部分未分配空间后再向左侧卷转移时，必须保留前置扩容并按扩大后的两卷总容量重算，拖回基线则只撤销成对转移而保留其他暂存修改，禁止删除前一步或从原始库存重建导致未分配空间回弹。成对转移应用时必须交给强类型 WinPE 搬移流程；禁止在当前 Windows 会话中把缩小某卷伪装成另一卷已得到连续空间。拖动帧必须在内存 DC 完整绘制后一次发布、拦截库存背景擦除并合并未变化的鼠标位置，禁止直接逐图元刷到屏幕造成闪烁；拖动只更新暂存方案，提交后不得删除和重建内容未变化的明细 ListView。异步库存替换后必须重新按真实行数计算 ListView 和窗口高度，少量行完整展开，超过上限后固定高度滚动；底部刷新、应用和清盘主操作之间始终保留标准控件间距。系统卷判定必须来自当前 Windows 卷身份，禁止写死 `C:`。右键菜单必须使用紧凑确定性 owner-draw 适配明暗主题，按真实文字测量宽度，并为 USER32 `#32768` 弹层设置 DWM 小圆角和 Win10/WinPE 的圆角窗口区域回退；同时提供删除、可配置文件系统/卷标/分配单元/快速格式化、盘符分配/移除、MBR 活动标记、未分配空间建卷及 RAW 盘按当前固件模式初始化；已有 ESP 或无足够未分配空间时隐藏不适用的创建按钮。“应用修改”只提交手工编辑；会清空整盘的主操作必须单独标为“清空整盘并分区”，不得与普通应用显示为同义按钮。关闭存在未应用修改的窗口必须提供应用、放弃和取消选择。下拉只包含真实磁盘并保持默认空选择，库存由宿主提供，模块不枚举磁盘或执行存储写入；所有危险意图必须携带磁盘/分区指纹并由宿主二次确认。
- `正常系统端/src/native_ui/tools/storage_driver.rs`：导入随包存储控制器驱动的专属紧凑 Inno 原生 Windows 下拉选择、条件状态和确认意图，下拉只包含真实 Windows 库存并直接映射索引，按桌面当前系统分区/PE 首个离线系统的库存顺序选择默认目标；空状态不扩张内容区，不显示或接受驱动目录、递归选项，不读取文件或调用 DISM。
- `正常系统端/src/native_ui/theme.rs`：固定从 Inno 6.7 Modern Windows 11 明暗参考截图逐像素核对的窗口、普通/引导按钮和边框色，负责系统应用主题检测、GDI 画刷及主题资源生命周期；客户区和全部组件始终使用确定性不透明明暗表面，不再提供全窗口 Mica、透明 DWM carrier 或相关合成文字分支。单行 Edit 保留原生文本、光标、选择、IME 和无障碍语义，以字体度量后的紧凑客户区配合同级 STATIC 绘制完整 23px 字段底色和确定性圆角外框；闭合 ComboBox 与独立 ListBox 在原生绘制完成后最后覆盖唯一确定性外框，ListView 的唯一外框则绘制在不参与 comctl32 滚动、保持在真实列表上方且命中透明的空心同级 STATIC 上，库存弹层、选择、滚动、键盘和无障碍语义保持不变。ListView 未选中行继续由 comctl32 原生绘制，真实选中行只通过 `NM_CUSTOMDRAW` 最小接管背景与文字；非空列表仅覆盖最后一条真实行以下的无项目客户区，不得重画项目行、表头或滚动条。工具进度条、滑块、复选框和单选框使用确定性明暗/DPI 图元，读取 GDI DIB 像素前必须执行 `GdiFlush`，禁止透明角被写成黑色或重复插值产生颗粒。
- 明暗主题复选框必须继续使用固定 Windows 11 主题资源的原始半径、勾号、状态和配色；单选框按当前物理像素尺寸以固定 Windows 11 状态色和至少 8×8 子像素覆盖率生成四向对称的圆环或选中圆点。两类图元的透明及半透明边缘都必须与当前不透明窗口底色正确合成，禁止把 alpha=0 或半透明角直接写成黑色；USER32 仍独占切换、分组、键盘、通知和无障碍语义。
- `正常系统端/src/native_ui/window.rs`：正常端原生窗口类、显式大/小应用图标、消息循环、Per-Monitor V2 DPI、统一 Microsoft YaHei 字体、受当前显示器工作区约束的 DPI 最小跟踪尺寸、国际化导航/页头/命令栏和页面布局；完整布局必须按“进度页→高级页→简易页→当前标准页”的优先级只选择一个实际可见 surface，隐藏页面不得在每次 resize 中重复测量或移动，安装页与各 page 的纯几何 `MoveWindow` 必须统一关闭逐控件 repaint，并由 `redraw.rs` 在整棵可见树稳定后发布一次异步客户帧；导航栏使用 168px 逻辑宽度并在全部 DPI 下同步驱动按钮、内容起点、底色和分隔线，既保证最长英文导航可读，也不得在按钮右侧留下过宽空栏；主窗口统一转发 ComboBox owner-draw，ListView 表头只由 `theme.rs` 的唯一入口绘制；页面切换批量暂停重绘并在显隐和布局稳定后一次性重绘全部子控件，避免暴露中间布局造成闪烁；首次显示前必须用当前配置完成初始页面显隐事务，简易模式不得遗留仍可见但被布局到客户区右边界外的普通安装命令 HWND；安装模式及其双系统容量控件只有标准安装页已经接受非空本地、远程或 Ghost/XP 镜像源后才可显示，清空或拒绝镜像、切换其它页面、打开高级页或进入进度页时必须整组隐藏，不得保留旧 ComboBox 覆盖新页面；安装页对驱动、重启、引导模式及无人值守控件做响应式分行，宽窗口中立即重启按实测译文宽度紧跟驱动下拉保持标准字段间距，取消无人值守后不再保留隐藏的文件选择和提示槽位；已移除的全局高级模式、修改引导命令以及旧 DiskPart 复选框和目录入口均不得创建或显示，但标准安装页必须创建并提供安装高级选项页入口，启动签名标签与下拉框只保留标准字段间距；底部命令栏保留稳定内边距，硬件页隐藏刷新后把保存和复制从右向左紧密排列，复制成功只局部刷新按钮为本地化“已复制”并用非阻塞三秒定时器稳定恢复，不使用缩放、弹性动画或整页重绘；分区动态状态随语言切换重新翻译。窗口统一过滤 Button 的非点击通知，工具对话框只响应一次真实点击且已有窗口优先激活，避免焦点通知导致重复窗口和后台任务；GHO、镜像和哈希浏览内容命令直接打开文件选择器并回填原对话框，不借用会隐藏窗口的命令栏结果；工具对话框关闭结果先于异步完成消息处理，轮询存活覆盖全部专用窗口和后台任务，关闭 A 后打开 B 不得复活 A；分区对拷首次库存仍在后台只读枚举，完成后额外投递窗口唤醒消息立即消费结果，定时轮询仅作为失败安全回退，不要求用户点击刷新；安装分区在桌面默认当前系统，在 WinPE 中使用 `disk.rs` 的 SSD 优先、第一物理 offset、排除 `X:`/ESP/MSR 的只读默认策略，刷新后优先保留仍存在的人工选择；其它离线系统工具仍按规范化 `SystemDrive` 排除与在线系统重复的离线盘符项，物理磁盘和工具页普通数据卷等不可逆目标仍不得自动预选。窗口把原生页面意图路由到安装、备份、异步下载目录及工具控制器、带代次校验的异步只读库存和强类型后台请求边界（包括区分在线/离线目标的 APPX 请求）；在线目录刷新后以同一 PE 快照驱动自动安装、备份和扩容，固定使用首个可用条目且不创建 PE 选择控件；安装或备份在首次点击、BitLocker 解锁和 PE 下载续做后都重新枚举并按完整磁盘/分区几何复核稳定身份、路由及锁定状态，BitLocker 门禁存续期间禁用主窗口，续做前不得复用可变列表索引，简易模式下载完成后重新读取真实镜像卷、架构和目标几何；安装意图通过稳定身份复核后必须把镜像文件名/格式/大小、目标卷名称/build/架构、目标和非目标 BitLocker 状态、磁盘结构及实际使用的 PE 名称写入诊断日志；安装页同时保留源/目标无人值守探测、多 PE 目录兼容、与引导修复和实际 UEFI 条件一致的 PCA2011/PCA2023 检测与选择，自动启动签名项必须显示当前实际推荐代次，以代次及目标磁盘/分区身份隔离异步同盘 ESP 只读检测结果，目标 ESP/EFI 检测错误始终失败关闭；Windows 7 USB3/NVMe 自动策略不得创建高级选项控件，USB3 在准确识别 6.1 镜像时自动启用，NVMe 仅在目标物理盘 WinAPI 总线查询明确为 NVMe 且镜像为 x64 时自动启用，目标镜像或磁盘身份变化后按新快照重算且查询失败不得猜测；UefiSeven 仍保持隐藏，存储驱动工具必须按当前 SetupAPI PCI ID 选出唯一 Intel VMD 子目录且禁止递归；`non-elevated-tests` 的确定性安装页和一键分区视觉夹具只提供只读虚拟库存，不得进入 release 行为或调用物理磁盘 API；执行失败时仅展示本地化摘要而把底层诊断留在日志；安装、备份、下载、扩容或镜像校验仍在运行时拦截窗口直接关闭，先请求取消并等待安全停止点；工具页负责把专属一键分区与 BitLocker 对话框接入 fresh 库存、二次确认和后台安全边界，并把镜像校验的实时进度及取消标志贯通到既有校验器；一键分区的连续直接扩容与离线成对转移必须在后台按序执行，直接扩容后重新枚举并只把 fresh 指纹和供体最终大小交给 PE，任一步失败都停止后续交接且不得伪装成原子成功；Wi-Fi 迁移只在已连接网络时抓取本次运行所需的瞬态配置；无损扩大 C 盘使用专属分析、二次确认、PE 下载及交接状态机。
  底部状态文字必须按实际换行后的完整文字块高度与右侧命令按钮垂直居中；单行和多行不得分别使用固定偏移。自动化导出成功提示必须把说明、位置和注意事项放在内容区，把“确定”保留在独立底部命令栏；超出可用宽度的位置路径使用单行尾部省略号，不得覆盖按钮或相邻文字。
  正常端在 WinPE 中运行时必须把简易模式的有效状态固定为关闭，并在关于页显示为未勾选且禁用；该有效状态必须同时用于首次显隐、完整布局、快速命令栏布局、条件控件、异步目录更新和命令路由，不能修改或覆盖用户持久化配置。启动 PCA 固件探测在安装选择尚不相关时不得覆盖首屏“启动模式/TPM/安全启动”摘要；镜像和目标使 PCA 校验相关后，未完成的固件探测或目标 ESP 探测才显示对应状态并持续阻断安装，固件既有结果必须复用而不得重新探测；已完成的目标 ESP 结果按目标磁盘/分区身份缓存，重新选择镜像时复用，目标身份变化时才重新探测。安装页启动后在后台枚举当前存在盘符，仅当全局恰有一个 `sources\install.esd` 或 `sources\install.wim` 且镜像输入仍为空、异步代次未变化时静默接受；没有结果或存在多个结果时不得填入，也不得显示“自动填入”类提示。可编辑镜像路径一旦发生用户修改，必须立即撤销旧异步代次、旧有效路径、卷列表、挂载 ISO 和安装可用状态；离开输入框后通过既有异步镜像检查重新接受新路径，禁止显示路径与执行源分离。安装页和备份页不得创建 PE 选择控件；远端非空 PE 目录刷新后替换当前快照，自动安装、备份和扩容始终使用当前快照首项，当前快照为空时失败关闭，不保留或恢复用户 PE 选择状态。
  安装页布局取决于异步镜像库存：每次从任意其他页面进入安装页，都必须在冻结提交期间按已接受库存执行完整布局，使条件镜像卷行始终位于分区标题之前；不得只恢复 HWND 可见性并假定启动布局仍然有效。`non-elevated-tests` 只允许用显式 `LETRECOVERY_UI_TEST_IMAGE_VOLUME` 注入确定性镜像卷视觉夹具，release 不得包含该入口。只重装所选分区或全盘重装时，安装模式与 PCA 启动签名选择器必须位于同一行；创建双系统时因容量输入占用同行，PCA 才移至下一行。最小窗口、多语言和 DPI 布局必须弹性分配两个下拉框且保持右边界内，固定单行高度的 STATIC 必须禁止换行并使用省略号，严禁把第二行裁成残缺笔画或杂线。
  双系统的新系统容量输入不得使用与执行校验分离的固定 20/32/80 GB 默认值。镜像首次读取、镜像卷切换，以及任意远程镜像完整下载后的本地重新识别时，必须从共享 `TOTALBYTES - HARDLINKBYTES + 2 GiB` 预算生成向上取整到整数 GiB 的可填写最低值；尚由程序管理或小于新最低值的输入自动刷新，用户主动填写且仍满足最低值的更大容量必须保留。若下载后的本地实际容量与下载前远端元数据预算不同，全盘或双系统必须返回安装页刷新计划并要求重新确认，禁止自动执行旧确认。确认框、正常端 Shrink/建卷事务和 PE 交接只能消费同一当前值；建卷请求必须使用该计划中的明确 Windows 和数据暂存容量，不得用 `size=0` 静默占满更大的 provider extent，也不得由另一层再叠加固定容量下限。
  主窗口首次显示不得使用临时 `WS_EX_LAYERED`、`WS_EX_TRANSPARENT`、全局 alpha 或颜色键首帧屏障；所有受支持系统都必须在隐藏状态准备完整子控件树，以普通不透明窗口显示并同步完成非客户区、客户区与全部子控件绘制，显示前后都要验证扩展样式仍拥有输入，异常时销毁窗口并失败关闭。普通页面导航必须只冻结可见顶层根窗口，在既有子控件显隐和布局稳定后恢复根窗口，并以 `RDW_ERASE|RDW_ALLCHILDREN` 异步排队一个完整客户帧，禁止逐子 HWND 发送 `WM_SETREDRAW` 或改写其 `WS_VISIBLE`；主题、语言和首次显示等必须立即稳定的整树事务仍用一次同步 `RedrawWindow` 发布。
- 安装页主命令状态补充约束：切回安装页时必须在控件显隐稳定后，从真实镜像、目标分区、PE、PCA 和无人值守状态完整重算“开始安装”，不得沿用页面切换时的临时禁用态或直接伪造启用；`CB_SETCURSEL` 和 `BM_SETCHECK` 的程序化默认值不会产生用户通知，因此每次重算前必须从当前可见控件同步安装偏好，并调用与真实点击完全相同的 `install_intent()` 权威校验，禁止维护另一套近似启用条件。标准安装页必须创建安装高级选项页并由“高级选项...”进入、由“保存并返回”原子回写当前会话偏好；不得把该入口重新解释为已移除的全局高级模式，硬件页仍可把共享命令按钮作为“保存”使用。
- 主窗口分区库存实时性约束：使用顶层窗口收到的 `WM_DEVICECHANGE` 到达、移除、配置和设备节点变化通知触发后台只读枚举，以短时一次性定时器合并同一设备变化产生的通知突发，禁止常驻秒级轮询；消息线程只负责调度和应用完成结果，不得同步枚举磁盘。替换库存时按磁盘号、分区号优先并以盘符和容量安全回退来恢复安装及备份选择，目标消失时清空选择，且必须屏蔽批量替换期间的半完成 `LVN_ITEMCHANGED`。
- 安装分区选择通知必须检查 `NMLISTVIEW.uChanged` 的 `LVIF_STATE` 及新旧 `LVIS_SELECTED` 位，只把同一次移动产生的旧行取消与新行选中合并成一条队列消息；依赖状态和布局只能在 ListView 同步通知返回后的一轮根窗口重绘事务中更新，文本、焦点及未改变选中位的通知不得重绘页面。
- ListView 表头外框补充约束：表头只能绘制自身背景、文字和列分隔线，不得在 Header DC 或父 ListView DC 上补画圆角外框；固定外框只由同父空心 STATIC 覆盖层负责。Header 与 ListView 的坐标系和水平滚动裁剪范围不同，混用会在最后可见列表头旁产生第二个圆角收尾。
- Win10 控件主题补充约束：库存 `CBS_DROPDOWNLIST` 的弹出 ListBox、键盘和无障碍语义保持原生，闭合选择字段由共用主题层覆盖为固定 Inno 明暗底色，浅色不得随 Win10 系统强调色漂移而变灰；`BS_AUTOCHECKBOX` 保留 USER32 自动切换、焦点和通知语义，仅由共用子类确定性绘制 Inno 明暗字形与微软雅黑文本，Win10 深色不得出现浅色方框或黑色标题。
- 安装页镜像卷条件布局补充约束：未识别到可安装卷时卷选择行必须零占位；识别成功后用约 120ms、三帧确定性线性布局过渡为后续分区和选项让出空间。过渡必须使用 UI Timer 和逐帧批量暂停重绘，不改变 ComboBox 选择或焦点，不使用弹性动画，窗口销毁或页面切换时必须清理定时器。
- 闭合 ComboBox 裁剪补充约束：为保留弹出列表高度而创建的 HWND 在设置矩形闭合字段区域后，必须立即失效并擦除父窗口刚暴露的尾部矩形；`SetWindowRgn` 只重绘被裁剪的子窗口，不会清掉此前位于新区域之外的 UxTheme 像素，遗漏父区重绘会在深色冷启动时留下字段下横线。
- 共享原生控件回归约束：Inno 自动单选标记按约 13px 的 96-DPI 逻辑基线缩放，保留 USER32 分组、键盘、通知和无障碍语义；所有自绘导航及命令按钮稳定使用原生箭头光标，跨越抗锯齿圆角不得在手形与箭头之间闪烁；固定只读多行报告保留原生文本、选择和 `WS_VSCROLL`，但移除 Edit 方形非客户区并由列表式明暗圆角外框收口；ListView、ListBox 和只读滚动报告的非客户区滚动条继续由 USER32/comctl32 原生绘制，悬停动画、滚轮、拖动和滚动消息必须先完成默认处理，再在最外层最后覆盖确定性明暗圆角边框，禁止接管滚动条或让右侧圆角退化为方角、黑块和颗粒；无损扩大 C 盘的普通滑块 tick 只更新字段、滑块和命令状态，仅在跨越移动分区警告阈值时重新布局；密码重置账户选择不得重新填充库存或重新测量窗口，一键分区“调整大小”按钮的启禁变化只局部无擦除重绘并保持四边同帧闭合。
- 所有 report ListView 必须启用 `LVS_EX_DOUBLEBUFFER`、`WS_CLIPCHILDREN` 与 `WS_CLIPSIBLINGS`；Header 禁用库存主题动画并由单一确定性入口绘制，Header 绘制不得反向失效整表。固定圆角外框必须由真实 ListView 上方的同父 STATIC 独占，以空心 HRGN 只覆盖外框像素，并返回 `HTTRANSPARENT` 把命中测试交还给真实列表；覆盖层必须持续同步真实列表的可见性、启用、位置、尺寸与 DPI，并在列表窗口位置变化后重新升到其上方。真实 ListView 保持原父级、控件 ID、通知、选择、键盘、滚动和无障碍语义，只缩入 DPI 缩放的外框宽度。`WM_HSCROLL`、`WM_VSCROLL`、滚轮和 `SB_THUMBTRACK` 必须交由 comctl32 默认处理，高频分支不得逐消息失效整表、调用 `UpdateWindow`、同步补画 Header/外框或直接竞争重定向表面，避免原生像素位移把固定外框复制进项目行。UxTheme 滚动条定时器仍只调用默认过程；深色滚动条保留 comctl32 v6 原生语义并使用 `DarkMode_Explorer`，不得使用只支持旧 comctl32 的 FlatSB 颜色覆盖。
- `正常系统端/src/native_ui/window.rs` 状态补充约束：工具页固定说明不得被“时间同步成功”等加载或完成结果覆盖，具体结果只进入对应对话框和日志；PCA 固件/目标 ESP 探测文案只能出现在安装页，切换到工具、硬件等页面时必须清空，返回安装页后必须先恢复当前系统启动模式、TPM 和 Secure Boot 摘要，再按适用的 pending、错误或完成状态覆盖，PCA 不适用时不得留下空白状态栏；主窗口底部状态控件必须使用命令栏除边距外的完整可用高度承载换行，禁止继续固定为单行 20px 而裁掉远程镜像、PE 校验或安装准备错误的后续文字；安装分区标题必须占用表格完整内容宽度，镜像字段与格式化对话框标签列必须按当前字体实测译文宽度，语言切换后不得把换行的第二行裁成残缺笔画；本地及远程镜像的迟到异步结果只能更新内部安装状态，只有标准安装页当前可见且未进入高级页或进度页时才可发布底栏和主按钮文案；PCA pending 必须独立阻断“开始安装”，禁止残留“正在检测”与已启用安装按钮矛盾。分区设备刷新在高级页或进度页可继续更新内部库存，但不得污染被遮挡的安装底栏或主按钮，返回标准安装页时必须重放仍有效的刷新错误。可关闭并重新打开的批量格式化、存储驱动、一键分区和 BitLocker 对话框，其库存、预检及操作完成消息必须携带打开代次，并在涉及具体卷或系统目标时同时核对目标身份；迟到结果不得写入后来重开的窗口。所有由工具页启动的磁盘、卷、离线系统或 BitLocker 写任务必须经过窗口级单活动任务闸门，只有匹配的完成令牌才能释放，禁止存储驱动预检迟到结果或其它对话框并发启动第二个真实写任务。正常安装启动前必须重新查询目标卷的完整物理范围、GPT DiskId/PartitionId 或 MBR 磁盘签名，并在可获得时叠加 `StorageDeviceIdProperty` 哈希，统一复用共享 IOCTL 强身份比较器；MBR 分区号、VDS 枚举摘要和展示容量不得参与匹配。底层卷打开或 IOCTL 查询失败必须保留详细日志并显示“无法读取身份”，只有查询成功且范围或不可变 token 不一致时才可显示“身份已确认变化”。
- `正常系统端/src/native_ui/pages/mod.rs`：正常端原生页面模块声明。
  正常端终态错误交互补充约束：`native_ui/window.rs` 只能在安装、备份、扩容等工作流进入真实 Failed 或工作线程异常断开后，先通过日志 flush barrier 确认正常端日志落盘，再显示共享错误日志弹窗；warning、成功带警告和用户取消不得触发，弹窗中的“打开文件”必须打开资源管理器并选中本次日志文件。
- `正常系统端/src/native_ui/scrollbar_compositor.rs`：安装高级选项页滚动条的 Composition/GDI 双缓冲发布边界；随标准安装页创建并在高级选项可见时进入运行时窗口树，必须保持主题、DPI、滚轮与拖动回退一致。
- `正常系统端/src/native_ui/pages/advanced.rs`：安装高级选项页的原生控件与 `AdvancedOptionsData` 映射；标准安装页启动时创建、默认隐藏，通过全局命令栏进入和保存返回，只修改当前安装会话的高级选项，不恢复已删除的全局高级模式配置或 DiskPart 执行能力；个人文件保留复选框是独立当前会话意图，不得因镜像能力掩码误隐藏或写入长期配置，最终兼容条件由安装控制器统一校验。必须按当前镜像的 major/minor/build 能力掩码隐藏并即时重排不适用的系统设置，切换镜像时不能遗留空行或继续读取隐藏控件。预装应用按钮按当前本地化标题实测宽度紧凑增长并在所属列中水平居中；按钮在零项、自动选择和手工选择三种状态下都必须保留“选择预装应用”的操作含义，再以简短后缀区分数量与来源。服务器目录与目标系统已安装软件交集产生的自动选择必须真实写回当前会话，用户随后手工确认时切换为手工选择状态。
- `正常系统端/src/native_ui/preinstall_dialog.rs`：无人值守安装的预装软件分类/多选对话框；仅在 v4 服务端目录成功加载且无人值守开启时可由高级页打开，分类和软件身份必须来自同一目录快照。初次选择可由 `native_software_detection.rs` 提供“目标系统已安装项与本次目录交集”，但用户一旦应用过选择（包括明确清空）便不得再次自动勾选；`vm_tools=true` 的条目不得出现在该通用列表；VMware Tools 只在环境明确识别为 VMware 时作为高级页独立、默认勾选项显示，探测未知时不得猜测。
- `正常系统端/src/native_ui/pages/backup.rs`：系统备份原生输入页、可刷新且保留稳定选择的分区库存、运行期列头、动态行与控件本地化，表格列按 DPI 最低可读宽度和可用剩余空间响应式分配，避免数值被挤成省略号而右侧闲置；分区标题使用完整内容宽度，字段标签列与增量备份复选框必须按当前字体的译文实测宽度分配，禁止用字符数或固定 20px 单行高度猜测后把法语等换行第二行裁成残缺笔画；默认名称和描述使用同一稳定时间戳按当前语言生成，切换语言时只更新仍等于旧默认值的字段，逐字段保留用户输入；负责格式/路径状态读取、明确校验及到既有 `BackupConfig` 的无副作用转换；PE 路由由窗口和备份控制器从已验证目录首项自动决定，本页不创建 PE 选择控件；保留桌面端默认当前系统分区、PE 中仅有一个 Windows 分区时才默认选择的规则，并公开所选非 Windows 源警告刷新入口，不显示对用户无决策价值的“将通过 PE 环境备份”路由提示。
- `正常系统端/src/native_ui/pages/download.rs`：在线下载原生分类、资源列表和保存位置控件，按分类隐藏空白或重复的类型/大小列并响应式分配剩余列宽；远程目录、TLS、代理及下载准备状态变化时必须使用实际字体和当前可用宽度测量完整换行高度，立即下移资源列表且保持底部路径与命令栏稳定，禁止把可变错误文本塞进固定单行高度后由列表遮住；底部保存位置标签同样按当前字体的完整译文测宽，禁止固定窄列裁掉长语言；支持运行期完整本地化，只产生下载/安装意图。
- `正常系统端/src/native_ui/pages/easy_mode.rs`：简易模式的原生系统/卷选择、可选 Logo、说明和主操作控件，支持明暗主题、DPI 与运行期本地化；组合框仅在目录实际变化时重建内容且选择路由只响应 `CBN_SELCHANGE`，程序化选择后完整重绘闭合字段，避免展开期间重建或暗色箭头区域造成旧项残影；异步快照更新必须服从页面可见状态，不能把提示或 Logo 重新显示到普通安装页；启用状态只在关于设置页修改，系统安装页不显示关闭开关，只产生页面命令。
- `正常系统端/src/native_ui/pages/tools.rs`：工具箱 22 个原生入口及正常端/PE 可用性约束；无损扩大 C 盘、详细硬件检测和默认隐藏的 PE 维护入口使用末尾稳定命令 ID，维护按钮只在正常系统端且配置明确启用时参与布局，只产生工具意图。
- `正常系统端/src/native_ui/pages/info.rs`：把启动时已读取的 `HardwareInfo` 映射为响应式“类别/项目/值”原生 ListView，同组类别只在首行显示且类别列保持紧凑；复制与 UTF-8 TXT 导出使用清晰的分组标题和逐行“项目: 值”格式，兼容中英文标签、续行缩进和完整硬件数据，不重复输出同组类别或把多条内容粘连成一行；网卡速率为零时必须显示本地化“未知”，不得把探测失败伪装为 0 Mbps；`non-elevated-tests` 可用 `LETRECOVERY_UI_QA_LONG_LIST=1` 把库存替换为 120 条确定性只读行，仅供滚动视觉回归，release 不得存在该入口；硬件报告占满页面主体，保存和复制位于稳定底部命令栏；同时承载紧凑且不侵入状态栏的关于页，以及语言、日志、小白模式、默认关闭的自动化导出入口、8/16/32 下载线程档位和 WIM 引擎；自动化开关只控制安装/备份命令栏按钮显隐，不能直接执行任务；正常端不再显示或处理旧全局高级模式开关，保留运行期完整本地化、PE 环境禁用规则、版权致谢、单一许可入口、纯按钮链接与日志目录意图。
  硬件库存更新时，必须在同一窗口消息内完成整批删除和插入，再执行一次包含非客户区与子窗口的同步 `RedrawWindow`；不得对子 ListView 使用会由 `DefWindowProc` 切换 `WS_VISIBLE` 的 `WM_SETREDRAW`，否则 DWM 可能发布暂时缺少真实 Header 子窗的重定向帧。禁止按行、按列或用定时器逐项发布中间帧。
- `正常系统端/src/native_ui/pages/progress.rs`：原生长任务标题、说明、双层进度、运行/取消/成功/失败状态和稳定命令栏；内容区使用紧凑的上部工作流排布，双层进度条与 PE 端保持同一 10 logical-pixel 高度及半高胶囊圆角并随 DPI 缩放。只更新实际变化的文本、整数百分比、命令状态和进度区域，运行与完成保持同一 Inno 绿色；运行期或终态状态文字必须按当前字体、可用命令宽度和 DPI 向上扩展，不能用固定单行高度裁掉错误详情，也不能覆盖进度条或按钮；长任务使用完整客户区且不得透出普通页面的导航分隔线，下载无后续动作时显示本地化返回按钮而不暴露内部枚举，终态只生成重启、打开下载文件或返回等显式后续意图，不自行执行系统操作。
- `正常系统端/src/core/mod.rs`：正常端核心模块声明。
- `正常系统端/src/core/app_config.rs`：`config.json` 用户偏好、语言、日志、外观和默认选项的读取保存；所有写入必须在共享进程锁内通过原子替换完成，普通偏好保存必须重新读取并保留在线目录异步写入的最新 PE 缓存，PE 缓存更新只能替换自身字段并保留最新用户偏好，禁止由窗口持有的旧配置快照造成字段丢失；已移除的正常端全局高级模式字段只为旧配置反序列化兼容，加载时必须固定关闭且保存时不再写回，独立 `automation_export_enabled` 与 `pe_maintenance_entry_enabled` 均默认为 false，后者只控制正常端工具箱的手动 PE 维护入口；旧 DiskPart 开关必须清空，但仍受支持的安装高级选项继续保存普通偏好，密码、当前用户名和 Wi-Fi 瞬态材料不得持久化；当前会话用户名探测必须复用共享本地账户策略，PE 中的 SYSTEM/TrustedInstaller 等服务身份不得成为安装用户名；下载线程字段向后兼容旧配置并在加载与保存入口归一到 8/16/32 三档，旧配置保持原有 16 连接默认值。
  已删除的 `mica_enabled` 只作为 serde 默认的未知旧字段被忽略；加载并再次保存配置后自然移除，不得恢复外观入口或 DWM 全客户区材质逻辑。
- `正常系统端/src/core/automation_export.rs`：把已经由安装/备份页面捕获的当前意图映射为 `schema_version=1` CLI 配置，在 EXE 相邻 `cli` 目录发布受保护 JSON 与使用自身目录相对定位的 CMD；只生成和回读文件，不执行安装、备份、提权或磁盘操作。高级选项映射必须显式覆盖全部当前字段，包括当前运行的个人文件保留选择；关闭的路径型选项不得把陈旧路径带入配置；CMD 文件名、子命令与 EXE 文件名必须拒绝 shell 元字符并保留子进程退出码。
- `正常系统端/src/core/native_software_detection.rs`：只读枚举所选目标 Windows 的机器级卸载注册表项；当前 Windows 使用实时 HKLM，离线目标只通过共享 `OfflineRegistry` 以随机别名加载/卸载目标 SOFTWARE hive。默认选择只能是检测名称与当前 v4 服务端分类目录的交集，匹配仅允许规范化精确名称或数字版本/架构后缀，禁止宽泛 substring、编辑距离或仅凭软件 ID 猜测。
- `正常系统端/src/core/advanced_options.rs`：安装高级选项的序列化兼容、离线系统应用、脚本/文件暂存、Wi-Fi 瞬态配置、内置 Administrator 非持久化密码选项、当前会话个人文件保留意图和历史 XP/Win7 兼容业务边界；个人文件保留选择不得写入长期偏好。历史 `remove_uwp_apps=true` 调用共享精确 PFN 离线 AppX 清理并把普通失败记为结构化 warning，明确保留 Outlook 与 OneDrive，并在 Win11 默认用户首次生成前调用共享 Shell 默认值边界抑制推荐/动态内容；不再生成首次登录 PowerShell 广泛删除脚本或接线 Win32 OneDrive 卸载器。内置 Win7 USB3 必须先验证锁定资源，再按离线目标架构和当前 SetupAPI 硬件 ID 逐包注入，内置 NVMe 必须只按锁定顺序使用 DISM `/Add-Package` 安装两个微软 CAB；离线 hive 必须由清理守卫覆盖所有提前返回，临时卸载后必须在继续执行前完整重载，用户选择的驱动目录缺失、CAB 解包或 DISM 注入失败都必须失败关闭；不再包含 egui 或其他渲染代码，正常端 `native_ui/pages/advanced.rs` 只负责采集当前会话意图，默认兼容选项仍由安装意图按镜像和硬件安全推导。
- `正常系统端/src/core/appx_legacy_impl.rs`：历史在线/离线 APPX 库存、关键包判断和移除实现，仅由受限兼容桥调用；新的执行入口必须优先经过 `native_appx.rs` 的 fresh 库存与关键包复核。
- `正常系统端/src/core/bcdedit.rs`：定位和挂载 ESP、BCD/BCDBoot 修复、活动分区处理及 PCA 引导源选择；自动 PCA/EFI 只读检测先通过 `FindFirstVolumeW`/`FindNextVolumeW` 枚举并以 `IOCTL_VOLUME_GET_VOLUME_DISK_EXTENTS` 复核物理身份后的卷 GUID 根路径读取，Windows 7 隐藏 ESP 无法映射时才允许使用共享 `GetLogicalDrives` 和 `IVdsAdvancedDisk` 分配真正空闲的临时盘符。回退挂载必须在读取前后复核磁盘号与分区偏移，盘符分配与 MBR 活动位写入前都必须捕获当前规范磁盘布局并把精确分区偏移交给 checked 共享边界；已有盘符只借用，所有正常、错误和取消路径都显式关闭守卫，卸载失败必须详细记录、明确提示并阻止安装；UI 必须区分“磁盘上没有 ESP”与“ESP 已存在但无法解析或挂载”。正常端 BCD 查询和 PE 启动项维护使用 `GetSystemDirectoryW` 定位的宿主 `bcdedit.exe`，不得让随包现代副本成为 Win7 装载前提；为离线新系统保留随包 BCDBoot。已经移除 UI 授权入口的 `bin/repair_boot.txt` 和 `bin/diskpart/` 不得出现在发布包或恢复执行。ESP 盘符分配和 MBR 活动标记必须复用共享 WinAPI 存储边界，不得生成或解析 DiskPart 文本；Legacy 修复必须同时确认 Bootsect 成功和活动位写入成功后才能报告完成。
- `正常系统端/src/core/bitlocker.rs`：BitLocker 卷枚举、状态解析、解锁、暂停/恢复保护、解密及恢复密钥处理；PE 维护入口只按当前 Windows API 盘符掩码 best-effort 读取每卷数字 RecoveryPassword，单卷失败静默跳过且不得触发解密。
- `正常系统端/src/core/cabinet.rs`：兼容再导出共享 `lr-core::windows_cabinet`，正常端不得另建 `expand.exe` 或本地化输出解析路径。
- `正常系统端/src/core/cli.rs`：正常系统端公开 CLI 的严格语法、只读 `inspect disks|image|pe-cache` fresh 库存、公开 `update restore` 与 `tool` 路由、稳定退出码、stdout 最终 JSON、stderr 脱敏 JSON Lines 进度和父控制台/继承标准句柄适配；`inspect image` 必须与 GUI/安装计划复用 `dism::is_installable_image`，禁止用 wimlib XML 库存中恒为 false 的旧 `verified_installable` 字段把真实 WIM 索引过滤为空；install 生成器的全盘模式必须同时显式接收 `repartition_all_disks` 和当前会话已确认磁盘号，默认模式不得携带清盘选择；install/backup plan 必须输出规划器实际采用的 `effective_config`、fresh 身份绑定和 warnings，不能只回显输入；只有显式 `--interactive` 才允许读取向导输入，交互提示也必须是 stderr JSON Lines 且 EOF 明确失败；真正执行的 `install|backup run` 和工具箱修改命令必须已提权并带 `--yes`，BitLocker 恢复密钥读取也必须已提权，`update restore` 同样必须由已提权控制台显式调用，`run --dry-run` 固定等价只读 plan 且不得触发管理员门禁，非提权测试构建必须硬拒绝真实 run/restore，禁止使用 MessageBox 或复制 PE 用户 CLI。
- `正常系统端/src/core/cli_tools.rs`：工具箱全部 22 项的公开 JSON CLI 适配与唯一稳定名称映射；`tool list` 输出当前环境可用性、风险分类和用法，inventory/inspect/plan 复用 GUI 的 fresh 库存、纯规划或只读边界，`run|remove --yes` 只把相同强类型计划交给现有生产后端，不得复制磁盘、账户、APPX、BitLocker、PE 交接或外部工具实现。盘符、磁盘号、文件清单和布局文件必须严格有界解析；BitLocker 解锁秘密只允许从 stdin 读取并在生命周期结束时清零，GHO 密码和恢复密钥默认脱敏且只有显式 `--show-secret` 才输出；CLI 不自动提权，PE 维护仍须同一 `pe_maintenance_entry_enabled` 门禁。
- `正常系统端/src/core/cli_config.rs`：`schema_version=1`、拒绝未知字段及任意深度重复 JSON 键的安装/备份 CLI 配置 DTO、严格本地绝对路径归一化、脱敏、4 MiB 有界普通文件读取及受保护同目录原子替换/回读；安装自动化可显式要求整体继承 EXE 相邻 `config.json` 的普通 `install_prefs`，并用去重、规范化的稳定 v4 软件 ID 列表表达当次预装选择，禁止把 URL 或静默命令持久化到 CLI DTO；高级 DTO 必须覆盖 GUI 可执行的个人文件保留、Wi-Fi 瞬态凭据、VMware Tools、Windows 7 USB3/NVMe/存储/UEFI 兼容项并在输出中隐藏 Wi-Fi XML 与账户密码，个人文件保留只能由当前版本化文档显式表达、不得从相邻 GUI 偏好继承；破坏性 `install_mode` 与去重的 `confirmed_disk_numbers` 必须由当次 CLI 配置显式表达，默认单分区模式禁止携带磁盘号，全盘模式禁止空集合，且不得从 GUI 偏好继承旧全盘或双系统计划；`automation_shutdown_on_terminal` 只能作为版本化 CLI 的显式可丢弃虚拟机策略，默认关闭且不得持久化到 GUI 偏好；备份意图以 `execution_mode=auto|direct|via_pe`、`output_policy=create|replace|append` 和 `auto_reboot` 表达，禁止恢复会被格式静默忽略的 `incremental` 布尔值，且输出扩展名必须匹配 WIM/ESD 格式；配置路径及现有祖先拒绝重解析点，临时文件与发布目标的 protected DACL 只授予当前交互用户、SYSTEM 和 BUILTIN\Administrators，发布后必须在最终路径再次精确核对该 DACL，但不得改写桌面、文档、项目等任意用户父目录的 ACL。DACL 回读按 trustee SID 的唯一集合判断：当前用户、SYSTEM、Administrators 必须各自出现且具有允许式完全控制；同一受信 SID 的等价重复 ACE 可以规范化接受，任何额外 SID、缺失 trustee、拒绝 ACE、继承 ACE、非完全控制或损坏 SID 仍须拒绝，禁止把 ACE 数量误当身份集合。覆盖必须显式 `--force`，密码不得成为命令行参数或输出内容。
- `正常系统端/src/core/cli_install.rs`：把严格 CLI 安装 DTO 映射到 fresh 分区/镜像/BitLocker/PE 缓存库存和 `NativeInstallState::start_intent`；用户指定的 WIM/ESD/SWM index 必须先存在于真实镜像库存并复用 GUI 的 `dism::is_installable_image` 排除明确 PE/非安装项，不得叠加只在部分解析路径赋值的旧 `verified_installable` 标志；全盘模式必须从所选镜像的真实展开最低容量、fresh SetupAPI 内部磁盘库存、`target_partition` 当前磁盘和本次显式确认集合调用共享 `build_full_disk_plan`，只把随机 locator 计划交给受认证 PE，磁盘号不得变成跨重启指纹；即使继承 GUI 偏好也必须先清除旧 `custom_install_plan`，再仅按本次 CLI 全盘请求覆盖；显式继承 GUI 偏好时必须要求 EXE 相邻 `config.json` 存在且严格可解析，不得缺失后回退默认值，并在同次计划中从当前 v4 目录按稳定 ID 唯一解析预装软件、拒绝 VMware Tools 特殊项和无静默命令项，再走共享 selected-package 校验；若可选 v4 软件目录因网络/DNS 在有界重试后仍整体不可用，必须在 plan 输出有界 warning、当次清空预装包并继续核心安装，禁止把全部软件下载失败升级为 Windows 安装失败；目录已经成功读取后的确定性 ID/命令错误仍须停止。计划时只确认当前已有 PE 是普通、非重解析文件并记录目录项；已有本地 WIM 允许用户在 plan 与 run 之间继续自定义，CLI 不得按目录 SHA-256/MD5、计划时字节或大小阻断，真正消费时仍只使用复制后回读一致的私有快照；显式终态关机策略必须只写入本次 runtime intent，再进入受认证 PE config 和首登收尾器，`plan`、`dry-run`、缺少确认和 GUI 路径不得触发关机；当版本化配置同时显式启用 `auto_reboot` 与可丢弃虚拟机自动化终态策略时，交接成功后必须走共享 Win32 自动化重启并给控制器 15 秒发布本轮 SessionId/loader 证据，普通交互重启仍保持 5 秒且不得强制关闭应用。计划输出必须以 controller 产出的 intent/options 形成脱敏 `effective_config`；计划与执行只复用 `NativeInstallExecutor` 和 `ProductionInstallBackend`，不得手工构造 `InstallConfig`、复制镜像或直接写 PE 交接，旧 `--install --advanced` 由公开路由直接返回确定迁移错误，模块内不得保留旧执行函数。
  公开安装 CLI 的 `install_mode=dual_boot` 必须同时接收正整数 `dual_boot_size_gib`，禁止携带 `confirmed_disk_numbers`，并要求引导修复开启；配置、生成器和帮助文本必须使用同一严格字段。`cli_install.rs` 只把该整数 GiB 转为字节后与当前镜像共享的精确展开最低预算比较，再对 fresh `target_partition` 调用 GUI 同一个 `build_dual_boot_request`；后续 Shrink、目标/暂存预创建、回读和 PE 交接仍只能由 `ProductionInstallBackend` 完成。计划输出的 `effective_config` 必须明确实际模式、全盘确认磁盘或双系统容量，但不得输出 locator token。
- `正常系统端/src/core/cli_update.rs`：正常系统端公开 `update restore` 的当前 Windows 根验证与可逆更新恢复适配；只允许从已提权控制台调用共享 `offline_update_control` 的安装绑定、逐项所有权和 compare-and-swap 恢复，不得自动提权、弹框、覆盖管理员/域/MDM 冲突或把 partial 伪装成无警告完成。
- `正常系统端/src/core/cli_backup.rs`：把严格 CLI 备份 DTO 映射到 fresh 分区、稳定源/目标卷身份与 BitLocker 状态后复用 `plan_backup_launch` 和 `execute_backup`；`execution_mode=auto|direct|via_pe` 必须显式约束实际路由，`direct` 不得捕获正在运行的系统卷，`via_pe` 必须复用受认证 LRBK2/LRPE4 配置、PE 私有快照和 BCD 事务，禁止另造 CLI 交接旁路。计划时只确认实际选中的普通、非重解析 PE 缓存条目；本地 WIM 可由用户自定义，run 前不得按目录哈希或旧字节重新阻断，执行器仍必须复制到受保护私有快照并回读复制一致性；ViaPE 只接受未加密源/目标卷，Direct 可接受稳定已解锁卷。计划输出必须明确实际 mode、create/replace/append 策略、稳定身份摘要、BitLocker、PE 摘要、`effective_config` 和 warnings；既有目标必须在计划与执行时绑定完整长度和 SHA-256。ViaPE 执行器的 Completed 只表示认证交接和 PE 启动项已提交，终态必须报告 `ready_to_reboot_into_pe` 与 `backup_not_yet_created=true`，不得伪装成备份完成；显式 `auto_reboot=true` 只能在 ViaPE 交接完成后调用共享 Windows 重启边界，重启调度失败仅作为有界 warning 保留已提交任务。Direct Completed 后只能信任共享 CAS 的 durable receipt，CLI 不得再按目标路径重开、重新分类或用路径级卷查询覆盖已提交结果；SWM/GHO 多文件发布仍不属于公开 CLI。
- `正常系统端/src/core/disk.rs`：分区枚举、样式和磁盘关系查询、SSD/HDD 与内外置介质探测、WinPE 安装目标显示默认值、ViaPE 暂存策略接入，以及通过共享 WinAPI 边界缩小/创建/删除/回收暂存与恢复分区；枚举结果还必须从物理磁盘 IOCTL 库存补齐磁盘容量、分区偏移、精确长度和 ESP/MSR/Recovery/BasicData 角色，缺失这些几何或角色事实时不得自动预选 WinPE 安装分区，安装意图仍按既有承重身份边界失败关闭。WinPE 默认值只能在有盘符且不为 `X:` 的候选中先选磁盘号最小的已确认 SSD，再按真实分区 offset 选该盘首个非 ESP/MSR 分区；没有已确认 SSD 候选时才按磁盘号和真实 offset 选择第一块磁盘，`StorageDeviceSeekPenaltyProperty`/descriptor 查询失败保持 Unknown 并进入后备排序，禁止猜成 SSD/HDD。`IOCTL_STORAGE_GET_DEVICE_NUMBER` 的短结果和 `0xFFFFFFFF` MPIO/不可分区哨兵必须拒绝进入默认候选；该默认值只是 UI 便利，不能替代首次写入前的 fresh 身份、extent 和 BitLocker 复核，库存刷新必须优先保留仍存在的人工选择。当前运行 Windows 卷必须由共享 `GetWindowsDirectoryW` 边界确定，禁止读取可能陈旧的 `SystemDrive` 或回退写死 `C:`/`X:`。ViaPE 暂存可在固定存储不足时回退到可写外置存储，但必须在候选入口排除光驱、网络盘、RAM 盘和只读卷；微软支持在不完整解密的情况下在线缩小 BitLocker NTFS 卷，因此当前系统卷只有在已解锁、转换状态稳定且位于非外置磁盘时可进入原生缩卷路径，已锁定、加密/解密中和未知状态继续失败关闭；自动缩卷前必须重新复核 BitLocker 状态及当前启动会话内的物理磁盘和分区身份，创建失败时只允许按已验证的实际回收量回滚扩容。双系统目标卷与可选数据/暂存卷必须由同一个 move-only 正常端事务一次 Shrink 后连续创建并回读，单 C 卷不得先因缺少数据卷失败或发生第二次缩卷；双系统所需总容量不是完整扇区时，必须从当前物理磁盘的权威 IOCTL 几何读取逻辑扇区大小并仅把 Shrink 请求向上表达为完整扇区，禁止硬编码 512/4096、放宽容量最低值或改写 provider 回读 extent；`QueryMaxReclaimableBytes` 只能记诊断，不能替代真实 Shrink 结果成为门禁。
Shrink 成功后的任何二次回读错误都必须按当前同盘同起点的实际尾缩安全恢复；Shrink 前后权威卷 extent 推导出的精确 reclaimed tail 是 caller authorization envelope。checked create 对该 envelope 只复核即时 canonical 布局无重叠，随后执行真实 Create 和 canonical delta 回读；不得因 `Refresh` 后仍滞后的 raw/provider 库存重复阻断。caller-authorized GPT `CreatePartitionEx` 保持 canonical offset 原值、`ulAlign=0`，只把 desired/minimum 容量收束到当前真实 logical sector 可表示的完整字节数；禁止把 logical/physical sector 或固定 512 B/4 KiB 猜成额外 VDS alignment。正常端从现有卷尾 Shrink 自动创建暂存时，必须在 Shrink 前按 canonical 磁盘容量读取微软 MS-VDS Appendix B 指定的 `HKLM\System\CurrentControlSet\Services\vds\Alignment` size-tier DWORD（缺失才用对应 64 KiB/1 MiB 文档默认值），精确增加当前 desired start 的布局余数，使仍未产生 provider extent 的 reclaimed tail 起点落在 provider-default 边界；这只是拓扑开销，不得重复加到 payload 加固定 2 GiB 的内容预算。Shrink 后实际 tail offset 仍须原值传递，禁止再次对齐或取整。可逆 Extend 回滚同样以 canonical 当前源 extent、下一分区起点或磁盘末端划定相邻上界，再调用真实 Extend 并回读；不得要求另一次 `QueryExtents` 先声明空闲。`VDS_INPUT_DISK.plexId` 在 Extend 中是必填的当前 plex 对象 ID，禁止传零 GUID 猜测“默认 plex”；基础分区只允许从已核对的 VDS volume 唯一枚举到一个 non-empty simple plex/one member 后使用其真实 ID 和 member index 0。输入 disk ID 必须由同一 volume 的 `IVdsVolume::GetPack` 和该 pack 的 `IVdsPack::QueryDisks` 取得，禁止从全局 provider 枚举拼入另一 pack 的同磁盘号对象；其他拓扑或 pack 内无法唯一绑定时停止。暂存创建的 caller minimum 必须包含精确 payload 和同一份固定 2 GiB 余量，不能只用 payload；格式化后实际 free bytes 必须不少于精确 payload，禁止先截断 MiB 再比较。
- `正常系统端/src/core/custom_install_plan.rs`：仅从 SetupAPI 当前存在磁盘和 UI 明确确认集合生成全盘计划，为每个选中磁盘生成独立随机 locator；构建双系统最低容量请求、解释权威 Shrink 的 `defragsvc` 环境错误，并把 provider 实际缩卷/创建几何留给 `disk.rs` 回读后写回同一计划。
  该计划直接消费 `quick_partition.rs` 从同一 exact SetupAPI 接口句柄取得的容量、动态完整布局及 descriptor attachment，不得再用 `PhysicalDriveN` 或第二套属性查询硬门；“Windows 使用全部剩余空间”只改变布局策略，绝不能把选中镜像的最低容量清零。
- `正常系统端/src/core/dism.rs`：正常端高层镜像查询、释放、捕获和进度模型，完整透传 index、版本、Build、架构及镜像类型元数据并接入统一 WIM 引擎；`is_installable_image` 是 GUI、CLI inspect 和 CLI 安装计划唯一共享的可选镜像分类，明确排除 WinPE 后应接受真实库存返回的标准安装/整盘备份 index，旧 `verified_installable` 仅为兼容字段，不能成为独立阻断条件，最终可应用性由真实镜像引擎操作及完整性结果判断；同时把安装执行器持有的原子取消标记传递到可取消的镜像应用入口；在线驱动导出优先走受支持的 DISM 命令，失败时才严格回退 SetupAPI，离线导出只允许 DISM 且不得回退手工复制 DriverStore；用户显式“仅保存”仍必须验证非零 INF，安装默认“自动导入”则允许 SetupAPI 完整枚举后得到零个第三方 OEM 包，但仅在启动存储要求也为空、空清单已原子生成并回读时作为明确 no-op。在线导出使用当前 Windows 卷的真实父链生成版本 2 清单；PE 中离线导出必须先以所选源盘符获取当前磁盘父链，再用 DISM 离线库存把这些设备映射到实际导出的非 inbox 包，只有证据完整才原子写清单。拓扑或目标包库存不完整时必须删除旧清单，SaveOnly 仍保存非零完整驱动树；不得根据当前 PE 自身控制器生成离线源清单，也不得伪造空清单。错误必须保留底层 DISM 命令链以便日志定位。
- `正常系统端/src/core/dism_cmd.rs`：DISM.exe 参数封装、进度解析、在线/离线驱动导出、离线驱动和更新包操作；优先使用当前 Windows/WinPE 自带 DISM，仅在不可用时回退随包兼容副本；stdout/stderr 必须并发持续排空以避免管道互锁，逐行按 UTF-8/GBK 解码，错误摘要有界且非零退出必须携带可用诊断；驱动源目录枚举拒绝根和任一子项的重解析点并传播遍历错误，INF/CAB 混合目录或多 CAB 处理只要任一子操作失败就整体返回失败，禁止部分成功伪装成导入完成；批量驱动导入失败时只用标准 DISM 逐个精确 INF 隔离，不得添加 `/ForceUnsigned` 或重复自制验签链。
- `正常系统端/src/core/driver.rs`：共享驱动实现的兼容再导出，以及只允许 DISM、任何失败均停止的离线驱动导入策略。
- `正常系统端/src/core/ghost.rs`：Ghost 镜像信息、备份、还原、进度、取消和错误分类。
- `正常系统端/src/core/gho_password.rs`：读取和解码多种 GHO 头部中的密码信息。
- `正常系统端/src/core/hardware_info.rs`：使用 WinAPI/WMI 收集 CPU、内存、主板、BIOS、磁盘、GPU、网络、电池和系统信息；网络摘要必须复用 `tool_network` 的统一 IP Helper 枚举和 Windows 7 链路速率回退，禁止保留固定返回 0 Mbps 的第二套旧枚举；物理磁盘容量使用长度查询并以扩展几何查询安全回退，分区布局查询同时保留真实分区数；诊断日志按制造商与机型识别常见 VMware、Hyper-V、VirtualBox、QEMU/KVM、Parallels 和 Xen，未知指纹只能写“可能为实体机”，不得断言为实体机。
  物理磁盘出现性、容量和布局复用 `quick_partition.rs` 的 SetupAPI exact-path 完整库存，禁止固定扫描 `PhysicalDrive0..15/31`；对已知磁盘号的 SSD/TRIM 探测仅为可选显示，失败只能为 Unknown，不能进入安装计划或写盘授权。
- `正常系统端/src/core/hardware_info/names.rs`：硬件厂商和 GPU 名称的纯规范化及占位符识别。
- `正常系统端/src/core/hardware_inspector.rs`：详细硬件检测的只读快照边界，组合既有硬件摘要、CPUID intrinsic、严格边界检查的 `RSMB` 固件表解析、按 LUID 去重的 DXGI 适配器，以及标准 NVMe Identify/SMART 健康日志查询；解析 128 位寿命计数器并显示累计读写、健康度、温度、通电和错误统计，不加载驱动、不访问硬件端口、不执行磁盘写入。
- `正常系统端/src/core/image_verify.rs`：识别 WIM/ESD/SWM/GHO 等镜像并编排验证缓存探测、完整校验、进度和结果汇总；只有完整 libwim 校验成功后才授权写入当前指纹，支持调用方持有且不会被内部复位的取消标记，并把它传入 WIM/ESD/SWM 的 libwim 校验回调。SWM 校验必须复用共享严格连续分卷枚举，并把除主卷外的精确有序路径逐一交给 flags=0 的 resource-file API；不得构造或传递 glob，非规范同名前缀文件不能进入引擎消费集合。
- `正常系统端/src/core/image_verification_cache.rs`：正常端 WIM/ESD 重复校验的有界持久化 BLAKE3 缓存；以拒绝写入/删除共享的源句柄、只读映射、官方 BLAKE3 的 Ryzen/Intel 运行时 SIMD 分派与 Rayon 分块、原子缓存替换和严格回退保证快路径不把变化或缓存错误误判为成功。
- `正常系统端/src/core/install_config.rs`：生成 authoritative config 与 LRHM3 boot payload，事务发布独立 `LetRecovery_Data.marker` / `LetRecovery_Target.marker`，并把当前会话 `PreservePersonalFiles` 作为 HMAC 认证布尔字段传给 PE；该字段为真时必须同时证明关闭格式化、使用单分区重装且不是 GHO/XP。启用内置 Administrator 时先完整验证选项，再清空 authoritative INI 的密码字段，把规范化密码字节作为唯一 `ProtectedAdministratorSecret` 记录加入 LRHM3 并交给私有 WIM 注入；禁用时不得生成 secret。不再把配置或 LRHA1 副本写入公开卷；回滚只恢复本次 locator、目标 locator 与实际 artifact，不能递归触碰其它文件。自动暂存删除授权仍单独生成 canonical post-shrink source/temporary pair 与 exact pre-shrink source length。
- `正常系统端/src/core/iso.rs`：通过 Windows Virtual Disk API 挂载和卸载 ISO；挂载后必须用 `GetVirtualDiskPhysicalPath` 与 `IOCTL_STORAGE_GET_DEVICE_NUMBER` 精确关联本次附加设备和盘符，映射失败立即按同一句柄回滚。卸载只能按 LetRecovery 持有的原始 ISO 路径执行，不得扫描、猜测或弹出其他 ISO、物理光驱和用户介质，成功操作后的卸载失败也必须向上传播。
- `正常系统端/src/core/nvidia_driver.rs`：GPU 枚举、厂商识别、NVIDIA 驱动设备和软件清理支持。
- `正常系统端/src/core/pe.rs`：所有已经存在于随包目录或下载缓存的本地 PE WIM 都允许用户自定义；目录 MD5/SHA-256 只在下载执行器把新文件发布为缓存前校验，查找、安装、备份及维护启动不得再次把目录哈希或旧字节当门禁。模块协调 PE 文件准备、启动项安装和进入 PE；安装会话把 manifest 绑定的 Administrator secret 只注入固定私有 WIM 路径并在提交 BCD 前解出回读逐字节验证；另构造独立 `maintenance` LRHC1/LRHM3 会话，将可选 `LRBL1` 恢复密码只作为 manifest 绑定的私有 WIM secret 注入，并在 BCD 事务提交后通过共享 WinAPI 安排一次重启。点击维护入口后原生窗口必须立即显示带动画的阶段状态，不能等后台准备完成后才弹结果。CLI/后端只能从拒绝写入和删除共享的普通非重解析源句柄复制到受保护、拒绝删除共享的私有快照，并通过源流计算值与快照复读值确认本次复制一致；这不是与 config、目录或计划哈希比较，后续禁止再打开原缓存路径。私有根必须位于 `GetWindowsDirectoryW` 所确定的当前 Windows 卷根 `LetRecovery_PE`，不得假定系统卷为 `C:`；新建时必须用原子安全描述符仅授予 SYSTEM/Administrators 继承式完全控制，既存根必须通过 owner、受保护 DACL 和非重解析验证，空但不可信可删除后原子重建，非空不可信直接拒绝。根目录以不共享删除的 `OPEN_REPARSE_POINT` 句柄贯穿事务，journal/载荷只能是其直属普通文件；新任务先按可信 journal 回滚旧 BCD，再只删除共享分类器精确识别的孤立产品文件，未知内容保留且不得阻断新任务。每次交接预生成全新 ramdisk/loader GUID，把 WIM/SDI 从拒绝写入和删除的源句柄流式复制、SHA-256 复读后原子发布到会话隔离文件，并在任何 BCD 修改前持久化 LRPE journal；不得预删旧对象或修改全局 display order/timeout，只能用 `bootsequence <GUID> /addfirst` 发布一次性启动并回读，回滚先 `/remove` 并回读，再按创建逆序删除并回读新对象，最后删除本次载荷；active/pending journal、显式事务提交/回滚和 Drop 保护必须覆盖崩溃及中途失败。所有 BCD 写入逐条检查启动及退出状态，journal 缺失、损坏或无可信 `boot.sdi` 时停止，不得用伪造占位文件继续重启。仅在显式 `ci-automation` 测试 feature 下，允许从精确 `LETRECOVERY_CI_FAULT=missing_boot_sdi:<32 位十六进制 run id>` 在创建默认 SDI 前注入写前失败；正式、`dev-build` 和 GUI 默认构建不得包含或触发该入口。
- `正常系统端/src/core/pca_preflight.rs`：正常端 PCA 写盘前预检适配、固件读取、匹配兼容包准备及共享错误到本地化用户提示的映射。
- `正常系统端/src/core/partition_copy_impl.rs`：历史分区对拷库存、断点标记、空间检查和逐文件复制实现；只有 `native_partition_copy.rs` 在 fresh 复核与请求校验后才能进入该执行边界，部分失败必须保留可恢复标记。
- `正常系统端/src/core/quick_partition.rs`：通过存储属性/卷/磁盘 IOCTL 一次进程内枚举物理磁盘、分区布局、卷标/文件系统/使用量和活动标记，避免逐盘启动 PowerShell/WMIC；一键分区及已有分区创建、删除、格式化和扩缩复用共享 WinAPI 边界，执行前后复核磁盘身份、分区偏移、大小和结果状态。相邻未分配空间必须保留精确字节数，与分区原始字节数合并后才能统一向下转换为 MiB，禁止分别取整造成合法的末端拖动被误拒绝并回弹。
  物理库存必须以 SetupAPI 不透明 device path 和同一 `GENERIC_READ` 句柄读取当前 disk number、容量、动态完整布局及有界 `STORAGE_DEVICE_DESCRIPTOR`，每个完整快照成功后才按 disk number 收束；两个容量 IOCTL 都失败时只允许 VDS 容量字段回退，exact-path 布局失败仍隔离该接口。`STORAGE_DEVICE_NUMBER` 的身份是 `DeviceType + DeviceNumber`，VDS 候选的 `dwDeviceType` 与打开 locator 后 IOCTL 回读的 `DeviceType` 都必须是 `FILE_DEVICE_DISK` 才能比较 disk number；CD/DVD 设备 0 不得别名为物理磁盘 0。共享 checked 边界已经操作后回读成功时不得再次全机枚举并以精确相等制造写盘后的假失败。
- `正常系统端/src/core/registry.rs`：共享离线注册表实现的兼容再导出。
- `正常系统端/src/core/system_info.rs`：当前机器启动模式、Secure Boot、TPM 和环境摘要。
- `正常系统端/src/core/system_utils.rs`：PE 文件架构、离线 Windows 版本/架构、权限和系统路径等通用 Windows 工具，并通过 Task Scheduler 2.0 COM API 连接本机服务、定位和触发系统自带的 `StartComponentCleanup` 任务。
- `正常系统端/src/core/ui_state.rs`：原生 UI、配置文件和 CLI 共用的安装偏好、启动模式、驱动动作及高级选项纯数据模型，保持旧 JSON 默认值和字段兼容，并显式转换到旧高级选项业务类型以保留不可序列化的 Wi-Fi 瞬态数据；Wi-Fi profile 和 SSID 不持久化，因此加载后若只有历史 `migrate_wifi=true` 而没有本次会话 profile，必须同时归零勾选和残留 SSID，禁止显示无法执行的欺骗状态；首次缺省启用移除快捷方式箭头、跳过 Windows 11 联网要求、禁用保留存储和禁用设备自动加密，PE/服务账户令牌不得成为自动填充用户名；同时集中维护 XP、Windows 7/8/8.1/10/11 的高级选项能力矩阵，界面隐藏不能破坏其他镜像的持久偏好，但进入安装意图的克隆必须清除目标版本不支持的字段。
- `正常系统端/src/core/tool_actions.rs`：历史外部工具启动、Ghost/SpaceSniffer、引导修复和驱动导出兼容入口；只允许由原生工具控制器生成并确认的固定动作调用，不接受服务端自由命令。
- `正常系统端/src/core/tool_driver.rs`：历史在线/离线驱动导出、导入及固定存储控制器目录兼容实现；新的存储驱动入口仍须经过硬件 ID 唯一匹配与 `native_storage_driver.rs` 安全计划。
- `正常系统端/src/core/tool_network.rs`：网络适配器只读详情和既有网络重置步骤实现；链路速率优先使用 `GetAdaptersAddresses` 的 64 位值，只有返回零时才按接口索引回退 Windows Vista+ 的 `GetIfEntry2`，最后使用旧 `GetIfEntry`，不得把全部接口未知误显示为 0 Mbps；修改性入口必须经过原生确认与工具后端，不在渲染回调中执行。
- `正常系统端/src/core/tool_time_sync.rs`：固定可信 NTP 服务器查询、北京时间换算、Windows 系统时间写入和结果模型；网络与写时钟操作只从已确认的工具后端进入。
- `正常系统端/src/core/tool_types.rs`：原生工具控制器、库存与兼容业务共用的驱动模式、APPX、软件、Windows 分区、GHO、NVIDIA 和镜像校验数据类型的唯一模块实例。
- `正常系统端/src/core/windows_version_detect.rs`：Windows 分区版本与架构检测；先通过共享 `GetWindowsDirectoryW` 边界动态识别当前 Windows 卷，当前系统只读在线 `HKLM\\SOFTWARE`，不得把正在使用的 SOFTWARE hive 再交给 `RegLoadKeyW`，其它 Windows 分区才使用碰撞安全别名加载离线注册表，并以文件版本与架构特征回退；向工具库存提供版本摘要，不承担界面渲染。
- `正常系统端/src/core/native_wifi.rs`：使用 Native Wi-Fi API 枚举已连接接口、读取当前连接属性并按显式明文密钥标志取得本次安装会话需要的 profile XML；只能在唯一已连接接口上自动选择，多个接口必须因歧义停止，`WlanGetProfile` 返回成功后仍须证明共享密钥为可移植明文或 profile 为 open/OWE，依赖 802.1X/per-user 凭据或仍受保护的密钥不得伪报捕获成功。XML 只在当前意图内存或受认证私有 boot.wim 固定文件中传递，绝不能写入公开 handoff 数据卷、持久配置或日志；落到新系统后由共享首登 finalizer 隐藏导入并检查退出码，开发测试构建禁止宿主探测和获取。
- `正常系统端/src/core/native_expand_c_controller.rs`：旧入口通过 `GetWindowsDirectoryW` 动态确定当前系统卷，同时只读分析任意指定盘符、相邻未分配空间及后方可收缩分区，区分直接扩展与需要搬移数据的容量上限；为成对分区边界拖动提供“目标从左侧相邻普通数据卷借空间”的独立只读分析，左侧借空间的直接扩展上限固定为当前大小。两种方向都通过同一 WinAPI 身份拒绝把当前运行 Windows 卷作为待移动卷，不得回退 `SystemDrive` 或写死 `C:`；不执行缩小、移动、PE 准备或重启，开发测试构建在读取宿主磁盘前拒绝。
- `正常系统端/src/core/native_expand_c_executor.rs`：指定盘符无损扩大的强类型 PE 交接 worker（旧 C 盘界面继续复用）；生产入口只接受目标大小不超过 fresh `no_move_max_mb`、不携带左侧借空间或供体最终大小的纯扩展请求，并在建线程、下载/校验 PE、写 marker/INI 或创建 BCD 前拒绝所有 shrink/transfer/raw-move 计划；执行前重新分析并比对完整磁盘/目标分区身份、当前大小和安全容量，复验受管 PE，把显式认证目标大小及 fresh 磁盘容量、目标精确几何写入 LRPE4 交接，禁止使用旧 `0 = maximum` 编码把布局变化后的搬移空间静默纳入任务；PE/BCD 启动项安装失败时精确回滚本次扩容 marker/INI，开发测试构建在建线程和任何宿主 I/O 前拒绝。
- `正常系统端/src/core/native_install_controller.rs`：原生安装页输入快照的校验、Direct/ViaPE 路由，以及保持 XP/GHO/PCA/驱动/无人值守字段兼容的安装意图与配置转换；个人文件保留只接受包含既有 Windows 的单分区重装目标和 Windows 7+ WIM/ESD/SWM，必须强制关闭格式化并从完整 Windows 走受认证 ViaPE，GHO/XP、全盘、双系统、空目标或 PE 内临时发起都在写盘前拒绝。目标授权身份必须具有磁盘号、分区偏移和精确长度，任一缺失不得产生安装意图；分区号和磁盘容量仅供诊断，不得参与后续物理卷相等判断。ViaPE 只校验目录是否可用并固定产生首项索引，不接受 UI 选择状态；Wi-Fi 迁移是普通会话优化，缺失瞬态 profile 时必须在最终意图克隆中记录 warning 并归零，不能弹框或阻断安装，ViaPE 则必须保留有效 profile 并交给受认证私有 boot.wim，不得因跨重启路由静默关闭；内置 Administrator 在写盘前校验无人值守、镜像族、外部应答文件、普通自定义用户名、账户名与密码的兼容边界，普通自定义用户名必须通过共享系统保留身份策略；只有经库存确认仍包含 Windows 的目标才允许计划导出旧系统驱动，新建或已清空分区不得因默认 AutoImport 在镜像应用前触发离线 DISM 导出；原版 XP I386/AMD64 选择当前系统盘时走标准 ViaPE 路由而不是桌面原地写盘；PCA 检测只在镜像支持、启用引导修复且目标可能使用 UEFI 时阻断，不能让持久化选择误拦 GHO、XP 或 Legacy 安装；生成意图前还必须以选中镜像 major/minor/build 对高级选项克隆再次过滤，禁止隐藏的现代 Windows 默认项流入 Win7/旧系统或 ViaPE 配置，并只为 Windows 7 x64 UEFI 自动置位 UefiSeven 交接字段。
- `正常系统端/src/core/native_install_executor.rs`：原生安装意图的 Direct/ViaPE 有序阶段计划、BitLocker/PCA/目标与输入保护顺序、Direct 驱动导出先于目标分区写入和格式化的保护顺序、进度取消消息和可注入副作用后端；总体进度必须由本次真实有序 plan 累加各阶段权重后生成连续区间，镜像应用和源复制占主要区间，禁止再给 phase 枚举分配会因可选阶段、执行模式或重复区间而倒退的全局固定百分比。`StartInstallIntent.running_in_pe` 固化启动环境：正常端运行在 PE 且选择预装软件时不得安排前置空下载，而必须把真实的 `StageDirectPreinstalledSoftware` 放在镜像、驱动和可选引导之后，并给网络传输独立总体区间；桌面 Direct 则保留破坏性写入前下载、系统可用后只复制暂存包的顺序。ViaPE 从 `WritePeInstallConfig` 开始进入不可取消提交区，`PhaseStarted` 必须同步向 UI 报告 `cancellable=false`，之后即使收到旧取消信号也必须完成配置/BCD 提交或明确回滚，禁止成功写入后再报告 Cancelled。正常端只把用户选择写入本次随机 marker 和受认证交接，不得把磁盘号、分区号或完整几何变成 PE 跨重启筛选条件；Direct 仅在紧邻首次写入处确认当前目标与输入不相交。历史分区脚本阶段名只为旧状态兼容，不得由新意图启用。`InstallExecutionError::user_message` 按失败类别和阶段提供不泄露底层诊断细节的本地化用户摘要，源镜像校验失败还必须展示不含路径的常见原因与固定诊断代码，使单张截图可供支持判断，完整 code/detail 继续保留在 `Display` 日志边界，开发测试构建强制拒绝执行；测试必须用无副作用 backend 对每个受支持阶段注入确定性 API 错误，证明失败阶段精确归因、后续阶段和成功事件均不再执行。
- `正常系统端/src/core/native_partition_copy.rs`：原生分区对拷的只读展示库存，以及严格盘符、fresh 卷库存、目标空间、断点来源验证、受限日志和可注入执行边界；展示库存不授予执行权限，确认后仍须 fresh 复核，执行时可向原生窗口流式回报进度，并把完成、部分失败和断点续传结果显式区分；开发测试构建在宿主 I/O 前拒绝。
- `正常系统端/src/core/native_nvidia_removal.rs`：恢复迁移前英伟达驱动清理语义的强类型当前/离线 Windows 目标、仅含真实 NVIDIA GPU 型号的启动硬件摘要和既有后端请求适配；执行范围固定为原业务支持的全部 NVIDIA 驱动，不虚构单设备或 NVIDIA 软件卸载能力，开发测试构建在宿主硬件读取前拒绝。
- `正常系统端/src/core/native_password_reset.rs`：当前系统/离线 Windows 的强类型单账户密码重置请求、只读账户库存和执行边界；当前系统复用共享 NetAPI 边界清空密码并启用账户，离线系统复用带 SAM 备份的共享实现，执行前 fresh 复核账户且开发测试构建在任何宿主 I/O 前拒绝。
- `正常系统端/src/core/native_quick_partition.rs`：原生一键分区的磁盘号、型号、容量及现有布局强类型指纹，GPT/MBR 与分区大小、盘符、卷标、文件系统计划校验，执行前 fresh 枚举 fail-closed 复核及可注入测试边界；开发测试构建在任何磁盘 I/O 前拒绝。
- `正常系统端/src/core/native_quick_partition_dialog.rs`：一键分区专属原生对话框的纯编辑状态、可用盘符和当前 Windows 卷保护信息；生成 `QuickPartitionRequest` 与暂存的已有分区扩缩、相邻两个 NTFS 数据卷总容量守恒转移、删除、参数化格式化、盘符、活动标记、未分配空间创建及 RAW 盘初始化操作批次。已有分区的原地扩大上限和 fresh 复核必须先合并分区与右侧连续未分配区的精确字节数，再统一转换为 MiB。成对转移请求必须同时绑定左右分区指纹、原始/目标大小、使用量和完整磁盘指纹，拒绝非紧邻、特殊、当前 Windows、无盘符、非 NTFS 或剩余空间不足的目标；“右侧分区先吸收未分配空间、再向左侧分区转移”的连续计划必须保留为有序的直接扩容与 PE 转移两步，第一步完成后重新枚举、重建指纹并验证两卷最终总容量，禁止从原始布局重算或删除前置扩容。批次执行前必须 fresh 复核原始完整磁盘指纹，每一步都按当前库存重新定位目标、动态识别运行 Windows 卷、拒绝危险系统目标并在共享 WinAPI 调用后重新枚举验证；需要搬移数据的成对转移只能由 PE 强类型交接执行，桌面直接执行边界必须失败关闭。开发测试构建在任何宿主磁盘 I/O 前拒绝。主窗口收到合并后的设备/卷变更消息时自动刷新无暂存修改的对话框；已有暂存修改时必须标记库存过期并禁用应用，禁止静默覆盖编辑方案。
- `正常系统端/src/core/native_storage_driver.rs`：原生存储控制器驱动工具的固定随包 `bin/drivers/storage_controller` 来源、离线 Windows 目标及 fresh 库存复核计划；只读检查可注入且开发测试构建在宿主 I/O 前拒绝，本模块不调用 DISM 或写入驱动。
- `正常系统端/src/core/native_install_backend.rs`：原生安装执行器的生产后端阶段实现；复用镜像、受检 WinAPI 格式化、盘符/活动标记/MBR 签名、驱动、引导和高级选项边界。ViaPE 选数据分区前必须构造同一 `StagingPayloadBudget`，精确累计镜像/XP、在线 OEM 驱动、PCA、版本化用户驱动和 UefiSeven，选择与自动创建只消费“全部文件 + 固定 2 GiB”，各阶段完成后按实际产物回读；驱动或其它可选项少于预算只 warning，任何产物超过预算必须在镜像复制前停止。双系统没有现成数据卷时必须先把这份精确预算并入同一个正常端预创建事务，在一次 Shrink 中创建 Windows 与数据卷并把 provider 实际几何写回计划；后续配置阶段复用该事务，禁止再缩一次。任一首次破坏性写入前失败必须先释放本次受保护输入句柄，再回滚 config/BCD 和事务创建卷。Direct 在紧邻首次写入处确认目标与全部后续输入不相交，并持续锁定 WIM/ESD/SWM、GHO/GHS 或 XP 树；ViaPE 只签发随机 marker 与受认证交接，不得要求 PE 在每个阶段重绑正常端缓存的完整目标库存。连续 SWM 或以 `.gho` 为主卷的连续 GHS 必须从拒绝写入/删除共享的原句柄全套逐 span 复制到 SYSTEM/Administrators 受保护随机会话目录，逐项回读长度/SHA-256 后按确定 ordinal 写入 LRHM3，禁止只复制首卷、按源路径重开或接受 `.ghs` 主卷。单文件可并行融合校验与复制，所有模式贯穿原子取消标记；ViaPE config 与 BCD 事务必须按提交边界显式提交/回滚。Administrator 密码必须通过 `ProtectedAdministratorSecret` 私有 WIM 制品传递，公开/authoritative INI 保持空密码；自定义 answer 在等价 ProtectedBoot 制品尚未接通时仍对 ViaPE 失败关闭。只有承重输入或用户明确选择的必需结果失败才阻断 Finish，非启动存储驱动和其它可选项按开头的错误分级规则处理，开发测试构建保持无副作用。`ci-automation` 的 `existing_target_candidate:<RunId>` 仅用于指定可丢弃 Hyper-V Win10 矩阵：在驱动导出后、认证快照前按当前系统盘真实存储父链注入两个故意无有效目录签名的测试包，并把固定小额逻辑大小计入同一预算；它要求基线没有其它 OEM 启动存储要求，普通构建不得包含或触发该路径。
- 上一条 `native_install_backend.rs` 职责中关于“多卷 ViaPE 尚未实现、必须拒绝”的旧描述已由当前实现取代：SWM 与 GHO/GHS 现在只能整套从 held handles 复制进随机 SYSTEM/Administrators 会话目录，逐 span 回读后以连续 LRHM3 ordinal 发布；PE 端持有该安全目录与所有 span 句柄并拒绝实时全集中的 missing/extra 项。任一环节不满足时仍失败关闭，绝不退回首卷复制。
- `native_install_backend.rs`、`data_staging.rs` 与 `PE端/src/app.rs` 的预装软件补充职责：正常端在桌面环境先下载到碰撞安全临时目录，按实际非空普通文件长度把精确总字节加入同一数据暂存预算，再复制到本次数据分区并删除临时副本；正常端自身运行在 PE 时必须在镜像、驱动和可选引导完成后，通过独立 Direct 阶段直接下载到目标系统的 `LetRecovery_Scripts`，不得占用或重复复制 `X:`，也不得把网络下载隐藏在“高级选项”阶段。每个软件包下载最多有界尝试三次且单项失败必须与后续包隔离；任何阶段的软件包网络下载或响应读取失败在重试耗尽后都只能记录 warning 并跳过该包，即使全部选中软件均失败也必须以空软件计划继续系统安装，不得把可用 Windows 倒判为整机失败。后续暂存、ViaPE 认证配置和首登计划只能引用实际写入并回读成功的精确子集；成功子集为空时不得创建空的可选辅助目录，发现既有空目录也必须从 handoff manifest 静默省略，禁止把“没有可选软件可交接”升级为 `WritePeInstallConfig` 失败；结构/路径/认证错误和用户取消仍按其真实影响处理。HTTP `Content-Length` 只可平滑包内显示进度，最终接受与预算只能依据实际写入、普通文件回读和后续认证，缺失或错误长度不得成为阻断条件。ViaPE 的每个成功安装包必须作为 LRHM3 `PreinstalledSoftware` artifact 记录长度/SHA-256，PE 在首次破坏性写入前要求成功子集配置与 manifest 文件名集合一一精确匹配，随后只复制已认证打开的文件并生成首登计划。系统已可启动后的单项静默安装失败只记录 warning 并继续收尾。
- `正常系统端/src/core/native_install_compat.rs`：Direct 安装的默认无人值守 XML、版本化用户驱动目录、MBR 非零签名生成、同盘 XP active 清理目标筛选及 WinAPI 格式化参数的无副作用验证；不得恢复命令文本生成或解析，也不得因历史 `remove_uwp_apps` 字段重新生成 PowerShell 首次登录广泛清理命令，AppX 清理只允许在离线共享边界完成；Reserved Storage specialize 片段只能由 `lr-core::reserved_storage` 在已验证 build 19041+ 后签发的不透明支持 token 启用，不得重新接受裸布尔值；“移除预装应用”不得生成或注入 Win32 OneDrive 卸载 hook，Outlook 与 OneDrive 必须保持安装来源原状。普通本地用户名和内置 Administrator 无人值守片段必须复用 `lr-core::unattend_account`，验证发生在 XML 转义和写文件之前；Windows 10/11 渲染必须显式接收已验证国际化设置并完整写入 `Microsoft-Windows-International-Core` 与时区，缺失时失败关闭，不得恢复只对 Server 生效的 `HideLocalAccountScreen`。
- `正常系统端/src/core/native_image_source.rs`：原生安装页的 WIM/ESD/SWM/GHO/ISO 分类、镜像卷读取、只读 ISO 挂载与 XP/2003 文本模式源识别，并只读探测介质根、i386 及 WIM 卷内自带无人值守应答；启动发现只枚举当前存在盘符根目录下精确的 `sources\install.esd` 和 `sources\install.wim`，仅返回全局唯一候选，多个候选保持歧义；开发测试构建拒绝 ISO 挂载。
- `正常系统端/src/core/native_backup_controller.rs`：原生备份输入的 Direct/ViaPE 路由、WIM/ESD/SWM/GHO 任务规划及 PE 交接意图，只构造无磁盘副作用的用户意图；GUI 与 CLI 都不得自行附加可选盘符授权，执行层必须统一生成并强制消费源/目标稳定身份。格式和路径能力只有在共享执行器已实现稳定卷重绑与事务输出时才可开放。
- `正常系统端/src/core/native_backup_executor.rs`：统一授权并执行 GUI/CLI 备份的后台 worker；在捕获开始前把源/目标绑定到本次 canonical 卷并只把 volume-GUID 根交给镜像引擎，之后依靠持续持有的源、staged、held-old 与目录句柄及 CAS 事务证明同一对象，不得在数小时捕获完成后因为重新枚举无关磁盘失败而否决有效结果。ViaPE 要求两端均为 NotEncrypted，不读取或落盘恢复密钥，使用声明 SHA-256 的受保护 PE 私有快照、LRPE4/LRHA1 认证和备份专用启动策略，禁止复用安装端 BitLocker 恢复材料注入。WIM/ESD create/replace/append 统一在目标同卷 SYSTEM/Administrators 私有 staging 完成捕获和完整校验；create/replace 的完成态 catalog 必须精确为单个请求镜像，append 必须先从绑定 staged file ID 的 held-old copy 读取旧 catalog，再证明旧项逐项不变且只增加一个精确尾项；发布继续复用 PREPARED journal、handle CAS、durable committed receipt 与崩溃恢复。正常端图形 Direct/ViaPE 均可使用三种策略，公共 CLI 只可直接执行；SWM/GHO 生产路径仍失败关闭。
- `正常系统端/src/core/native_batch_format.rs`：原生批量格式化的固定卷重新枚举、按 `GetWindowsDirectoryW` 实际身份排除当前运行卷、强类型 `FormatCommandSpec` 计划、共享 VDS 格式化边界和逐卷结果；不得写死 `C:`/`X:`，开发测试构建在进入任何格式化 API 前拒绝。
- `正常系统端/src/core/native_bitlocker_gate.rs`：原生安装、备份和工具箱 CLI 共用的 BitLocker 锁定卷纯规划、严格盘符及凭据校验、脱敏凭据、凭据离开作用域时的内存清零和受控解锁边界；开发测试构建在管理器初始化及宿主 I/O 前拒绝。
- `正常系统端/src/core/native_bitlocker_manage.rs`：原生 BitLocker 管理工具的只读加密卷库存、状态允许操作矩阵、严格盘符、密码/恢复密钥复用校验和脱敏强类型意图，并提供 fresh 状态复核后的专用恢复密钥读取与后台操作执行入口；缓存库存不授予执行权限，开发测试构建在管理器初始化及宿主 I/O 前拒绝。
- `正常系统端/src/core/native_boot_repair.rs`：把一键修复引导的单个已检测 Windows 分区转换为既有强类型工具后端请求，执行前要求目标仍在 fresh 检测列表中并固定使用 `BootRepairMode::Auto`，UEFI/Legacy 继续由后端按分区样式自动判定；本模块无副作用。
- `正常系统端/src/core/native_download_controller.rs`：原生在线资源目录状态、分类选择、HTTPS/文件名/保存路径校验、架构 URL 选择、显式下载线程数和下载后动作计划；只有经固定 HTTPS 服务端目录装载并原样选中的历史条目可兼容其 HTTP URL，本地或任意构造目录仍默认拒绝 HTTP，显式兼容开关语义保留。
- `正常系统端/src/core/native_download_executor.rs`：执行已验证下载计划的后台 worker，把计划中的下载线程数显式交给 aria2，并负责进度/取消/完整性校验及显式完成后动作；开发测试构建禁止网络和文件写入。
- `正常系统端/src/core/remote_wim_metadata.rs`：通过精确且有界的 HTTP Range 读取远程 WIM/ESD 的头和 XML，或解析远程 ISO 的 ISO 9660/Joliet 目录及内嵌安装镜像单段/多段 extent；绑定最终重定向地址、Content-Range、实体验证器和 ISO/WIM 资源边界，过滤非安装卷并按实际 Windows 代际补全简易模式目录。探测器不得读取服务器忽略 Range 后返回的完整 `200 OK` 正文，只返回强类型“需完整下载”；UI 完整下载后必须用本地检查器重新验证并选择第一个可安装分卷。其它传输/范围/实体不一致仍失败关闭。
- `正常系统端/src/core/native_driver_transfer.rs`：驱动导出/导入对话框的纯状态、条件目录角色、按桌面当前系统/PE 首个离线系统排序的 Windows 目标库存复核、输入校验和强类型执行/浏览意图；不读取驱动目录、不调用 DISM。
- `正常系统端/src/core/native_tools_controller.rs`：工具箱 22 项的稳定原生动作映射、桌面/PE 可用性、预加载请求、对话框/外部工具路由和安全分类；无损扩大 C 盘使用专属对话框、只读分析预加载与危险存储分类，详细硬件检测使用后台只读快照，PE 维护入口是正常端专属的启动存储变更计划。
- `正常系统端/src/core/native_tool_executor.rs`：工具箱原生意图的类型化安全执行边界；只读任务可进入既有读取实现，镜像校验在独立工作线程中实时转发进度并接受外部原子取消标志，修改性与外部工具操作仅在明确确认后生成计划，开发测试构建保持无危险副作用。
- `正常系统端/src/core/native_appx.rs`：原生 APPX 移除安全边界；在线路径使用 PackageManager，离线路径使用 DISM `/Get-ProvisionedAppxPackages` 与 `/Remove-ProvisionedAppxPackage`，两者都在修改前 fresh 枚举、复核选择子集并过滤关键包；命令参数逐项传递并检查退出码和文本错误，严禁删除 WindowsApps 目录，开发测试入口在构造生产后端前拒绝。
- `正常系统端/src/core/native_appx_legacy.rs`：旧 APPX 实现的唯一兼容加载桥，只向原生代码公开固定当前系统的 PackageManager 库存、移除和关键包判断，禁止暴露旧离线目录删除入口。
- `正常系统端/src/core/native_appx_selection.rs`：专属 APPX 对话框的纯目标/包库存、桌面当前系统/PE 首个离线系统默认目标、关键包与重复项过滤、全选/全不选/反选、刷新清空和安全移除请求映射；只产生现有 `native_appx` 边界可复核的强类型意图，不读取目录或执行部署操作。
- `正常系统端/src/core/native_tool_inventory.rs`：原生修改性工具对话框的异步只读库存边界，枚举 Windows 版本/架构、账户、在线 PackageManager APPX、离线 DISM provisioned APPX、NVIDIA 设备、物理磁盘和带版本/架构信息的引导修复目标，按 `SystemDrive` 将桌面当前 Windows 排到系统目标库存首位（PE 中自然保留首个离线系统），提供在线当前系统入口时按忽略大小写及尾部分隔符的规范化盘符去除重复离线项，并保持显示标签与严格内部值分离；开发测试构建在宿主读取前拒绝。
- `正常系统端/src/core/native_tool_backend.rs`：执行已二次确认的原生工具计划，复用既有 Ghost、SpaceSniffer、时间同步、网络重置、NVIDIA 清理以及强类型批量格式化/分区对拷/在线与离线 APPX 安全边界，并返回逐项结果；开发测试构建在任何宿主 I/O 前拒绝。
- `正常系统端/src/core/native_easy_mode_controller.rs`：把已补全远程卷元数据的简易模式配置展平为原生页面状态，目录加载或启用时把首个可安装系统和卷同步为真实控制器选择，并生成保持旧默认安装选项、URL 查询参数安全剥离后的文件名、下载目录与自动安装语义的无副作用意图；简易模式默认尝试捕获唯一且可移植的当前 Wi-Fi profile，成功才启用本次迁移，未连接、权限不足、多个接口或不可移植凭据必须无误报地关闭该项并记录原因。下载前后必须同时要求完整磁盘号、分区偏移、精确 extent 长度和共享 GPT/MBR 强身份 token 匹配，任一关键字段或 token 缺失即失败关闭；盘符、列表索引、分区号、包含磁盘容量和 UI 显示容量均不得作为不变身份或续装依据。

### 正常系统端下载层

- `正常系统端/src/download/mod.rs`：下载模块声明，只暴露仍在生产调用图中的 aria2、配置、微软目录和服务端目录边界；不得恢复已删除且从未接入运行路径的独立下载管理器或 PE URL resolver 副本。
- `正常系统端/src/download/aria2.rs`：aria2 生命周期、WebSocket 下载控制、状态和速度进度；RPC 只监听回环地址并为每次进程启动生成高熵随机 secret，WebSocket 客户端的全部调用必须携带该 token，禁止开放跨源无鉴权 RPC；按每个已验证计划的 8/16/32 档位设置 `split`，并把 aria2 的 `max-connection-per-server` 安全限制为 16，不再硬编码单一线程配置。
- `正常系统端/src/download/config.rs`：在线系统、PE、软件、驱动及简易模式配置数据模型和本地配置加载；PE 目录缓存必须通过 `AppConfig` 的字段级原子更新入口保存，不得加载整份配置后再用可能过期的快照覆盖；系统、PE、分类和软件目录使用 `https://letrecovery.cloud-pe.cn/v4/` JSON，软件条目的静默参数、管理员要求与 `vm_tools` 标记均视为不可信输入，进入安装流程前必须转成 `lr-core::software_install` 的受检强类型数据，禁止回退旧 v1/v2 在线目录。
- `正常系统端/src/download/microsoft_catalog.rs`：每次启动或人工刷新时从微软 Update Metadata Service 获取当前 Windows 11 MCT 产品 CAB，并可附加 Windows 10 官方 CAB；验证更新身份、CAB 大小与 SHA-256、官方主机和重定向后，复用 `lr-core::windows_cabinet` 的 `SetupIterateCabinetW` 边界解析唯一 `products.xml`，只发布简中 x64 `CLIENTCONSUMER_RET` 长期 ESD；Windows 11 强制携带官方 SHA-256，Windows 10 22H2 旧目录只提供 SHA-1 时仅验证其格式并保持未声明 SHA-256 状态。

正常端 ViaPE 交接日志只能在配置与 BCD 两个事务均提交后截取，提交或回滚失败不得留下无主日志目录。PE `/AUTO` 与默认原生进度界面必须在格式化前建立同一终态日志守卫；日志只按本次 SessionId 与已持有目标/数据卷句柄发布，不得为发布日志重新枚举或认证安装目标。格式化或后续失败则把日志留在数据分区，日志发布本身始终是 warning-only，不能弹框或改变安装、清理和重启结果。
- `正常系统端/src/download/server_config.rs`：只通过固定 HTTPS `v3/index.json` 单请求获取 PE、API 系统镜像、软件、简易模式和显卡驱动目录，严格校验 schema、过滤禁用项并映射到既有配置契约；解析 `system_image_mode` 的 1=微软官方、2=API、3=合并语义，缺失时默认为 2，并在启动/人工刷新事务中解析官方目录或执行确定性合并；v3 请求、解析或结构校验失败时整体失败并保留上下文，禁止回退 v1/v2 多文件目录，也禁止把全空或部分吞错响应标记为加载成功。


### 正常系统端工具模块

- `正常系统端/src/utils/mod.rs`：正常端工具模块声明。
- `正常系统端/src/utils/cmd.rs`：仅保留创建隐藏控制台 `Command` 的薄兼容入口并直接复用 `lr-core::command::new_command`；已删除无调用的字符串 shell、spawn 和输出包装，新危险命令必须使用 `lr-core` 类型化边界。
- `正常系统端/src/utils/command.rs`：共享命令边界的兼容再导出。
- `正常系统端/src/utils/encoding.rs`：共享编码转换的兼容再导出。
- `正常系统端/src/utils/i18n.rs`：语言文件扫描、加载、切换、翻译和参数替换；始终列出内置 `zh-CN` 与 `zh-TW`，繁体缺失词条调用共享 NLS 转换，可选外部 `zh-TW.json` 仅作覆盖；同时内嵌 `en-US`、`ja-JP`、`ko-KR`、`fr-FR`、`de-DE` 完整发布词表，外部同名 JSON 只覆盖键且缺失或损坏时回退内嵌表；内置 `ko-KP` 彩蛋只替换经国际化层展示的界面文案，不得改写路径、分区名或安装配置业务值。
- `正常系统端/src/utils/dprk_easter_egg.rs`：正常端 `ko-KP` 彩蛋的可逆桌面副作用边界；从 EXE 内嵌 JPEG 原子发布到 LocalAppData，首次启用前保存当前壁纸路径，切回其他语言后恢复；同时校验并通过 Windows MCI 循环播放 `bin/dprk_easter_egg.mp3`，切换语言或退出窗口时关闭别名并释放文件；PE 端不得调用这些系统副作用，也不得打包该音频。
- `正常系统端/src/utils/logger.rs`：日志目录、滚动保留、格式、最新日志选择和脱敏 JSON 支持包导出；安装交接的异步写入屏障返回持有同一日志文件对象的快照，Windows 句柄只共享读写而拒绝 delete-share，并在同一句柄内完成 marker 检查和落盘，调用方不得退化为路径重开。
- `正常系统端/src/utils/path.rs`：exe、bin、用户管理 PE、受管 PE 下载缓存、工具、驱动、只读旧分区脚本兼容目录及临时目录定位；兼容目录不得被新配置暂存或执行。
- `正常系统端/src/utils/privilege.rs`：管理员权限检查和以管理员身份重启；`ShellExecuteW(runas)` 返回值小于等于 32 时必须报告提权失败，只有成功创建提升进程后当前进程才可退出。

### PE 端入口与核心

- `PE端/build.rs`：生成 PE 程序资源、清单、图标和可复现构建版本；日期版本随 PE 源码、清单和 `SOURCE_DATE_EPOCH` 重新求值，避免新二进制复用旧关于页版本；release 禁止测试权限 feature。
- `PE端/src/main.rs`：PE 进程入口、文件日志、panic 记录、语言检测、只读固件/PCI 探测、受校验 VMD 驱动加载、LRHC1 discovery、move-only authenticated task 转移、persistent PE payload 精确清理和原生 Win32进度页启动。严格单参数旧 `/PEINSTALL`、`--pe-install`、`/PEBACKUP`、`--pe-backup` 必须在任何日志、驱动、认证、UI 或 MessageBox 前大小写不敏感地无副作用退出；安装、备份和扩容只有 `/AUTO` 和默认入口可消费 typed task，死代码 `run_cli_mode` 不得恢复。维护 purpose 在认证后只对当前 locked BitLocker 卷 best-effort 逐项调用参数化 `manage-bde -unlock -recoverypassword`，不得记录 secret、不得使用 `-off` 或修改保护器；随后隐藏驻留而不创建 LetRecovery PE 进度窗口。PE 日志开头只记录 `BUILD_VERSION` 日期版本、架构及 Secure Boot/PCA 的只读状态或明确错误，禁止再输出 Cargo“包版本”；安装终态日志发布必须记录当前会话与最后一次成功发布路径，真实失败时优先打开目标系统或受验证数据卷上的正常端+PE 合并日志；写前失败尚无终态 finalizer 时，必须等待 worker 完全退出并刷新 PE 日志后在 RAM 盘生成同格式合并副本再弹窗，发布失败只能降级展示当前 PE 日志，不得改变安装结果。LRPE4 清理必须在 volume GUID namespace 中只接受与当前私有 WIM 的 authoritative `SessionId + operation + capsule SHA-256` 完全相等的唯一 journal，损坏、异内容和其它会话记录静默忽略；历史磁盘号、GUID、容量、extent 和布局摘要只保留兼容解析/诊断，不得参与跨启动匹配。清理仅删除该会话命名的 loader、ramdisk、私有 WIM/SDI 与 journal 并逐项回读，零个、多个精确匹配或真实清理失败均在首次目标写前失败关闭；全部授权项删除后只以非递归 `remove_dir` 删除已证明为空的私有根，根中有任何其它内容时原样保留。DrvLoad 非零时只允许在重新枚举的精确 VMD 代际 devnode 已由 Configuration Manager 证明全部运行且无问题时继续，诊断输出必须按 UTF-8/GBK 双路径正确解码。安装、备份、扩容只路由到单一原生进度页，不得恢复 egui/eframe/OpenGL；`non-elevated-tests` 独有的 `--ui-progress-preview` 必须在任何副作用前进入，release 不得包含该分支。`ci-automation` 还可从证据盘 `LR-CI\state\active-run.json` 读取唯一、严格格式、与已经认证 typed task 的当前 `SessionId` 完全相等的安装上下文：显式 `fault_injection=none` 表示已认证的正常成功运行，不得被当成未知故障；只有 `before_target_write`/`after_target_format` 才获得对应注入权限。旧会话、重复、其它未知值或格式错误记录不得获得上下文或注入权限，正式 PE 构建必须编译移除该能力。
- `PE端/src/app.rs`：PE 安装主工作流、共享无人值守生成和 `WorkflowSession`。只消费 move-only typed task；格式化或首次镜像写入前完成承重输入检查并自动收束尚可逆的本次 PE/config/marker/暂存事务，进入不可逆边界后不得伪造旧系统回退。个人文件保留必须在清理一次性启动载荷前完成全量只读计划，释放目标 marker 后再次确认同一卷，先清理精确产品残留、再调用 `lr-core::personal_files`；移动失败且逆序恢复完整才可走写前自动回退，逆序恢复不完整或任何快捷方式/旧系统删除已经开始都只能报告保留根与 partial state，禁止恢复旧系统或删除保留根。不格式化安装在首次目标写入前、完成同对象身份复核后，还必须清理目标卷 `LetRecovery_PE` 中上次任务遗留的精确产品普通文件；未知内容、目录和重解析点一律保留或拒绝，不得递归删除或扩大到其它路径。镜像释放、启动存储覆盖、用户明确要求的必需无人值守内容和引导属于承重结果；普通驱动、驱动附带 CAB、用户驱动及其它非关键高级项必须隔离单项失败、输出有界 warning 并继续。保留驱动只有逐 INF 隔离后仍有失败时才读取目标 DISM 候选；版本 2 清单中经拓扑证明的要求只要任一 Hardware/Compatible ID 被目标适用候选精确覆盖即接受，目标 inbox、同 OEM 或其它版本均等价，旧版无证明清单只诊断。真实启动路径缺失或因目标包详情查询失败而未知时先记录延迟失败，继续完成 BCDBoot、无人值守与最小系统配置，再在清理和自动重启前进入失败终态，保留认证暂存和日志供修复。镜像与引导完成后，会话 marker、诊断文件或自动暂存分区回收失败只能完成带 warning 并禁止自动重启，不得倒打整次安装；终态合并日志此时必须写 `cleanup=incomplete`，只有所有会话/全盘/认证暂存清理均成功才可写 `cleanup=verified`。Windows 10/11 内置无人值守必须写入已释放镜像的语言、区域、键盘和时区，不得生成广泛 PowerShell 清理；用户名与 RID-500 账户规则复用 `lr-core::unattend_account`，日志不得记录密码、恢复密钥或其它 secret。XP/NT5、Win7 UEFI/UefiSeven、PCA 与镜像 span 只消费 typed task 持有的受检输入，不得按目录重新发现。该会话集中持有单一 worker 启动门、进度与终态通道；UI 轮询须有消息数和时间片上限，连续进度按最新值合并，语义步骤和终态前提交已有样本，worker 真正退出前不得关闭进程，未收到终态即断线必须合成本地化失败并记录 journal；认证软件子集非空时，离线终态必须明确说明首次登录仍会继续安装软件，不得把 PE 的 100% 描述成首登任务也已完成。CI `after_target_format` 故障只允许在格式化 API 成功且目标 extent/文件系统/卷标回读通过后触发，必须生成真实失败终态并保持“旧系统回退已禁用”，不得在故障路径恢复旧系统或把已破坏状态伪装成写前失败。
- `PE端/src/workflow_journal.rs`：原子检查点与脱敏支持包实现；旧版依赖公开 INI 扫描选择数据卷的创建入口已停用，只有以后由 typed task 显式传入已按 `DataLocatorToken` 选定的根目录后才可重新启用，诊断功能不得自行扫描磁盘。
- `PE端/src/workflows/mod.rs`：PE worker 工作流模块边界和受限再导出。
- `PE端/src/workflows/backup.rs`：唯一生产 PE 备份执行链；只消费 LRHC1 认证后形成的 typed backup task，在捕获前把源与目标绑定为 volume-GUID 根并 fresh 证明两端未加密。运行到 X: 后、开始枚举源树前必须先按当前认证会话删除一次性 BCD/WIM/SDI/journal，并确认源卷根下 `LetRecovery_PE` 已不存在；清理失败或仍有未授权内容时在捕获前停止，禁止把产品私有启动载荷带入系统备份。WIM/ESD create/replace/append 都写目标同卷受保护 staging，捕获后依靠仍持有的源、staged、held-old 与目录句柄、完整镜像验证和 CAS 事务完成发布，不得重新枚举全部物理盘或因无关设备查询失败否决已经完成的捕获；create/replace 的完成态 catalog 必须精确为单个请求镜像，append 必须从绑定 staged file ID 且持续 held-old 的副本读取旧 catalog，完成后证明旧项逐项不变且只增加一个精确尾项，再复用 PREPARED journal、handle CAS、durable committed receipt 和实时身份崩溃恢复。成功后只按本次 SessionId 与持有的精确控制文件句柄清理；失败不得重启，SWM/GHO 不得回退旧盘符入口。
- `PE端/src/workflows/expand.rs`：PE 无损扩容 typed task 消费、authenticated canonical target 重绑、checked VDS 纯扩展调用及成功清理/重启协调；失败必须保留认证配置、marker 与可重试材料，不得用共用清理路径提前销毁授权。
- `PE端/src/core/mod.rs`：PE 核心模块声明。
- `PE端/src/core/account_fix.rs`：只读判断离线系统是否是可部署的新装模板。必须同时取得微软定义的 `ImageState` 和一次成功的真实 SAM 库存：`GENERALIZE/SPECIALIZE_RESEAL_TO_OOBE` 且 SAM 没有用户自有账户才允许新装无人值守；SAM 加载、结构、RID 记录或 Names 互证失败一律为 `Indeterminate`，继续核心安装但关闭所有账户/无人值守修改。RID 500–999 属于 Windows 内置账户；RID≥1000 还须排除共享策略列出的 `defaultuser0`、`WDAGUtilityAccount`、`DWM-n`、`UMFD-n` 等 Windows/安装程序身份，只有其余账户才是用户账户；不能把官方空 SAM、RID 500 或 `defaultuser0` 误判为已有用户。离线 SOFTWARE hive 路径直接交给真实 `RegLoadKeyW`，不得用 `Path::is_file` 或 canonicalize 抢先误阻断。
- `PE端/src/core/bcdedit.rs`：PE 中 ESP 定位挂载、BCD/BCDBoot 修复、活动分区和按目标系统版本选择 PCA 或 Vista/7/8/8.1 标准 UEFI 引导；另提供只接受受信 LRPE3 精确 loader/ramdisk GUID 的删除与 `/enum all` 回读边界，禁止 `{current}`、描述匹配或批量清理。ESP 枚举与盘符分配必须复用共享 VDS/WinAPI 边界并核对物理磁盘与偏移，分配前还必须捕获当前规范磁盘布局并通过 checked 边界精确授权该偏移；已有盘符只复用不移除，无盘符时必须由持有同一布局快照和预期物理身份的临时守卫覆盖所有返回路径并显式关闭，卸载失败必须阻止继续，不得在多次修复后留下额外盘符，也不得调用 DiskPart。已经移除 UI 授权入口的 `repair_boot.txt` 不再执行，PE 必须始终走内置参数化引导边界。
- `PE端/src/core/config.rs`：从 X: 固定 LRHC1/config/LRHM3 建立 move-only typed task；通过 `FindFirstVolumeW`/`FindNextVolumeW` 的当前 volume GUID namespace 分别扫描固定 data/target marker 名称，不假定候选卷必有 DOS 盘符，且只接受与 manifest 中独立 token 逐字节相等的唯一卷。认证配置中的 `PreservePersonalFiles` 必须严格解析为布尔值，并在为真时拒绝格式化、GHO/XP 和非单分区重装组合。维护 purpose 不扫描 locator，只有 manifest 声明 secret 时才锁定固定 `X:\LR_BitLockerKeys.txt`，逐字节复核长度/SHA-256 与规范 `LRBL1` 内容。错误内容、旧 INI/LRHA1、不可读或重解析同名文件全部静默忽略；不得用磁盘号、卷 GUID 路径、容量或布局定位普通 data/target，也不得用公共文件选择 operation。选定后从同一拒写/拒删句柄按 LRHM3 完成一次内容认证并向进度页报告 locator/artifact/finalize 阶段；不得重复哈希同一大镜像或把目录树复制到 X:。普通/双系统清理只删除 typed task 持有的 exact locator 对象，全盘首次拓扑写入前只释放 locator 句柄，不以逐文件删除成功作为 clean/delete 的新门禁。
- `PE端/src/core/custom_install.rs`：全盘重装和双系统 typed plan 的 PE 执行边界；双系统只消费正常端预创建且回读的 target，绝不 Shrink 或重建。全盘只处理独立 locator 唯一命中的用户确认磁盘，同盘暂存时禁止 clean 并保留精确既有 extent；需要共享层发现位置的新分区在当前 raw/provider free extent 交集内创建，已有 canonical caller envelope 的创建则直接受该 envelope 约束，布局 desired size 与功能最低容量分离。Windows 始终以镜像展开容量加 2 GiB 为最低值；ESP 按当前 logical sector 使用 200/300 MiB、MSR 使用 Win7 兼容的 128 MiB、BIOS System Reserved 至少 350 MiB，Data 保持有用容量门槛；每次创建前还必须为后续承重分区保留最低预算。provider 实际几何可不同于 desired，但必须处于授权范围、角色正确并满足当前与后续最低值。同盘暂存的回收接收卷只能从暂存之前当前已挂载的普通卷中选择最靠近暂存的一项；ESP、MSR、Recovery 等无盘符基础分区必须跳过，不能把它们误报为“缺少接收卷”，完整布局结束后仍没有合格普通卷时才失败。安装全部承重步骤成功后才按 exact recipient/free-gap/staging 当前关系回收暂存，允许非整 MiB 尾隙，失败只返回完成 warning。
- `PE端/src/core/disk.rs`：PE 分区枚举、样式判断、格式化、删除、扩容和临时分区回收；当前 PE 卷由共享 `GetWindowsDirectoryW` 边界识别，不得假定为 `X:`。安装目标由 typed task 的随机 marker 唯一匹配结果提供；紧邻首次写入处只确认当前盘符仍对应该 extent，并确认镜像源与当前 PE 不在目标卷。格式化通过实际 VDS 对象核对同一 extent，必要时强制卸载旧卷句柄，完成后回读 extent、文件系统和卷标；不得再升级为 GPT/MBR token、可选 StorageDeviceId 或完整物理盘库存的重复门禁，也不得在镜像、驱动、引导和高级项阶段重复认证。分区删除、扩容和自动暂存回收属于独立拓扑事务，仍须使用 typed manifest 中的受认证 canonical pair、卷句柄 IOCTL 邻接关系与操作后布局回读，不能依赖本地化工具输出或只比较分区号。已删除永不编译的旧 `LetRecovery_AutoCreated.marker`/`A:`…`Z:` 扫描与历史布局指纹回收链，禁止恢复或绕过 typed authorization 重新启用。
- `PE端/src/core/dism.rs`：PE 高层镜像释放、WIM/ESD staged 捕获和统一 WIM 引擎进度；SWM 安装只能把 typed task 持有的有序精确 span 路径传给 `apply_image_with_exact_swm_resources`，驱动 INF 与可选 CAB 也只能传入 manifest 中仍由 task 句柄锁定的精确路径集合，禁止调用会自行发现邻近文件或重新递归扫描公开目录的入口。备份捕获只作为事务执行器的镜像内容步骤，最终验证和发布由共享 staging/CAS 边界负责；PE 不保留未接入生产流程的独立原子捕获、SWM split 或镜像信息解析副本。
- `PE端/src/core/dism_exe.rs`：DISM.exe 参数、子进程输出、进度和错误解析，固定路径均不可用时用 `SearchPathW` 查找可执行文件而不启动 `where.exe`；typed ViaPE 安装的普通驱动使用微软支持的单次 `/Add-Driver` 加多个 `/Driver:<exact INF>`，批量失败后只逐个重试同一认证集合，并以角色中性的结构返回失败包，由 `app.rs` 结合拓扑清单与目标库存分类，禁止在命令层把未知包先写成“可选”或“承重”。当精简 WinPE 的 DISM API 不可用时，本文件还负责调用同宿主 `dism.exe /English /Get-Drivers /All`，再为每个去重后的 PublishedName 单独执行一次 `/Get-DriverInfo /Driver:<published INF>`；禁止在一条详情命令中拼接多个 `/Driver:`，单包失败保留其它候选与有界失败，不得复制外部 DismApi.dll 或只查询 OEM 包而漏掉 inbox。可选 CAB 只遍历上层传入的精确路径列表并返回成功数、失败总数和有界样例，禁止重新扫描公开目录、逐包洪泛日志或因一个非必要 CAB 中止已经释放的系统。通过只读 `/Get-Intl` 查询已释放镜像的国际化默认值，当旧 WinPE 无法加载目标镜像的国际化 provider 时，使用碰撞安全的临时 hive 名只读加载目标 SYSTEM/DEFAULT 注册表，严格验证安装语言、系统/用户区域、键盘和时区后供无人值守生成，DISM 与注册表两条路径都失败时必须保留双重错误并失败关闭；不得添加 `/ForceUnsigned` 或额外验签器。
- `PE端/src/core/driver.rs`：共享驱动实现的兼容再导出。
- `PE端/src/core/expand_move.rs`：PE 无损扩容的 canonical 目标授权和 checked VDS 执行边界；当前生产 LRPE4 路径只允许目标后方已经存在连续未分配空间的单 extent 纯扩展，执行前后都重绑并回读同一完整物理范围。保留的右移/左移块级搬移辅助实现不得使用固定 1 MiB/4 KiB/512 B 阻断：无缓冲原始 I/O 只能消费 `windows_storage` 权威 logical/physical sector 查询，按 logical sector 校验 offset/length/delta、按 physical sector 对齐 buffer，查询失败即停止。Shrink 错误或 success warning 后只按同一稳定分区 canonical 回读处理实际尾缩，禁止按请求值盲扩；离线数据已移到精确 raw extent 后，分区表重建仍必须逐字节匹配该真实数据范围，否则按本次 actual delta 清理错误表项并报告 partial state。相关辅助路径即使仍为测试/未来实现保留，也不得退回盘符、分区号或不完整布局授权。
- `PE端/src/core/ghost.rs`：PE 中 Ghost 镜像还原、保留的备份辅助、进度、取消和错误处理；GHO/GHS 安装只允许在 SYSTEM/Administrators protected exact session directory 的同一持续 guard 下运行，启动 Ghost 前实时目录全集必须与 manifest 有序 span 完全一致，使 Ghost 自身的 sibling 发现只能看到已认证集合。格式化后进入下一阶段前必须通过 Ghost 官方 `-chkimg,<file> -batch` 完整性检查，进程启动失败、非零退出码或文本错误都失败关闭，输出按原始字节使用 GBK 兼容解码；GHO 备份生产入口继续关闭。
- `PE端/src/core/pca_preflight.rs`：PE `/AUTO` 与默认原生安装工作流共用的 PCA 写盘前预检适配，验证正常端暂存的兼容包或安全获取匹配包，并映射本地化失败提示；不得重新暴露 PE 用户 CLI。
- `PE端/src/core/registry.rs`：共享离线注册表实现的兼容再导出。
- `PE端/src/core/system_utils.rs`：PE 端共享的可执行文件版本读取和离线 Windows 架构检测；当前系统版本、环境、临时目录与 scratch 均由各自真实调用边界直接处理，不保留无调用的诊断副本。

### PE 端 UI 与工具

- `PE端/src/native_ui/mod.rs`：PE 生产环境原生 Win32 UI 的模块边界，汇总局部主题、基础控件、窗口状态、共享窗口壳、长任务进度页、只读详情页和低分辨率/DPI 纯几何层；不得重新引入 egui、eframe、glow 或 OpenGL 回退。
- `PE端/src/native_ui/theme.rs`：PE 端 Inno Setup 6.7 Modern Windows 11 明暗色、统一绿色进度、与正常端主按钮一致的浅色 `#005FB8`/深色 `#4CC2FF` 强调色、DPI 尺寸和 GDI 画刷生命周期；PE 无可靠 Shell 主题服务时默认深色，并允许部署环境显式选择浅色。
- `PE端/src/native_ui/controls.rs`：PE 原生按钮、编辑框、下拉框、列表、复选框、单选框、进度条和分隔线的类型化创建与主题入口，统一使用 Microsoft YaHei UI、紧凑 DPI 尺寸和可测试状态映射，并提供按实际微软雅黑字体测量且受当前视口上限约束的命令按钮宽度；按钮以单次离屏合成实现 normal/hot/pressed/disabled 和不叠加第二焦点框的单边框，`TrackMouseEvent` 失败、取消、隐藏、禁用及销毁时清除热态；Edit 移除 `WS_BORDER`、统一安装 `WS_EX_CLIENTEDGE` 并通过 `SWP_FRAMECHANGED` 生效，完整保留 USER32 原生单行垂直居中和多行文字排版，不叠加手绘边框、圆角或单行强制绘制；ComboBox 闭合字段、独立 ComboLBox 弹层和 ListView 以 5px 逻辑圆角、固定 4 倍超采样及仅回贴四角/1px 外缘的方式绘制，不设置 HRGN、不裁滚动条、箭头或命中区，圆角外部始终回填当前页面背景；ListView 在主题入口显式设置表体、文字背景和文字色，即使空表也不得露出系统白底；下拉弹层与列表项通过所有者/自定义绘制复用主操作按钮 normal/hot 颜色，只有真实选中或当前悬停项获得强调，不得回退到系统蓝色，弹层重复展开或销毁时必须清理悬停状态；抗锯齿缓冲区尺寸使用溢出检查、小尺寸圆角受控件边界约束；进度条按调用方几何和完整胶囊圆角在同一 BGRA 像素表面一次性合成窗口背景、无独立描边的轨道和填充，长任务页保持紧凑的 10px 逻辑高度，轨道边缘只保留抗锯齿覆盖率过渡，填充圆角外部不得用矩形轨道色回贴并产生黑块；加载圈必须逐项复用 Cloud-MGR `ProgressRing` 的 16×16 视口、半径 7、1.5px 线宽、圆头和 2 秒 linear 关键帧：0% 为 0.01/43.97 dash 与 0°，50% 为 21.99/21.99 dash 与 450°，100% 回到 0.01/43.97 dash 且旋转继续到 1080°，伸缩与旋转必须在两段内同时线性推进，不得改成余弦缓动或视觉上先收缩再重新起转，并使用解析覆盖率而非 GDI 折线绘制；完成与失败徽标使用高分辨率下采样和高对比粗线条，确保勾号与叉号清晰，尚未执行的 Pending 状态必须只显示步骤文字而不绘制空心圆或其他占位图元；模块不执行任何安装、备份或磁盘命令。
- `PE端/src/native_ui/state.rs`：PE 原生 UI 的页面、命令栏和工作流类型边界；`NativeWindowState<W>` 分离高级选项、进度、错误、恢复页面导航与宿主持有的 worker/接收器状态，切页不得替换或重启工作流；后端选择和旧渲染器路由已经删除。
- `PE端/src/native_ui/details.rs`：PE P4 高级选项、错误与恢复信息的原生只读详情区；高级选项仅映射已校验 `InstallConfig` 并隐藏路径、凭据和配置内容，错误页只显示本地化摘要而把原始诊断留在日志，恢复页只显示检查点和安全收尾摘要并明确不承诺断点续做；P5 在低高度下按真实可用区收紧标题、说明、列表和备注，列表高度与两列总宽不得反向撑出内容区；不执行或重启工作流。
- `PE端/src/native_ui/layout.rs`：PE P5 不依赖 HWND 的生产进度页纯窗口几何，计算进度、真实详情命令栏、monitor work-area 居中和夹取；已删除没有运行入口的壳预览几何。步骤进度与总体进度必须使用相同的 30px 逻辑行结构，接收调用方按当前语言和实际 9px 逻辑字体测得的共同标签宽度，使右对齐标签、3px 逻辑间距和同宽 10px 逻辑高度胶囊进度条组成的完整可见块水平居中；不显示条内或条外百分比，禁止把总体标签单独放在进度条上方；宽高足够且全部步骤可见时，把标题、两行进度和使用 24px 逻辑行距的完整步骤列表作为一个略向上偏置的整体分配可用高度，不允许固定贴顶或把未使用高度全部堆在列表下方；纯进度页不得绘制步骤列表分隔线，也不得为已删除的底部状态文案或命令栏保留间距，详情页仍保留真实按钮所需命令栏；以低分辨率高 DPI 保守客户端区域覆盖 96–192 DPI，并分别以 144 DPI 的紧凑安装客户端验证十个步骤保持单列、紧凑备份客户端验证六个步骤完整可见且底部空白受限，保证标题、两组进度、步骤列表和命令栏不相交；只有真实空间不足时才由调用方进入最多两列或只保留总体进度的 compact 展示。
- `PE端/src/native_ui/progress.rs`：PE P3/P4/P5 安装、备份和无损扩容的原生 Win32 长任务页，复用共享 `WorkflowSession` 单次启动 worker，以 50ms 定时器合并既有消息并只更新变化的文字/进度区域；加载圈另由约 16ms 的高精度 waitable timer 独立生产帧（不支持时回退常规 waitable timer 或线程时钟），通过合并的 `WM_APP` 消息保证队列内最多一个待处理帧，只使步骤列表中真实 `InProgress` 行的 16px 状态图元失效，首次布局尚无当前步骤时必须在后续真实步骤转换中重新绑定非空动画矩形并重置该步骤的动画起点；加载圈、两条进度条、状态图元和父窗口底色必须统一经 `BeginPaint`/`EndPaint` 的 `WM_PAINT` 路径写入与无效区同尺寸的兼容内存位图并只 `BitBlt` 发布一次，禁止用 `GetDC` 绕过更新区直接画屏、先清背景再向屏幕补画进度条或在高频步骤切换中同步 `UpdateWindow`；内存位图创建失败时才允许在同一 `WM_PAINT` 内直接绘制回退，且每次只绘制与脏矩形相交的父窗口图元，不得让 16px 动画帧重复生成整条进度图；动态安装/备份步骤保留无分配的静态键，仅在步骤列表实际重绘时本地化，英语模式不得残留中文且轮询不得为翻译反复分配；顶部以 16px 逻辑标题字体显示助手名称，不创建具体步骤名称或“当前步骤”标题，两条进度信息分别以独立 9px 逻辑字体显示“步骤进度:”和“总体进度:”，运行时按当前语言测量共同标签宽度，使标签与进度条的完整可见块水平居中；标签向进度条右对齐，百分比数字完全隐藏，进度变化只重绘胶囊条；步骤文字必须使用 `SS_CENTERIMAGE` 在 24px 逻辑行内垂直居中，使其与同样按行中心定位的 16px 状态图元对齐；完成步骤文字和徽标使用统一绿色，只有真实 `InProgress` 步骤使用当前主题主按钮同款蓝色加载圈与文字，状态切换后必须先发布新语义再异步重绘子 `STATIC`，禁止同步逐控件重画阻塞 UI 线程或只重绘父窗口图标而遗留旧文字颜色；无损扩容的总进度直接跟随既有百分比；纯进度页不绘制步骤列表分隔线，不创建只读状态 Edit、不显示高级选项或客户区关闭按钮，也不创建“操作进行中”“正在完成清理和收尾操作”等底部状态文案或空状态栏，详细诊断写入日志；工作流运行时必须灰显标题栏系统关闭命令并继续拒绝 `WM_CLOSE`，只有终态且 worker 已安全结束后才能恢复，禁止用警示标语代替关闭边界；顶层样式必须移除 `WS_MAXIMIZEBOX`，不得显示或响应最大化按钮；默认窗口使用 480×440 逻辑尺寸和 440×430 逻辑最小尺寸，初始窗口通过当前 HWND 最近显示器的工作区居中，`WM_DPICHANGED` 建议矩形仍夹到同一工作区；窗口类不设置图标且顶层窗口使用无标题栏图标的对话框框架扩展样式；`non-elevated-tests` 的运行态预览必须复用生产加载圈、高精度动画调度、绘制路径和受限 `WorkflowSession` 消息接收路径，并注入不访问磁盘、固定停留在非 0/100 中段状态的合成进度洪峰与真实步骤转换，另提供失败状态视觉入口以验收叉号；两者均不启动 worker 或访问磁盘；消息循环异常仍等待同一 worker 真正结束；检查点、完成重启和底层错误语义保持不变。
  上一项所称 480×440 首选/440×430 最小尺寸仅适用于安装和扩容；六步骤备份页使用 480×360 首选/440×350 最小逻辑尺寸，避免在高 DPI 下继承安装页为更多步骤预留的大段底部空白。
- `PE端/src/native_ui/window.rs`：PE 原生 Win32 生产进度页共享的显示器工作区查询、窗口居中/夹取、DPI 缩放、标题栏主题和消息清理辅助；已删除没有运行入口的通用壳预览与其绘制/字体副本。窗口类和顶层 HWND 均不得设置应用图标，避免标题文字左侧保留图标槽。
  PE 终态错误交互补充约束：`native_ui/progress.rs` 必须等工作线程及其日志 finalizer 全部退出后，才为真实 Failed 发布正常端与 PE 端的合并日志并显示一次错误弹窗；warning、成功带警告和取消不触发，持久发布不可用时只能在 RAM 盘生成有界合并回退或明确提示日志无法发布，不得把未落盘路径伪装成可打开文件。
- `PE端/src/ui/mod.rs`：PE UI 模块声明。
- `PE端/src/ui/advanced_options.rs`：把高级选项应用到离线系统，包括驱动、CAB、注册表、无人值守和兼容修复；所有高级选项关闭时必须是真正空操作，不得仅为探测而加载离线 hive；历史 `remove_uwp_apps=true` 只在此调用共享精确 PFN 离线 AppX 清理并把普通失败记为结构化 warning，明确保留 Outlook 与 OneDrive，并在 Win11 默认用户首次生成前调用共享 Shell 默认值边界抑制推荐/动态内容，不再生成首次登录 PowerShell 广泛删除脚本或接线 Win32 OneDrive 卸载器；Defender 等可选清理失败只允许 warning 并继续，不得把已经完成的核心安装改判失败。随包存储驱动必须先按 PE 当前 SetupAPI PCI 硬件 ID 选择 Intel VMD 目录，未匹配、包缺失或 AMD/Apple/VirtIO 控制器均不得跨过 DISM 边界；内置 Win7 USB3 必须验证锁定资源并按目标架构与当前硬件 ID 选择子包，内置 NVMe 必须只按固定依赖顺序安装两个微软 CAB。用户驱动目录采用与保留驱动相同的“结构安全检查→标准 DISM→逐 INF 隔离→有界 warning”流程，Wi-Fi、网卡、打印机、VMware 等非启动存储包失败不得中止整次安装；只有启动存储覆盖、用户明确要求且决定系统可用性的固定兼容资源或确定性结构错误才可升级为失败。CAB 驱动只允许解包后交由 DISM 导入，禁止直接复制 SYS/INF/CAT 或猜测并手写服务注册表；临时解包目录必须使用碰撞安全目录且遍历错误不得静默忽略，注册表 hive 中途失败也必须尝试卸载并保留操作与重载错误上下文。XP/2003 驱动分支只能消费正常端已验证并写入受认证配置的 NT5 标记，禁止再因精简镜像或 GHO 缺少 `Windows\Boot` 猜测系统类型；Vista+ 目标缺少该目录只能作为诊断，真实引导修复 API/工具结果才决定承重步骤成败。
- `PE端/src/ui/progress.rs`：PE 安装/备份步骤及共享 `ProgressState` 纯状态模型，以英语资源完整性测试覆盖全部动态步骤键；安装把镜像释放作为主要总进度区间，备份把镜像捕获作为主要区间，快速前后置步骤只占少量权重；以显式 `has_current_step` 区分准备首帧和真实步骤，步骤归零及迟到/重复消息只能维持或提高总体进度并由全阶段转换测试覆盖，无损扩容的总进度直接采用后端百分比；不再包含渲染代码，可见进度统一由 `native_ui/progress.rs` 渲染。
- `PE端/src/utils/mod.rs`：PE 工具模块声明。
- `PE端/src/utils/command.rs`：共享命令边界的兼容再导出。
- `PE端/src/utils/encoding.rs`：共享编码转换的兼容再导出。
- `PE端/src/utils/i18n.rs`：按正常端已选择的语言代码加载 PE 翻译并完成参数替换；不再维护未接入 PE UI 的语言枚举、刷新或运行时切换 API。PE EXE 内嵌 `en-US`、`ja-JP`、`ko-KR`、`fr-FR`、`de-DE` 完整发布词表作为缺失键兜底，WIM 中的外部同名文件仍可覆盖同名键；内置 `zh-TW` 即使 WIM 没有外部语言表也通过共享 Windows NLS 完整转换，并允许外部同名词条覆盖；正常端传入 `ko-KP` 时只启用统一朝鲜文彩蛋文案，不执行桌面壁纸副作用。
- `PE端/src/utils/path.rs`：PE exe 和 bin 目录定位。
- `PE端/src/utils/reboot.rs`：共享 `pecmd.exe` 结束逻辑的兼容再导出。

## 维护热点与后续拆分方向

以下文件超过约 1,000 行或承担多个职责。修改时需要额外审阅，但不要一次性重写：

- `lr-core/src/boot_pca.rs`、`driver.rs`、`fveapi.rs`、`wimlib.rs`；
- `正常系统端/src/core/advanced_options.rs`、`core/bitlocker.rs`、`core/disk.rs`、`core/hardware_info.rs`、`core/image_verify.rs`、`core/quick_partition.rs`；
- `正常系统端/src/native_ui/window.rs`、`native_ui/theme.rs`；
- `PE端/src/app.rs`、`ui/advanced_options.rs`。

优先拆分纯解析/策略、状态模型、Windows API 适配、命令执行和 UI 渲染。拆分后必须更新上面的文件职责目录、模块导出和相关测试。

## 面向用户自定义的稳定边界

- 用户可通过 `assets/release/lang/` 增加或修改语言，但语言文件缺失键时必须安全回退。
- 安装前脚本仍只允许从既有受控目录加载；历史 DiskPart/批处理脚本不得再加载或执行，旧配置仅能触发明确的失败关闭提示。不要把任意服务端字符串直接当脚本执行。
- 用户驱动、CAB、无人值守文件和镜像属于不可信输入，必须验证路径、类型和存在性，错误不能影响其他磁盘。
- 正常端无人值守默认状态只依据 `config.json` 中的用户偏好和已选择的源镜像/安装介质；未选择镜像时必须保留配置偏好，目标分区旧系统中的 Panther/Sysprep 文件不得参与判断，源镜像自动探测也不得反向覆盖持久化偏好。
- 自定义下载源仍受 URL、文件名和完整性策略约束，不能通过自定义功能绕过 HTTPS 或已声明哈希。
- 兼容再导出文件虽然很短，但用于保持两端调用接口稳定，不得因“只有几行”随意删除。

正常端 ViaPE 安装的补充职责：`native_install_backend.rs` 必须严格加载当前目录项并交给 `pe.rs` 的私有快照边界；随包和下载缓存里的已有 WIM 都允许用户自定义，目录 MD5/SHA-256 只在下载边界校验，plan、run、GUI、CLI、安装、备份和维护入口均不得复核声明哈希或计划时旧字节。私有快照仍必须从普通非重解析文件复制并复读证明本次复制一致，快照句柄持续拒绝写入和删除共享，直到 BCD/持久载荷复制已经消费完毕后才能释放。

最后更新本文件时，应重新统计所有 Rust 文件，并确认职责目录没有遗漏。本文档本身的修改也必须接受 `git diff --check`。
